[tool.poetry]
name = "aws-eks-helm-deploy"
version = "1.1.1"
description = ""
authors = ["Yves Vogl <yves.vogl@adesso.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
