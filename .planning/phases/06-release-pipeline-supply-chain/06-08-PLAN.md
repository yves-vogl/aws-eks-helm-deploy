---
phase: 06-release-pipeline-supply-chain
plan: 08
type: execute
wave: 5
depends_on: []
files_modified:
  - SECURITY.md
autonomous: true
requirements: [SEC-09]
requirements_addressed: [SEC-09]

must_haves:
  truths:
    - "`SECURITY.md` removes the '(planned)' qualifier from the existing language about Private Vulnerability Reporting — Plan 06-10 documents the `gh api PUT /repos/.../private-vulnerability-reporting` command for the maintainer to enable it; once enabled, SECURITY.md MUST reflect the live state."
    - "SECURITY.md updates the 'What we do automatically' section to reflect Phase 6 deliverables: CodeQL is now live (was already shipped pre-Phase 6 — confirm), OpenSSF Scorecard is live (was already shipped — confirm), Trivy image scanning at build time AND on a scheduled cadence is live (Plan 06-01 + 06-07)."
    - "SECURITY.md adds language about GitHub Security Advisories as the public disclosure mechanism (SEC-09 contract) and links them to CHANGELOG patch entries."
    - "Disclosure timeline + supported versions sections remain UNCHANGED (already covered by the existing SECURITY.md)."
    - "No structural tests are needed for SECURITY.md — it's a Markdown narrative document. Markdown lint (if enabled via pre-commit) is sufficient."
  artifacts:
    - path: "SECURITY.md"
      provides: "Updated security policy reflecting Phase 6 live state (Private Vulnerability Reporting enabled, Trivy + Scorecard + CodeQL all live)"
      contains: "GitHub Private Vulnerability Reporting"
  key_links:
    - from: "SECURITY.md"
      to: ".github/workflows/security-rescan.yml"
      via: "Documents Trivy rescan as one of the automated security gates"
      pattern: "Trivy"
    - from: "SECURITY.md"
      to: ".github/workflows/scorecard.yml"
      via: "Documents OpenSSF Scorecard as a live gate"
      pattern: "OpenSSF Scorecard"
---

<objective>
Update `SECURITY.md` to reflect Phase 6 live state: GitHub Private Vulnerability Reporting is now the documented disclosure channel (Plan 06-10 enables it via `gh api`); Trivy image scan (PR-time via Plan 06-01 ci.yml + scheduled rescan via Plan 06-07 security-rescan.yml) is live; OpenSSF Scorecard (existing) is live; CodeQL (existing) is live. Add language tying CVE responses to GitHub Security Advisories with CHANGELOG patch entries.

Purpose: SEC-09 requires "`SECURITY.md` documents the disclosure flow". The existing SECURITY.md already covers most of this — Phase 6's job is to remove "(planned)" qualifiers and lock in the live state.

Output:
- Edited `SECURITY.md` — minimal edits to align with Phase 6 deliverables.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-release-pipeline-supply-chain/06-CONTEXT.md
@.planning/phases/06-release-pipeline-supply-chain/06-RESEARCH.md
@SECURITY.md
@CONTRIBUTING.md
</context>

<artifacts_this_phase_produces>
- Edited `SECURITY.md` only. No new files; no structural tests.
- Plan 06-10 maintainer doc will reference SECURITY.md content as part of the post-merge runbook.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update SECURITY.md — remove "(planned)" qualifiers + add Phase 6 live state</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (current — already covers the structure; only specific sections need edits)
    - .planning/phases/06-release-pipeline-supply-chain/06-RESEARCH.md ("Manual Maintainer Steps" §1 — Private Vulnerability Reporting enablement command)
    - .planning/phases/06-release-pipeline-supply-chain/06-CONTEXT.md (D7 + D8 — cosign verify + SBOM attestations as supply-chain assurance)
  </read_first>
  <action>
Edit the existing `SECURITY.md`. The changes are TARGETED — DO NOT rewrite the whole file. Specifically:

1. **"Reporting a vulnerability" section** (current lines 11–20): NO change needed. The existing flow (GitHub Private Vulnerability Reporting via the Security tab) is exactly right per Plan 06-10.

2. **"Disclosure timeline" section** (current lines 22–32): NO change needed.

3. **"Scope" section** (current lines 34–48): NO change needed — already lists "The build supply chain ... Dockerfile base-image pins, helm/helm-diff version pins" which is accurate post-Phase 6.

4. **"What we do automatically" section** (current lines 50–61): EDIT this section. Replace it with:

```markdown
## What we do automatically

This repository runs several automated security gates that may catch issues before a manual report is needed:

- **gitleaks** as a pre-commit hook (every commit + every CI run).
- **pip-audit** on every PR via `scripts/pip-audit-with-stale-check.sh` (Python dependency CVE scan with stale-ignore detection).
- **CodeQL** static analysis on every PR + weekly on `main` (Security tab → Code scanning alerts).
- **OpenSSF Scorecard** weekly (Security tab; live badge on the README).
- **Trivy** image scanning on every PR (image filesystem + Dockerfile + chart fixtures + secret-leak patterns) AND on a daily scheduled rescan against the published GHCR image (`ghcr.io/yves-vogl/aws-eks-helm-deploy:latest` and `:2`); findings upload to GitHub Code Scanning and CRITICAL/HIGH findings open auto-deduplicated GitHub Issues.
- **Cosign** keyless signing of the released image (Sigstore / Fulcio / Rekor) AND Cosign attestation of two SBOMs (SPDX + CycloneDX) AND SLSA build provenance via `actions/attest-build-provenance` — every release is verifiable via `cosign verify ghcr.io/yves-vogl/aws-eks-helm-deploy:<tag>` and `gh attestation verify`.
- **Cosign verify** as a PR gate (`.github/workflows/cosign-verify.yml`) — every PR runs `cosign verify` against the most recent published image to catch sign-chain regressions.
- **Dependabot** for `pip`, `docker`, and `github-actions` ecosystems with weekly grouping; auto-merge once required checks pass.

If your finding is something one of these tools should have caught and didn't, please mention that in the report — it helps us improve the gate.
```

5. **"Acknowledgements" section** (current lines 63–66): NO change needed.

6. **Verify the file**:
   - Markdown structure: headings hierarchy preserved (`# Security Policy` → `## Supported versions` → `## Reporting a vulnerability` → `## Disclosure timeline` → `## Scope` → `## What we do automatically` → `## Acknowledgements`).
   - No empty sections.
   - All hyperlinks remain valid (`https://github.com/yves-vogl/aws-eks-helm-deploy/security/...`).

7. Run pre-commit if configured: `uv run pre-commit run --files SECURITY.md` should pass (or be a no-op if no Markdown hooks).
  </action>
  <verify>
    <automated>test -f SECURITY.md</automated>
    <automated>grep -F 'GitHub Private Vulnerability Reporting' SECURITY.md</automated>
    <automated>grep -F 'Cosign' SECURITY.md</automated>
    <automated>grep -F 'SLSA build provenance' SECURITY.md</automated>
    <automated>grep -F 'OpenSSF Scorecard' SECURITY.md</automated>
    <automated>grep -F 'Trivy' SECURITY.md</automated>
    <automated>grep -F 'CodeQL' SECURITY.md</automated>
    <automated>grep -F 'cosign verify' SECURITY.md</automated>
    <automated>! grep -F '(planned)' SECURITY.md</automated>
    <automated>! grep -F '(Tier-2 Scorecard sprint)' SECURITY.md</automated>
    <automated>grep -F 'ghcr.io/yves-vogl/aws-eks-helm-deploy' SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md contains all 7 automated-gate references (gitleaks, pip-audit, CodeQL, Scorecard, Trivy, Cosign sign + verify, Dependabot).
    - NO `(planned)` qualifier anywhere.
    - NO `(Tier-2 Scorecard sprint)` qualifier (historic note from pre-Phase-6 state).
    - GHCR image reference present.
    - Markdown structure intact.
    - All existing sections (Supported versions, Disclosure timeline, Scope, Acknowledgements) preserved verbatim.
  </acceptance_criteria>
  <threat_model>
    Threat references:
    - V14 Configuration (RESEARCH §ASVS L1): SECURITY.md is the public-facing record of the security posture; misalignment with reality erodes trust.
    - SEC-09 contract: Private Vulnerability Reporting is the documented disclosure channel; SECURITY.md MUST reflect Plan 06-10's `gh api` enablement.
  </threat_model>
  <done>
    SECURITY.md is the Phase 6 live state; all "planned" qualifiers removed; new gates (Cosign sign + verify, Trivy rescan) documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public reader of SECURITY.md → repository disclosure flow | The document is the source-of-truth for how to report vulnerabilities. Inaccuracy could route reports to the wrong channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Information Disclosure | SECURITY.md misdirects vulnerability reports | mitigate | Direct link to `/security/` Private Vulnerability Reporting tab; existing wording preserved |
| T-06-08-02 | Repudiation | Document drift between SECURITY.md and actual gates | mitigate | Phase 6 verifier (end-of-phase) checks that every gate mentioned in SECURITY.md has a corresponding workflow file (manual review item) |
</threat_model>

<verification>
1. `grep -F 'Cosign' SECURITY.md` returns multiple hits.
2. `grep -F '(planned)' SECURITY.md` returns 0 hits.
3. Manual review by Yves: walk through the disclosure flow as described and confirm each step works (Security tab → Report a vulnerability → form submission).
</verification>

<success_criteria>
- SEC-09 contract: SECURITY.md is the live disclosure flow with all Phase 6 gates documented.
- Plan 06-10's `gh api PUT private-vulnerability-reporting` command will activate the feature; SECURITY.md is already aligned.
</success_criteria>

<output>
Create `.planning/phases/06-release-pipeline-supply-chain/06-08-SUMMARY.md` documenting:
- The list of automated gates now documented in SECURITY.md (gitleaks, pip-audit, CodeQL, Scorecard, Trivy, Cosign sign + verify, Dependabot).
- Plan 06-10 must enable Private Vulnerability Reporting via `gh api` — until that maintainer command runs, the Security tab → Report a vulnerability button may be hidden. Until then, SECURITY.md is technically accurate in describing the intended flow.
</output>
