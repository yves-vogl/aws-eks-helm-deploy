---
phase: 04-oidc-chart-source-extensions
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
files_whitelist:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
files_blacklist:
  - src/**
  - tests/**
  - Dockerfile
  - pyproject.toml
  - uv.lock
  - any other .md under .planning/phases/04-*
autonomous: true
requirements:
  - AUTH-04
tags:
  - roadmap
  - requirements
  - precedence
  - botocore
  - doc-edit
  - atomic-precursor

must_haves:
  truths:
    - "`.planning/ROADMAP.md` Phase 4 entry — Success Criterion 1 wording is REVISED to mirror the boto3/AWS CLI default credential resolver chain order (static keys win when both present; one-time WARN log surfaces the precedence). Original wording (`OIDC wins deterministically`) is REMOVED."
    - "`.planning/ROADMAP.md` Phase 4 entry — a single-line `> 2026-06-18 revision:` blockquote note is added immediately under SC1, pointing at `.planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md` D1 for the full rationale."
    - "`.planning/REQUIREMENTS.md` — the AUTH-04 row in the v1 Requirements section is REVISED to the same new wording (`Strategy selection follows the boto3 / AWS CLI default credential resolver chain; when both static keys AND an OIDC token are present, static keys win — same behaviour as the AWS CLI itself; a one-time WARN log surfaces the precedence.`)."
    - "`.planning/REQUIREMENTS.md` traceability table — the `AUTH-04 | Phase 4 | Pending` row is UNCHANGED in shape; only the wording above the table is revised. Plan-Checker confirms: `grep -c '^| AUTH-04' .planning/REQUIREMENTS.md == 1`."
    - "The file `.planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md` is UNCHANGED — D1 already documents the revision; this plan is the deliberate ROADMAP + REQUIREMENTS sync to honor D1's Plan-Check obligation."
    - "No other phase's ROADMAP entry is touched. Phases 1-3, 5-7 entries are byte-stable. `git diff --stat .planning/ROADMAP.md` shows additions/deletions ONLY in the Phase 4 block."
    - "Resulting word-search assertions pass: `grep -F 'static keys win' .planning/ROADMAP.md` returns >= 1 hit AND `grep -F 'static keys win' .planning/REQUIREMENTS.md` returns >= 1 hit. The original phrase `OIDC wins deterministically` is REMOVED from both files (`grep -F 'OIDC wins deterministically' returns ZERO hits in both files`)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Revised Phase 4 Success Criterion 1 + a one-line revision-note blockquote pointing at 04-CONTEXT.md D1."
      contains: "static keys win"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Revised AUTH-04 row wording (same shape as the ROADMAP SC1 revision)."
      contains: "static keys win"
  key_links:
    - from: ".planning/ROADMAP.md Phase 4 SC1"
      to: ".planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md D1"
      via: "blockquote `> 2026-06-18 revision: …` pointing at the CONTEXT.md path"
      pattern: "2026-06-18 revision"
    - from: ".planning/REQUIREMENTS.md AUTH-04"
      to: ".planning/ROADMAP.md Phase 4 SC1"
      via: "matching wording — both files describe the revised AUTH-04 contract identically"
      pattern: "static keys win"
---

<objective>
Ship the atomic ROADMAP + REQUIREMENTS doc-edit that supersedes the original "OIDC wins deterministically" wording with the revised "static keys win — same as AWS CLI" wording from CONTEXT D1. Single commit. This plan is the **Phase 4 precursor** — it MUST land as the first commit of the phase, atomic with the OIDC strategy code change that follows in Plan 04-03. Per CONTEXT D1 Plan-Check obligation, the doc-edit cannot be deferred or merged with later plans — a downstream verifier reading the original ROADMAP wording would incorrectly flag Plan 04-03's correct behaviour as a regression.

Purpose: Closes RESEARCH §R1 (atomic doc-edit before code) and R10 (Plan-Checker AUTH-04 verification path uses revised wording). Locks AUTH-04 (revised). Wave 1 — first plan within the wave, executed before 04-02 / 04-04 / 04-05.

Output:
- `.planning/ROADMAP.md` — Phase 4 SC1 revised + one-line revision-note blockquote.
- `.planning/REQUIREMENTS.md` — AUTH-04 row revised to match.
- ZERO code changes. ZERO test changes. The OIDC code lives in Plan 04-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md
@.planning/phases/04-oidc-chart-source-extensions/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 04-1-01: Revise ROADMAP Phase 4 SC1 + REQUIREMENTS AUTH-04 wording (atomic doc-edit)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md (D1 — full revised wording for SC1 + AUTH-04 + rationale; the "ROADMAP REVISION" callout block is the source-of-truth)
    - .planning/phases/04-oidc-chart-source-extensions/04-RESEARCH.md (§1 — verified botocore chain order at boto3 1.43.31; §R1 + §R10 — Plan-Check obligations this task closes)
    - .planning/ROADMAP.md (existing Phase 4 entry, lines ~71-84; SC1 is the line starting `1. `src/aws_eks_helm_deploy/auth/oidc.py` adds `OidcWebIdentityStrategy`; when both ...`)
    - .planning/REQUIREMENTS.md (existing AUTH-04 row at line 26: `- [ ] **AUTH-04**: When both static keys and an OIDC token are present, OIDC wins (deterministic strategy selection).`)
  </read_first>
  <behavior>
    Two atomic edits, both committed in this task. NO production code edits. NO test edits.

    **Edit 1 — `.planning/ROADMAP.md` Phase 4 SC1:**

    The existing SC1 line ("`src/aws_eks_helm_deploy/auth/oidc.py` adds `OidcWebIdentityStrategy`; when both `BITBUCKET_STEP_OIDC_TOKEN` and static keys are present, OIDC wins deterministically (AUTH-03, AUTH-04); misconfigurations ...") is REWRITTEN to:

      > `src/aws_eks_helm_deploy/auth/oidc.py` adds `OidcWebIdentityStrategy`; when ONLY `BITBUCKET_STEP_OIDC_TOKEN` + `OIDC_AUDIENCE` + `ROLE_ARN` are set (no static keys), the pipe exchanges the token for STS credentials via `AssumeRoleWithWebIdentity` (AUTH-03); when BOTH static keys AND an OIDC token are present, **static keys win** — mirrors the boto3 / AWS CLI default credential resolver chain (env-var provider precedes web-identity provider in `botocore.credentials.create_credential_resolver`); a one-time WARN log (`auth.precedence.static_keys_won_over_oidc`) surfaces this precedence (AUTH-04); misconfigurations (`ROLE_ARN` without base creds, `OIDC_AUDIENCE` without `ROLE_ARN`, `BITBUCKET_STEP_OIDC_TOKEN` without `ROLE_ARN`) raise `ConfigurationError` with a clear message before any AWS API call (AUTH-06).

    Immediately under SC1, add a single blockquote line (preserve existing markdown indentation of the `**Success Criteria**` list — the blockquote sits BELOW SC1 at the same indent level as the criteria text):

      > 2026-06-18 revision: AUTH-04 wording superseded — see `.planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md` D1 for full rationale (boto3 default chain mirror; principle-of-least-surprise for AWS engineers).

    SC2, SC3, SC4 are UNCHANGED. The `**Plans**: TBD` line, the `**Risks**` block, and Phase 4 sub-headers are all UNCHANGED.

    **Edit 2 — `.planning/REQUIREMENTS.md` AUTH-04 row (the v1 Requirements / AWS Authentication subsection, line 26):**

    The existing line:
    `- [ ] **AUTH-04**: When both static keys and an OIDC token are present, OIDC wins (deterministic strategy selection).`

    is REWRITTEN to:

    `- [ ] **AUTH-04**: Strategy selection follows the boto3 / AWS CLI default credential resolver chain; when both static keys AND an OIDC token are present, **static keys win** — same behaviour as the AWS CLI itself. A one-time WARN log (`auth.precedence.static_keys_won_over_oidc`) surfaces the precedence so consumers who set both by accident can see why their OIDC token was ignored. **(Revised 2026-06-18 from "OIDC wins deterministically" — see `.planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md` D1.)**`

    The AUTH-04 row in the bottom traceability table (currently `| AUTH-04 | Phase 4 | Pending |`) is UNCHANGED in shape; only the wording above the table is revised.

    **Constraints enforced:**
    - NO other ROADMAP phase entries are touched (Phases 1-3 + 5-7 byte-stable).
    - NO other REQUIREMENTS row is touched (AUTH-03, AUTH-05, AUTH-06, AUTH-07 byte-stable; CHART-02..04 byte-stable; all other REQs byte-stable).
    - NO files outside `.planning/ROADMAP.md` + `.planning/REQUIREMENTS.md` are modified. The whitelist gate enforces this.
    - The revised wording must contain the exact phrases:
      - `static keys win` (lowercase; serves as the Plan-Checker grep target)
      - `auth.precedence.static_keys_won_over_oidc` (the structlog event name, surfaces for cross-referencing the WARN log in 04-03)
      - `2026-06-18 revision` (the date marker, makes the revision discoverable by future readers)
    - The original phrase `OIDC wins deterministically` MUST be removed from BOTH files — Plan-Checker greps for it and fails the PR if any occurrence remains.
  </behavior>
  <action>
    Edit `.planning/ROADMAP.md`:
    - Locate the Phase 4 block (`### Phase 4: OIDC & Chart Source Extensions` ... through ... `### Phase 5: ...`).
    - Replace SC1 (the single numbered line starting `1. `src/aws_eks_helm_deploy/auth/oidc.py` ...`) with the revised SC1 wording from the behavior block above. Preserve markdown list indentation (`  1. `).
    - Add the blockquote revision-note line immediately below the revised SC1 line (still inside the `**Success Criteria** (what must be TRUE):` list block; the blockquote uses `> ` prefix at the same indent as the SC1 text).
    - DO NOT touch SC2 / SC3 / SC4 / Risks / Goal / Depends-on / Requirements / Plans.

    Edit `.planning/REQUIREMENTS.md`:
    - Locate the AUTH-04 row in the v1 Requirements section (line ~26; under `### AWS Authentication (AUTH)`).
    - Replace the row with the revised wording from the behavior block above.
    - DO NOT touch the traceability table at the bottom (the `| AUTH-04 | Phase 4 | Pending |` row stays as-is).

    Verify before commit:
    - `grep -F "static keys win" .planning/ROADMAP.md` returns >= 1 hit.
    - `grep -F "static keys win" .planning/REQUIREMENTS.md` returns >= 1 hit.
    - `grep -F "OIDC wins deterministically" .planning/ROADMAP.md` returns 0 hits.
    - `grep -F "OIDC wins deterministically" .planning/REQUIREMENTS.md` returns 0 hits.
    - `grep -F "auth.precedence.static_keys_won_over_oidc" .planning/ROADMAP.md` returns >= 1 hit.
    - `grep -F "2026-06-18 revision" .planning/ROADMAP.md` returns >= 1 hit.
    - `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows changes only in those two files.
    - `git diff .planning/ROADMAP.md` shows additions/deletions only within the Phase 4 block (no Phase 1-3 / 5-7 lines touched).

    DO NOT touch any other file. The whitelist enforces this; the Plan-Checker grep-audit confirms.

    No tests to run (no code changed). No coverage gate. The verification is grep + git diff.
  </action>
  <verify>
    <automated>grep -F "static keys win" .planning/ROADMAP.md &amp;&amp; grep -F "static keys win" .planning/REQUIREMENTS.md &amp;&amp; ! grep -F "OIDC wins deterministically" .planning/ROADMAP.md &amp;&amp; ! grep -F "OIDC wins deterministically" .planning/REQUIREMENTS.md &amp;&amp; grep -F "auth.precedence.static_keys_won_over_oidc" .planning/ROADMAP.md &amp;&amp; grep -F "2026-06-18 revision" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    `.planning/ROADMAP.md` Phase 4 SC1 is revised; a 2026-06-18 revision blockquote line is added; `.planning/REQUIREMENTS.md` AUTH-04 row is revised to match; the original "OIDC wins deterministically" phrase is gone from both files; all grep assertions in `<verify>` pass; no other file is touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no runtime boundary) | This plan is a doc-edit only; no runtime trust boundary is crossed. The ONLY trust boundary at play is the editor's: the revised wording must match CONTEXT D1 exactly so downstream verifiers (Plan-Checker reading ROADMAP, gsd-verify-work asserting AUTH-04) read the same contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Doc-edit drifts from CONTEXT D1 wording | mitigate | Behavior block above quotes the exact replacement wording verbatim from CONTEXT D1; Plan-Checker greps for the load-bearing phrases (`static keys win`, `auth.precedence.static_keys_won_over_oidc`, `2026-06-18 revision`) as a structural check. |
| T-04-01-02 | Information Disclosure | None — doc-edit reveals no secrets | accept | Doc-edit only; no credential material involved. |
| T-04-01-03 | Repudiation | Future PR reverts the revision silently | mitigate | The `2026-06-18 revision:` blockquote in ROADMAP + the `(Revised 2026-06-18 from "OIDC wins deterministically" ...)` parenthetical in REQUIREMENTS make the revision discoverable by `git log -G "2026-06-18 revision"` and survive a copy/paste-style revert review. |
| T-04-01-SC | Tampering | None (no package install) | n/a | No npm/pip/cargo install in this plan. |
</threat_model>

<deviations>
## Documented Deviations from CONTEXT / RESEARCH

### Deviation 1: SC1 revision is INLINE in ROADMAP (NOT a separate changelog entry)

**Alternative considered:** add a top-level "Phase 4 Revisions" section at the bottom of ROADMAP.md and leave SC1 byte-stable with a `[REVISED — see Revisions]` marker.

**Plan response:** keep the revision INLINE in SC1. Rationale: future readers of the ROADMAP scan top-down, encounter SC1 in context, and form a model of what Phase 4 must deliver. A bottom-of-file revision section is easy to miss. The blockquote `> 2026-06-18 revision:` line below SC1 makes the change discoverable without breaking the reading flow.

**Action item:** none — surface-level choice documented for code-reviewer awareness.

### Deviation 2: REQUIREMENTS.md AUTH-04 wording includes an explicit `(Revised 2026-06-18 ...)` parenthetical

**Alternative considered:** silently revise the row text and rely on `git log` to surface the change.

**Plan response:** include the parenthetical inline. Rationale: REQUIREMENTS.md is the canonical reference consumers read; the parenthetical leaves a self-documenting marker so a v1 consumer porting their pipe.yml understands the revision without needing to read CONTEXT.md.

**Action item:** none — surface-level choice documented.

### Deviation 3: This plan ships in TWO files (NOT one)

**CONTEXT D1 implies one commit, but doesn't mandate one file.** This plan touches both `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` because both contain duplicated AUTH-04 wording. Editing only one would leave the other stale.

**Plan response:** edit BOTH files in a single commit. The git history shows ONE atomic commit with both files in the diff. Plan-Checker confirms via `git show --stat`.
</deviations>

<dependencies_on_prior_plans>
## Prior Plan Inputs

- **Phase 1-3 ROADMAP entries:** unchanged. This plan does NOT touch Phases 1-3, 5-7. The whitelist enforces this; git diff confirms.
- **Phase 4 CONTEXT.md (04-CONTEXT.md):** read-only INPUT — D1 is the source-of-truth for the revised wording. This plan COPIES D1's revised wording into ROADMAP + REQUIREMENTS; it does NOT modify CONTEXT.md.
- **Phase 4 RESEARCH.md (04-RESEARCH.md):** read-only INPUT — §1 verifies the boto3 chain order at the pinned version (1.43.31); the verified-source citation is what justifies "static keys win" as the canonical AWS behaviour.

## NOT dependencies

- This plan does NOT depend on any code change. It touches NO `.py` file. It runs no tests.
- This plan does NOT depend on Plan 04-02, 04-03, 04-04, 04-05, 04-06, or 04-07 — it lands FIRST within Wave 1.
</dependencies_on_prior_plans>

<dependencies_on_other_phases>
## Forward Compatibility

- **Plan 04-03 (`OidcWebIdentityStrategy` + `select_strategy` integration):** consumes the revised AUTH-04 wording. The 04-03 unit test `test_select_strategy_static_keys_win_over_oidc_and_emits_warn` is the runtime assertion of the revised wording.
- **Phase 7 docs site (`docs/guides/auth-resolver-chain.md`):** the full resolver chain table from CONTEXT D1 is surfaced there. This plan's REQUIREMENTS revision is the upstream reference Phase 7 cites.
- **`gsd-verify-work` for Phase 4:** reads the REVISED AUTH-04 wording from REQUIREMENTS.md when asserting AUTH-04 is closed. Per RESEARCH §R10, the verifier MUST NOT assert "OIDC wins" — that wording is GONE after this commit.

## Stable contracts (do NOT refactor without bumping major)

- The exact load-bearing phrases in the revised wording:
  - `static keys win` — Plan-Checker grep target; appears in both files.
  - `auth.precedence.static_keys_won_over_oidc` — the structlog event name; appears in ROADMAP SC1 AND in 04-03's `auth/__init__.py::select_strategy` WARN-log emit site.
  - `2026-06-18 revision` — the date marker in ROADMAP that makes the revision discoverable by `git log -G`.
- The `| AUTH-04 | Phase 4 | Pending |` traceability-table row is UNCHANGED — `gsd-verify-work` flips `Pending → Closed` at phase end (Phase 4 PR merge).
</dependencies_on_other_phases>

<known_stubs>
## Known Stubs / Scaffolds

None — this plan ships a complete doc-edit. There are no placeholder lines, TODO markers, or "Phase X" forward-references inside the revised SC1 wording.

The blockquote `> 2026-06-18 revision: ...` IS a forward-reference of sorts (it points at 04-CONTEXT.md D1), but CONTEXT.md already exists in this phase directory and is read-only INPUT to this plan. The pointer is stable.
</known_stubs>

<verification>
- `grep -F "static keys win" .planning/ROADMAP.md` returns >= 1 hit.
- `grep -F "static keys win" .planning/REQUIREMENTS.md` returns >= 1 hit.
- `grep -F "OIDC wins deterministically" .planning/ROADMAP.md` returns 0 hits.
- `grep -F "OIDC wins deterministically" .planning/REQUIREMENTS.md` returns 0 hits.
- `grep -F "auth.precedence.static_keys_won_over_oidc" .planning/ROADMAP.md` returns >= 1 hit.
- `grep -F "2026-06-18 revision" .planning/ROADMAP.md` returns >= 1 hit.
- `git diff --stat HEAD -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows BOTH files in the diff; no other file appears.
- `git diff HEAD -- .planning/ROADMAP.md | grep "^+" | grep -v "^+++" | wc -l` is small (< 20 added lines — the revision is targeted, not a rewrite).
- Existing Phase 1-3 + Phase 5-7 ROADMAP entries are byte-stable: `git diff HEAD -- .planning/ROADMAP.md` shows added/deleted lines ONLY within the `### Phase 4:` block.
- No production code changed: `git diff --stat HEAD -- src/ tests/ Dockerfile pyproject.toml uv.lock` is empty.
</verification>

<success_criteria>
- AUTH-04 wording in ROADMAP.md + REQUIREMENTS.md mirrors the boto3 / AWS CLI default credential resolver chain order — "static keys win when both static keys AND an OIDC token are present, with a one-time WARN log surfacing the precedence."
- The original `OIDC wins deterministically` phrase is REMOVED from both files.
- A discoverable revision marker (`2026-06-18 revision: ...`) is added inline in ROADMAP SC1 and as a parenthetical in REQUIREMENTS AUTH-04.
- NO production code changed; NO tests changed; NO other phase's entries touched.
- Plan-Checker greps + git diff confirm the doc-edit is surgical.
- This is Phase 4's commit #1.
</success_criteria>

<artifacts>
## Artifacts this plan produces (Plan 04-01 scope)

**Files (modified):**
- `.planning/ROADMAP.md` — Phase 4 SC1 revised + revision-note blockquote.
- `.planning/REQUIREMENTS.md` — AUTH-04 row revised.

**Files (new):** none.

**Out of scope for this plan:**
- `auth/oidc.py` + `select_strategy` integration (Plan 04-03).
- Any test, code, Dockerfile, settings edit.
- Phase 7's `docs/guides/auth-resolver-chain.md` (Phase 7 scope).
- Updating CHANGELOG.md / semversioner JSON (Phase 4 PR-close step; handled by release-please flow).
</artifacts>

<output>
On completion, create `.planning/phases/04-oidc-chart-source-extensions/04-01-SUMMARY.md` recording:
- The exact new wording of SC1 (so future readers don't need to chase the git history).
- The exact new wording of REQUIREMENTS AUTH-04 row.
- Confirmation that all `<verify>` grep assertions passed.
- Confirmation that `git diff --stat HEAD -- src/ tests/ Dockerfile pyproject.toml uv.lock` is empty (no code change).
- Confirmation that no other phase's ROADMAP block was touched.
- A one-line cross-reference to Plan 04-03 (`auth/oidc.py` + `select_strategy` ships there).
</output>
