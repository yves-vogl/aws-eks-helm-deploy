---
phase: 04-oidc-chart-source-extensions
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guides/oidc-setup.md
  - tests/unit/test_iam_trust_policy_template.py
files_whitelist:
  - docs/guides/oidc-setup.md
  - tests/unit/test_iam_trust_policy_template.py
files_blacklist:
  - src/**
  - Dockerfile
  - pyproject.toml
  - uv.lock
  - .planning/**
  - tests/unit/test_settings.py
  - tests/unit/test_auth_init_select_strategy.py
  - tests/unit/test_auth_oidc.py
  - tests/unit/test_chart_*.py
  - tests/unit/test_helm_*.py
  - tests/integration/**
  - tests/acceptance/**
autonomous: true
requirements:
  - AUTH-05
tags:
  - iam
  - trust-policy
  - bitbucket-oidc
  - documentation
  - stringlike-erratum
  - phase-4-draft
  - phase-7-polish

must_haves:
  truths:
    - "`docs/guides/oidc-setup.md` exists and contains a single fenced ```jsonc IAM trust-policy block parseable as valid JSON after removing the JSONC comment markers (if any are present)."
    - "The trust-policy block has the 5 required placeholders ALL APPEARING VERBATIM: `<ACCOUNT_ID>`, `<WORKSPACE>`, `<OIDC_AUDIENCE>`, `<BITBUCKET_WORKSPACE_UUID>`, `<BITBUCKET_REPO_UUID>` — CONTEXT D4 Plan-Check obligation (any deviation rejected). The unit test asserts presence via `grep` + parsed-JSON inspection."
    - "The `aud` condition lives under `StringEquals` with the value `<OIDC_AUDIENCE>` (literal placeholder)."
    - "The `sub` condition lives under `StringLike` (NOT `StringEquals` — RESEARCH §2 erratum / CONTEXT D4 erratum) with the value `{<BITBUCKET_WORKSPACE_UUID>}:{<BITBUCKET_REPO_UUID>}:*` — the wildcard `*` ONLY makes sense under `StringLike` because IAM only treats `*` as wildcard there."
    - "The policy `Action` is `sts:AssumeRoleWithWebIdentity`."
    - "The `Principal.Federated` value matches the Bitbucket OIDC issuer pattern `arn:aws:iam::<ACCOUNT_ID>:oidc-provider/api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc` (the canonical form as of 2026-06-18 per RESEARCH §2 + Atlassian docs)."
    - "The `Version` field is `2012-10-17` (standard IAM policy version)."
    - "`tests/unit/test_iam_trust_policy_template.py` exists with 5 assertions: 1) JSON parses, 2) 5 placeholders all present, 3) `sub` under `StringLike` (NOT `StringEquals`), 4) `Action` is `sts:AssumeRoleWithWebIdentity`, 5) `Principal.Federated` matches Bitbucket OIDC issuer pattern."
    - "The doc page also includes ABOVE the JSON block a short prose paragraph (3-5 sentences) explaining: what the template is (IAM trust policy for Bitbucket Pipelines OIDC), how to use it (replace the 5 placeholders, paste into AWS IAM console or Terraform), what each placeholder represents, and a one-line link forward note saying 'Polished documentation site lands in Phase 7 (`docs/guides/oidc-setup.md` is drafted here)'."
    - "The doc page does NOT include the Terraform companion snippet — CONTEXT D4 explicitly allows deferring it; Plan 04 defers to Phase 7 per Deviation 1 below."
    - "Coverage on the `tests/unit/test_iam_trust_policy_template.py` file itself is 100% (trivial — all 5 assertions are smoke-test asserts)."
    - "The doc page lives at the exact path `docs/guides/oidc-setup.md` (CONTEXT D4 explicit path); no other markdown file is touched."
  artifacts:
    - path: "docs/guides/oidc-setup.md"
      provides: "Drafted Bitbucket-OIDC IAM trust-policy template (fenced ```jsonc block) + a 3-5-sentence prose intro + forward-reference to Phase 7 polish."
      contains: "<BITBUCKET_WORKSPACE_UUID>"
      min_lines: 30
    - path: "tests/unit/test_iam_trust_policy_template.py"
      provides: "5 structural assertions on the trust-policy template extracted from docs/guides/oidc-setup.md."
      contains: "StringLike"
      min_lines: 50
  key_links:
    - from: "docs/guides/oidc-setup.md"
      to: "Bitbucket OIDC issuer URL"
      via: "literal `api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc` in Principal.Federated"
      pattern: "api.bitbucket.org/2.0/workspaces"
    - from: "docs/guides/oidc-setup.md"
      to: "StringLike for sub condition"
      via: "RESEARCH §2 erratum / CONTEXT D4 erratum — the wildcard `*` requires StringLike"
      pattern: "StringLike"
    - from: "tests/unit/test_iam_trust_policy_template.py"
      to: "docs/guides/oidc-setup.md"
      via: "test reads the markdown file and extracts the first ```jsonc block via regex"
      pattern: "oidc-setup.md"
---

<objective>
Ship the documented Bitbucket-Pipelines OIDC IAM trust-policy template at `docs/guides/oidc-setup.md`, with the 5 required placeholders (`<ACCOUNT_ID>`, `<WORKSPACE>`, `<OIDC_AUDIENCE>`, `<BITBUCKET_WORKSPACE_UUID>`, `<BITBUCKET_REPO_UUID>`) and the corrected `StringLike` operator for the `sub` condition (CONTEXT D4 erratum / RESEARCH §2 correction — the wildcard `*` in `{workspace_uuid}:{repo_uuid}:*` requires `StringLike`, not `StringEquals`). A unit test parses the template and asserts: JSON validity, presence of all 5 placeholders, `sub` under `StringLike`, Action is `sts:AssumeRoleWithWebIdentity`, Principal.Federated matches the current Bitbucket OIDC issuer pattern.

This plan ships the DRAFT — full prose polish + mkdocs integration + Terraform companion lands in Phase 7. The IAM trust template itself is FINAL (no Phase 7 wording changes); only surrounding prose is "drafted, polished later."

Purpose: Locks AUTH-05 (documented trust-policy template). Mitigates the CONTEXT D4 erratum (the StringEquals-on-sub-claim wildcard footgun would silently allow any step UUID under StringEquals). Wave 1 leaf — independent of all other Phase 4 plans; lands in parallel with 04-01, 04-02, 04-05.

Output:
- `docs/guides/oidc-setup.md` — new markdown file (~30-50 LOC: prose intro + ```jsonc block + forward-reference).
- `tests/unit/test_iam_trust_policy_template.py` — new test file with 5 assertions.
- ZERO production code changes. ZERO new Python deps. ZERO Dockerfile changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md
@.planning/phases/04-oidc-chart-source-extensions/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-4-01: Ship docs/guides/oidc-setup.md with the IAM trust-policy template + 5-assertion unit test</name>
  <files>docs/guides/oidc-setup.md, tests/unit/test_iam_trust_policy_template.py</files>
  <read_first>
    - .planning/phases/04-oidc-chart-source-extensions/04-CONTEXT.md (D4 — template shape with 5 placeholders + Plan-Check obligation that placeholders are byte-stable + the JSON sketch errata that `sub` MUST be under `StringLike` not `StringEquals`)
    - .planning/phases/04-oidc-chart-source-extensions/04-RESEARCH.md (§2 — verified Bitbucket OIDC issuer URL + condition-key format + `aud` value + `sub` claim format with literal curly braces + the researcher-ratified final template; §8.6 — unit-test cookbook for the template assertions)
    - tests/unit/test_kubeconfig.py (test style: `@pytest.mark.unit`, plain pytest assertions, no fixtures beyond pathlib; mirror the file-read + structure-assertion pattern)
  </read_first>
  <behavior>
    **`docs/guides/oidc-setup.md` content** (final shape per RESEARCH §2 researcher-ratified template):

    ```markdown
    # Bitbucket Pipelines OIDC — IAM Trust-Policy Template

    > Draft. Polished in Phase 7 alongside the mkdocs site.

    This page ships the AWS IAM trust-policy template the pipe expects when it authenticates to AWS via Bitbucket Pipelines OIDC (`AUTH-03`). The template is the **consumer-side** gate that constrains which Bitbucket workspace + repository can assume the role; the pipe's runtime path (`auth/oidc.py`) is the matching strategy that exchanges the OIDC JWT for STS credentials via `AssumeRoleWithWebIdentity`.

    ## How to use

    1. Copy the JSON block below.
    2. Replace the 5 placeholders with values from your AWS account + Bitbucket workspace:
       - `<ACCOUNT_ID>` — your AWS 12-digit account ID.
       - `<WORKSPACE>` — your Bitbucket workspace slug (e.g. `mycompany`).
       - `<OIDC_AUDIENCE>` — the audience value from **Repository settings → Pipelines → OpenID Connect → Audience**. Form: `ari:cloud:bitbucket::workspace/<WORKSPACE_UUID>`.
       - `<BITBUCKET_WORKSPACE_UUID>` — your Bitbucket workspace UUID (curly-braced UUID form). Find via the same Pipelines OpenID Connect page.
       - `<BITBUCKET_REPO_UUID>` — your Bitbucket repository UUID (curly-braced UUID form).
    3. Paste the result into the IAM role's trust-policy editor (AWS IAM console → Roles → your role → Trust relationships → Edit trust policy) OR into your Terraform `aws_iam_role.assume_role_policy` JSON value.

    ```jsonc
    {
      "Version": "2012-10-17",
      "Statement": [{
        "Effect": "Allow",
        "Principal": {
          "Federated": "arn:aws:iam::<ACCOUNT_ID>:oidc-provider/api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc"
        },
        "Action": "sts:AssumeRoleWithWebIdentity",
        "Condition": {
          "StringEquals": {
            "api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc:aud": "<OIDC_AUDIENCE>"
          },
          "StringLike": {
            "api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc:sub": "{<BITBUCKET_WORKSPACE_UUID>}:{<BITBUCKET_REPO_UUID>}:*"
          }
        }
      }]
    }
    ```

    ## Why `StringLike` (not `StringEquals`) for `sub`

    Bitbucket emits the `sub` claim as `{<WORKSPACE_UUID>}:{<REPO_UUID>}:<step-UUID>` — with literal curly braces and the step UUID as the third segment. IAM only treats `*` as a wildcard under `StringLike`, NOT under `StringEquals`. The template above uses `StringLike` with `{<BITBUCKET_WORKSPACE_UUID>}:{<BITBUCKET_REPO_UUID>}:*` so any step within the named workspace + repo can assume the role; constraining at the step level is impractical (step UUIDs are ephemeral). If a downstream consumer wants step-level scoping, they can replace `*` with the exact step UUID and switch the condition back to `StringEquals`.

    ## Notes

    - The pipe does NOT re-validate the JWT — STS validates the `aud` claim against the trust-policy condition above.
    - The `Principal.Federated` URL is the OIDC provider ARN; the OIDC provider itself must be created once per AWS account (typically via Terraform `aws_iam_openid_connect_provider`). The Phase 7 docs site will ship the full Terraform setup alongside the trust policy.
    - The pipe's WARN log `auth.precedence.static_keys_won_over_oidc` (Phase 4, AUTH-04 revised) surfaces when a consumer has both static AWS keys AND `BITBUCKET_STEP_OIDC_TOKEN` set — static keys win per the boto3 default chain order. To use OIDC instead, unset `AWS_ACCESS_KEY_ID` + `AWS_SECRET_ACCESS_KEY` in your `bitbucket-pipelines.yml`.

    ## Polish coming in Phase 7

    - mkdocs-material rendering (`docs/guides/oidc-setup.md` lands in the published v2 site).
    - Companion Terraform snippet (`aws_iam_role` + `data.tls_certificate` patterns).
    - Migration notes for v1 → v2 OIDC setup.
    ```

    Total: ~50-60 lines. The fenced ```jsonc block (lines 19-37 approximately) is the load-bearing artifact. Everything else is prose.

    **`tests/unit/test_iam_trust_policy_template.py` content** (per RESEARCH §8.6 cookbook + the CONTEXT D4 5-assertion list updated for the StringLike erratum):

    ```python
    """Unit tests for the IAM trust-policy template at docs/guides/oidc-setup.md.

    Requirements traceability:
      - AUTH-05: ships a documented IAM trust-policy template scoped to BITBUCKET_WORKSPACE_UUID
                 and BITBUCKET_REPO_UUID; the unit test below enforces structural correctness.

    Per CONTEXT D4 + RESEARCH §2 + §R-erratum: the `sub` condition MUST use `StringLike`
    (NOT `StringEquals`) because the value contains a literal `*` wildcard for the step UUID,
    and IAM only treats `*` as a wildcard under `StringLike`.
    """

    from __future__ import annotations

    import json
    import pathlib
    import re

    import pytest

    DOC_PATH = pathlib.Path("docs/guides/oidc-setup.md")
    REQUIRED_PLACEHOLDERS = (
        "<ACCOUNT_ID>",
        "<WORKSPACE>",
        "<OIDC_AUDIENCE>",
        "<BITBUCKET_WORKSPACE_UUID>",
        "<BITBUCKET_REPO_UUID>",
    )


    def _extract_policy_json() -> dict:
        """Pull the first ```jsonc fenced block out of the markdown file and parse it.

        Returns the parsed dict. Raises ValueError if no block found.
        """
        text = DOC_PATH.read_text()
        match = re.search(r"```jsonc?\n(.*?)\n```", text, re.DOTALL)
        if match is None:
            raise ValueError(f"no fenced ```jsonc block found in {DOC_PATH}")
        # Strip JSONC line comments (// ...) — none in our template, but defensive.
        block = re.sub(r"^\s*//.*$", "", match.group(1), flags=re.MULTILINE)
        return json.loads(block)


    @pytest.mark.unit
    def test_template_has_all_required_placeholders():
        """All 5 placeholders appear verbatim in the doc (CONTEXT D4 Plan-Check obligation)."""
        text = DOC_PATH.read_text()
        missing = [ph for ph in REQUIRED_PLACEHOLDERS if ph not in text]
        assert not missing, f"missing placeholders: {missing}"


    @pytest.mark.unit
    def test_template_parses_as_valid_json():
        """The fenced block is valid JSON after JSONC comment stripping."""
        data = _extract_policy_json()
        assert data["Version"] == "2012-10-17"
        assert isinstance(data["Statement"], list)
        assert len(data["Statement"]) == 1


    @pytest.mark.unit
    def test_sub_condition_uses_string_like_not_string_equals():
        """RESEARCH §2 + CONTEXT D4 erratum: the wildcard `*` requires StringLike.

        Catches the trap where a future PR converts `sub` back to StringEquals
        (which would silently allow `*` to be treated as a literal asterisk,
        not a wildcard — under-constraining the policy to literally one
        invalid sub value).
        """
        data = _extract_policy_json()
        statement = data["Statement"][0]
        condition = statement["Condition"]
        # sub key MUST live under StringLike
        assert "StringLike" in condition, "sub condition must live under StringLike (RESEARCH §2 erratum)"
        sub_key = next(
            (k for k in condition["StringLike"] if k.endswith(":sub")),
            None,
        )
        assert sub_key is not None, "no `*:sub` key found under StringLike"
        sub_value = condition["StringLike"][sub_key]
        assert "<BITBUCKET_WORKSPACE_UUID>" in sub_value
        assert "<BITBUCKET_REPO_UUID>" in sub_value
        assert sub_value.endswith(":*"), "sub value must end with `:*` wildcard"
        # And sub MUST NOT live under StringEquals (the erratum)
        if "StringEquals" in condition:
            for key in condition["StringEquals"]:
                assert not key.endswith(":sub"), (
                    f"sub key {key!r} found under StringEquals — must be under StringLike per RESEARCH §2 erratum"
                )


    @pytest.mark.unit
    def test_aud_condition_uses_string_equals_with_placeholder():
        """aud is an exact-match — StringEquals is correct here."""
        data = _extract_policy_json()
        statement = data["Statement"][0]
        condition = statement["Condition"]
        assert "StringEquals" in condition
        aud_key = next(
            (k for k in condition["StringEquals"] if k.endswith(":aud")),
            None,
        )
        assert aud_key is not None, "no `*:aud` key found under StringEquals"
        assert condition["StringEquals"][aud_key] == "<OIDC_AUDIENCE>"


    @pytest.mark.unit
    def test_action_is_assume_role_with_web_identity():
        """Action MUST be exactly sts:AssumeRoleWithWebIdentity (CONTEXT D4)."""
        data = _extract_policy_json()
        statement = data["Statement"][0]
        assert statement["Action"] == "sts:AssumeRoleWithWebIdentity"


    @pytest.mark.unit
    def test_federated_principal_matches_bitbucket_oidc_issuer_pattern():
        """Principal.Federated must reference the canonical Bitbucket OIDC provider ARN.

        Canonical form per RESEARCH §2 (Atlassian docs retrieved 2026-06-18):
          arn:aws:iam::<ACCOUNT_ID>:oidc-provider/api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc
        """
        data = _extract_policy_json()
        statement = data["Statement"][0]
        federated = statement["Principal"]["Federated"]
        expected_substring = "api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc"
        assert expected_substring in federated, (
            f"Principal.Federated `{federated}` does not match the Bitbucket OIDC issuer pattern"
        )
        # And it must be the OIDC provider ARN (not a federated user, role, etc.)
        assert federated.startswith("arn:aws:iam::<ACCOUNT_ID>:oidc-provider/")
    ```

    Total: ~100 lines including imports + docstring + 5 test functions + 1 helper.

    Coverage on the test file is 100% (trivial — all assertions execute).

    No fixtures beyond pathlib. No external services. No subprocess. The test runs in milliseconds.

    Mypy-strict: the test file uses minimal types. The `_extract_policy_json()` helper returns `dict` which mypy accepts; if mypy's strict-equality mode complains, add `dict[str, object]` annotation.
  </behavior>
  <action>
    Create `docs/guides/oidc-setup.md` with the content from the behavior block above. Place the file at the literal path `docs/guides/oidc-setup.md` (the path matches CONTEXT D4 + the test's `DOC_PATH` constant).

    IMPORTANT placeholder fidelity:
    - All 5 placeholders MUST appear verbatim with angle-bracket syntax: `<ACCOUNT_ID>`, `<WORKSPACE>`, `<OIDC_AUDIENCE>`, `<BITBUCKET_WORKSPACE_UUID>`, `<BITBUCKET_REPO_UUID>`. NO substitution. NO example values. The placeholders ARE the load-bearing artifact — consumers grep for them.
    - The `sub` condition MUST be under `StringLike` (NOT `StringEquals`). The CONTEXT D4 JSON sketch had `StringEquals` — that is the erratum; the corrected version (per RESEARCH §2) is what ships here.
    - The fenced block is ```jsonc (NOT ```json). This lets the markdown linter recognize JSON-with-comments syntax; the test extracts via regex matching both ```json and ```jsonc.

    Create `tests/unit/test_iam_trust_policy_template.py` with the content from the behavior block above. Each test `@pytest.mark.unit`.

    Verify before commit:
    - `test -f docs/guides/oidc-setup.md && test -f tests/unit/test_iam_trust_policy_template.py`.
    - `uv run pytest -m unit -q tests/unit/test_iam_trust_policy_template.py --no-cov` exits 0 — all 5 + 1 helper test pass.
    - `uv run pytest -q` (full unit tier with 100% gate) exits 0 — no regression. NOTE: the new test file does NOT add to any module's coverage; it's a doc-template structural test, so `--cov-fail-under=100` is unaffected.
    - `uv run ruff check src tests && uv run ruff format --check src tests` exit 0. The test file follows ruff conventions.
    - `uv run mypy --strict src` exits 0. Test files are not in `src/`; mypy doesn't scan them by default.
    - Placeholder grep (CONTEXT D4 Plan-Check obligation): `grep -F "<ACCOUNT_ID>" docs/guides/oidc-setup.md && grep -F "<WORKSPACE>" docs/guides/oidc-setup.md && grep -F "<OIDC_AUDIENCE>" docs/guides/oidc-setup.md && grep -F "<BITBUCKET_WORKSPACE_UUID>" docs/guides/oidc-setup.md && grep -F "<BITBUCKET_REPO_UUID>" docs/guides/oidc-setup.md` (all 5 must hit).
    - StringLike grep (CONTEXT D4 erratum gate): `grep -F "StringLike" docs/guides/oidc-setup.md` returns >= 1 hit AND the `sub` line is below the `"StringLike":` opening brace in the JSON block (Plan-Checker reads the JSON structure).
    - Negative StringEquals-on-sub grep: `! grep -E '"StringEquals"[^}]*":sub":' docs/guides/oidc-setup.md` (the `sub` condition is NOT under StringEquals).

    No production code touched. No Phase 1-3 test touched. No new Python dep. The whitelist enforces.

    Stub call-out: this plan is the DRAFT. Phase 7 adds the mkdocs `docs/` site polish + Terraform companion + migration notes. The IAM template JSON itself is FINAL (no Phase 7 wording changes — the placeholders are stable contracts).
  </action>
  <verify>
    <automated>test -f docs/guides/oidc-setup.md &amp;&amp; test -f tests/unit/test_iam_trust_policy_template.py &amp;&amp; uv run pytest -m unit -q tests/unit/test_iam_trust_policy_template.py --no-cov &amp;&amp; grep -F "&lt;ACCOUNT_ID&gt;" docs/guides/oidc-setup.md &amp;&amp; grep -F "&lt;BITBUCKET_WORKSPACE_UUID&gt;" docs/guides/oidc-setup.md &amp;&amp; grep -F "&lt;BITBUCKET_REPO_UUID&gt;" docs/guides/oidc-setup.md &amp;&amp; grep -F "&lt;OIDC_AUDIENCE&gt;" docs/guides/oidc-setup.md &amp;&amp; grep -F "StringLike" docs/guides/oidc-setup.md &amp;&amp; ! grep -E '"StringEquals"[^}]*":sub":' docs/guides/oidc-setup.md</automated>
  </verify>
  <done>
    `docs/guides/oidc-setup.md` exists with all 5 placeholders + the `sub`-under-`StringLike` JSON; `tests/unit/test_iam_trust_policy_template.py` ships 5 assertions + 1 helper, all passing; no production code touched; no other test file touched; the file paths match CONTEXT D4 verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer's IAM role trust policy → STS `AssumeRoleWithWebIdentity` | The trust policy is the gate that decides which Bitbucket workspace + repo can assume the role. The pipe (auth/oidc.py — Plan 04-03) is the CALLER; the trust policy is the AUTHORIZATION CHECK at AWS. |
| documented placeholders → consumer's IAM Console paste | The 5 placeholders MUST be obvious to find-and-replace — consumers will use VSCode's "Find & Replace All" or similar. The angle-bracket syntax `<X>` is the convention. |
| (no runtime trust boundary) | This plan ships a markdown file and a structural test. No code executes at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | `sub` condition under `StringEquals` (CONTEXT D4 erratum) | mitigate | The shipped template uses `StringLike` (per RESEARCH §2). Test `test_sub_condition_uses_string_like_not_string_equals` enforces — fails if any future PR converts `sub` back to `StringEquals`. The wildcard `*` under `StringEquals` would NOT be treated as a wildcard, silently constraining the policy to literally one invalid `sub` value, locking everyone out (which is "safer" than over-permissive, but breaking the consumer flow). |
| T-04-04-02 | Elevation of Privilege | A consumer copy-pastes the template and forgets to substitute `<BITBUCKET_WORKSPACE_UUID>` | mitigate | The 5 placeholders are angle-bracketed and DO NOT match any valid UUID format — IAM rejects the policy at write time (`Invalid policy: invalid characters in condition value`). The consumer gets a loud error from AWS, not a silent permissive policy. |
| T-04-04-03 | Information Disclosure | Template shows real account IDs / UUIDs via example values | mitigate | NO example values. All 5 fields are placeholder-only. Consumers fill in their own. |
| T-04-04-04 | Spoofing | Bitbucket OIDC issuer URL drift — the canonical URL changes upstream | accept (mitigated by note) | RESEARCH §2 verified the URL via Atlassian docs on 2026-06-18 with a 60-day validity window. The doc page mentions this in the "Polish coming in Phase 7" forward-reference; Phase 7 docs revision would catch any drift. If Bitbucket changes the URL in Q3 2026 the test breaks (federated principal pattern mismatch), surfacing the drift loudly. |
| T-04-04-SC | Tampering | None (no package install) | n/a | No npm/pip/cargo install in this plan. |
</threat_model>

<deviations>
## Documented Deviations from CONTEXT / RESEARCH

### Deviation 1: Terraform companion snippet is DEFERRED to Phase 7

**CONTEXT D4 mentions** "an optional Terraform companion snippet (`docs/guides/oidc-setup-terraform.md`) shows the same trust policy via `aws_iam_role` + `data.tls_certificate` patterns. Defer if Phase 4 is already heavy — gate is 'ships in Phase 7 at latest'."

**Plan response:** DEFER. Phase 4 is heavy (7 plans across 4 waves); the Terraform snippet is purely additive (not in any AUTH-05 acceptance criterion). Phase 7's docs site work is the natural home — it'll have the full mkdocs context + the migration guide for v1 consumers using Terraform.

**Action item:** none — explicit deferral per the gate. Phase 7 picks this up.

### Deviation 2: `sub` is under `StringLike` (NOT `StringEquals` as CONTEXT D4 JSON sketch cites)

**CONTEXT D4 explicitly flags this as an erratum** at the bottom of D4: "Erratum on the JSON sketch above: replace `StringEquals` with `StringLike` for the `sub` key."

**Plan response:** ship with `StringLike`. The CONTEXT note itself says "the JSON sketch is wrong; use StringLike." This plan honors the erratum. The unit test `test_sub_condition_uses_string_like_not_string_equals` AND the negative grep in `<verify>` BOTH enforce.

**Action item:** Plan-Checker should not flag the deviation from D4's first JSON sketch — D4's own erratum overrides the sketch. The verifier reads the test's assertions as the canonical contract.

### Deviation 3: Drafted in markdown; full prose polish lands in Phase 7

**Phase 4 ships the IAM template (final) + a short prose intro (drafted).** The PROSE is acknowledged as "drafted, polished in Phase 7" — the doc opens with a `> Draft. Polished in Phase 7 alongside the mkdocs site.` blockquote and closes with a "Polish coming in Phase 7" section listing the deferred work.

**Plan response:** the IAM template JSON itself is FINAL — no Phase 7 wording changes. The 5 placeholders are stable contracts. The PROSE around it is drafted; Phase 7's mkdocs work expands the intro, adds the Terraform companion, adds the migration guide.

**Action item:** none — Phase 7 inherits the drafted-but-final state.
</deviations>

<dependencies_on_prior_plans>
## Prior Plan Inputs

- **No prior plan inputs from Phase 4.** This plan is fully independent — it ships a markdown file + a test that reads it. No code dependency on 04-01 / 04-02 / 04-03 / 04-05 / 04-06 / 04-07.

## NOT dependencies

- All other Phase 4 plans run in parallel with this one (or AFTER, but not BEFORE — they have no upstream dependency on this plan either).

## File-overlap note

- `docs/guides/oidc-setup.md` is a NEW file. No prior plan touches `docs/guides/`.
- `tests/unit/test_iam_trust_policy_template.py` is a NEW file. No prior plan has a test about IAM trust policies.
- ZERO `src/` files modified. ZERO conflict with any other plan.
</dependencies_on_prior_plans>

<dependencies_on_other_phases>
## Forward Compatibility

- **Phase 4 Plan 04-03 (`OidcWebIdentityStrategy`):** the strategy's `audience` constructor argument is semantically aligned with the `<OIDC_AUDIENCE>` placeholder in the template. The consumer puts the SAME value in both places. The pipe does NOT enforce this — STS validates the `aud` claim against the trust-policy condition key. The template + the strategy are TWO sides of the same trust contract.
- **Phase 5 (log masking):** no impact — this plan ships a doc + test, no runtime code.
- **Phase 7 (docs site):** consumes this file. Phase 7 will:
  - Move/extend the file into the mkdocs site structure.
  - Add the Terraform companion (`docs/guides/oidc-setup-terraform.md`).
  - Wire it into the v2 docs navigation.
  - Polish the prose.
  - The JSON template stays byte-stable.

## Stable contracts (do NOT refactor without bumping major)

- The 5 placeholder names: `<ACCOUNT_ID>`, `<WORKSPACE>`, `<OIDC_AUDIENCE>`, `<BITBUCKET_WORKSPACE_UUID>`, `<BITBUCKET_REPO_UUID>` — consumers grep for these and use Find-and-Replace All. Renaming a placeholder is a breaking change for ANY consumer who has set up the trust policy.
- The `sub` condition under `StringLike` — under-constraining the policy by reverting to `StringEquals` would silently lock consumers out (or worse, allow `*` as literal asterisk).
- The Action `sts:AssumeRoleWithWebIdentity` — the only STS API call OIDC can possibly trigger.
- The Principal.Federated URL pattern `arn:aws:iam::<ACCOUNT_ID>:oidc-provider/api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc` — verified canonical form as of 2026-06-18; RESEARCH §2 has a 60-day validity window for the URL.
</dependencies_on_other_phases>

<known_stubs>
## Known Stubs / Scaffolds

- The PROSE intro + "Polish coming in Phase 7" section is acknowledged as a DRAFT. The IAM template JSON itself is FINAL.
- Terraform companion snippet is EXPLICITLY DEFERRED to Phase 7 per Deviation 1. Not a stub in this plan; not a gap.
- No `# TODO` markers in either file.
</known_stubs>

<verification>
- `test -f docs/guides/oidc-setup.md` exits 0.
- `test -f tests/unit/test_iam_trust_policy_template.py` exits 0.
- `uv run pytest -m unit -q tests/unit/test_iam_trust_policy_template.py --no-cov` exits 0 (all 5 assertions + helper pass).
- `uv run pytest -q` (full unit tier with 100% gate) exits 0 — no regression.
- `uv run ruff check src tests` exits 0.
- `uv run ruff format --check src tests` exits 0.
- Placeholder fidelity (CONTEXT D4):
  - `grep -F "<ACCOUNT_ID>" docs/guides/oidc-setup.md` returns >= 1 hit.
  - `grep -F "<WORKSPACE>" docs/guides/oidc-setup.md` returns >= 1 hit.
  - `grep -F "<OIDC_AUDIENCE>" docs/guides/oidc-setup.md` returns >= 1 hit.
  - `grep -F "<BITBUCKET_WORKSPACE_UUID>" docs/guides/oidc-setup.md` returns >= 1 hit.
  - `grep -F "<BITBUCKET_REPO_UUID>" docs/guides/oidc-setup.md` returns >= 1 hit.
- StringLike erratum (CONTEXT D4 / RESEARCH §2):
  - `grep -F "StringLike" docs/guides/oidc-setup.md` returns >= 1 hit.
  - `! grep -E '"StringEquals"[^}]*":sub":' docs/guides/oidc-setup.md` — `sub` NOT under StringEquals.
- Action constraint: `grep -F "sts:AssumeRoleWithWebIdentity" docs/guides/oidc-setup.md` returns >= 1 hit.
- Federated principal pattern: `grep -F "arn:aws:iam::<ACCOUNT_ID>:oidc-provider/api.bitbucket.org/2.0/workspaces/<WORKSPACE>/pipelines-config/identity/oidc" docs/guides/oidc-setup.md` returns 1 hit.
- `git diff --stat HEAD -- docs/guides/oidc-setup.md tests/unit/test_iam_trust_policy_template.py` lists ONLY these two files; no other file appears.
- `git diff --stat HEAD -- src/ Dockerfile pyproject.toml uv.lock` is empty.
</verification>

<success_criteria>
- AUTH-05: `docs/guides/oidc-setup.md` ships the IAM trust-policy template with the 5 required placeholders constraining `aud` (StringEquals) + `sub` (StringLike with wildcard).
- Unit test enforces 5 structural assertions: JSON validity, placeholder presence, `sub` under `StringLike` (NOT `StringEquals`), Action is `sts:AssumeRoleWithWebIdentity`, Principal.Federated matches Bitbucket OIDC issuer pattern.
- CONTEXT D4 erratum is honored — `sub` is under `StringLike`.
- Terraform companion snippet is explicitly DEFERRED to Phase 7 (Deviation 1).
- NO production code changed. NO existing test touched. NO new Python dep.
- File paths match CONTEXT D4 verbatim (`docs/guides/oidc-setup.md`).
</success_criteria>

<artifacts>
## Artifacts this plan produces (Plan 04-04 scope)

**Files (new):**
- `docs/guides/oidc-setup.md` — Bitbucket-Pipelines OIDC IAM trust-policy template + drafted prose intro (~50 lines).
- `tests/unit/test_iam_trust_policy_template.py` — 5 structural assertions + 1 helper (~100 lines).

**Files (modified):** none.

**Out of scope for this plan:**
- `OidcWebIdentityStrategy` runtime path (Plan 04-03).
- Terraform companion snippet (Phase 7).
- mkdocs site integration (Phase 7).
- Migration guide for v1 OIDC setup (Phase 7 + Phase 7 docs site work).
- Any production code change.
</artifacts>

<output>
On completion, create `.planning/phases/04-oidc-chart-source-extensions/04-04-SUMMARY.md` recording:
- Confirmation that all 5 placeholders are present in `docs/guides/oidc-setup.md`.
- Confirmation that the `sub` condition is under `StringLike` (NOT `StringEquals`) — CONTEXT D4 erratum honored.
- Confirmation that all 5 + 1 unit tests pass.
- The exact file path (`docs/guides/oidc-setup.md`) verifying CONTEXT D4 compliance.
- A one-line cross-reference to Phase 7 work (Terraform companion + mkdocs polish + migration guide).
- A one-line cross-reference to Plan 04-03 (the runtime side of the OIDC contract — `OidcWebIdentityStrategy`).
</output>
