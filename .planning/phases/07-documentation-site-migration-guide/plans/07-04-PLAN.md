---
phase: 07-documentation-site-migration-guide
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - docs/migration/v1-to-v2.md
  - docs/guides/v1-to-v2.md
  - examples/basic/bitbucket-pipelines.yml
  - examples/oidc-only/bitbucket-pipelines.yml
  - examples/oci-chart/bitbucket-pipelines.yml
  - examples/multi-env/bitbucket-pipelines.yml
  - examples/migration-v1-to-v2/before.yml
  - examples/migration-v1-to-v2/after.yml
  - examples/migration-v1-to-v2/README.md
  - pyproject.toml
  - tests/structural/test_examples_lint.py
  - tests/structural/test_migration_guide.py
autonomous: true
requirements: [DOC-03, DOC-08, MIG-03]
requirements_addressed: [DOC-03, DOC-08, MIG-03]

must_haves:
  truths:
    - "`docs/guides/v1-to-v2.md` is MOVED to `docs/migration/v1-to-v2.md` via `git mv` (preserves history per CONTEXT D7 + project commit policy); the moved file replaces the Plan 07-01 Wave-1 stub."
    - "The migration guide is POLISHED — wording sharpened, mkdocs-material admonitions added (e.g., `!!! warning`, `!!! tip` blocks), all internal cross-links re-targeted to the NEW mkdocs nav layout — but content is NOT rewritten; every breaking-change section drafted in Phase 5/6 is preserved (INJECT_BITBUCKET_METADATA, SET/VALUES, NAMESPACE, image-tag policy, distribution change, ACTION=diff/rollback, SAFE_UPGRADE, new v2 env vars, Quick migration checklist)."
    - "`examples/` directory ships 5 sub-directories per D8: `basic/`, `oidc-only/`, `oci-chart/`, `multi-env/`, `migration-v1-to-v2/`. Each `bitbucket-pipelines.yml` opens with a header comment block (purpose / prerequisites / expected outcome). Each is `check-jsonschema --builtin-schema vendor.bitbucket-pipelines`-clean."
    - "`examples/migration-v1-to-v2/{before.yml, after.yml, README.md}` (MIG-03): `before.yml` is the v1 reference pipeline; `after.yml` is the v2 equivalent; `README.md` has line-level explanations cross-referenced from every breaking-change section in `docs/migration/v1-to-v2.md`."
    - "`pyproject.toml` `[dependency-groups] dev` adds `check-jsonschema == 0.37.3` (RESEARCH Q8 verified Apache-2.0 PyPI pin)."
    - "`tests/structural/test_examples_lint.py` invokes `uv run check-jsonschema --builtin-schema vendor.bitbucket-pipelines <every yml under examples/>` as a single subprocess call and asserts exit-0 (DOC-08 + MIG-03 gate). The test ALSO asserts `examples/migration-v1-to-v2/README.md` exists (MIG-03)."
    - "`tests/structural/test_migration_guide.py` is the DOC-03 gate: asserts the moved guide contains literal section headings for every breaking change (`INJECT_BITBUCKET_METADATA`, `NAMESPACE`, image-tag pinning, Distribution change) and that every internal link resolves under `mkdocs build --strict`."
    - "Post-move, `mkdocs build --strict` continues to exit 0 — every cross-link inside the moved migration guide has been re-targeted to the new docs nav layout (RESEARCH Q9: strict-mode link validation is fatal)."
  artifacts:
    - path: "docs/migration/v1-to-v2.md"
      provides: "Headline migration guide (mkdocs nav slot 2 — D7)"
      contains: "INJECT_BITBUCKET_METADATA"
    - path: "examples/basic/bitbucket-pipelines.yml"
      provides: "Copy-paste-ready basic pipe step (static keys + local chart)"
      contains: "pipe:"
    - path: "examples/oidc-only/bitbucket-pipelines.yml"
      provides: "OIDC + repo:// chart"
      contains: "OIDC_AUDIENCE"
    - path: "examples/oci-chart/bitbucket-pipelines.yml"
      provides: "OIDC + oci:// chart from GHCR"
      contains: "oci://"
    - path: "examples/multi-env/bitbucket-pipelines.yml"
      provides: "Multi-env deploy with helm-diff PR comment"
      contains: "POST_DIFF_AS_COMMENT"
    - path: "examples/migration-v1-to-v2/before.yml"
      provides: "v1 reference pipeline (MIG-03 source side of diff)"
      contains: "yvogl/aws-eks-helm-deploy"
    - path: "examples/migration-v1-to-v2/after.yml"
      provides: "v2 equivalent pipeline (MIG-03 target side of diff)"
      contains: "ghcr.io/yves-vogl/aws-eks-helm-deploy"
    - path: "examples/migration-v1-to-v2/README.md"
      provides: "Line-level explanations of every required change (MIG-03)"
      contains: "INJECT_BITBUCKET_METADATA"
    - path: "tests/structural/test_examples_lint.py"
      provides: "Structural gate: check-jsonschema vendor.bitbucket-pipelines on every yml"
      contains: "vendor.bitbucket-pipelines"
    - path: "tests/structural/test_migration_guide.py"
      provides: "Structural gate: DOC-03 section coverage + link sanity"
      contains: "def test_migration_guide_covers_inject_bitbucket_metadata"
  key_links:
    - from: "docs/migration/v1-to-v2.md"
      to: "examples/migration-v1-to-v2/before.yml"
      via: "every breaking-change section links to the before/after diff line range"
      pattern: "examples/migration-v1-to-v2/"
    - from: "docs/migration/v1-to-v2.md"
      to: "docs/guides/oidc-setup.md"
      via: "AUTH section links to OIDC setup guide (relative path under new nav)"
      pattern: "guides/oidc-setup.md"
    - from: "tests/structural/test_examples_lint.py"
      to: "examples/**/*.yml"
      via: "single subprocess invocation of check-jsonschema against all example yml files"
      pattern: "examples"
---

<objective>
Move the Phase 5/6 migration guide draft from `docs/guides/v1-to-v2.md` to `docs/migration/v1-to-v2.md` via `git mv` (preserves history), polish it (admonitions + cross-link retargeting), and ship the `examples/` corpus per D8 + MIG-03 with `check-jsonschema 0.37.3` as the lint gate.

Purpose: DOC-03 (migration guide covers every breaking change) + DOC-08 (≥ 4 end-to-end `bitbucket-pipelines.yml` snippets) + MIG-03 (before/after diff with line-level explanations) are the three biggest content-side contracts of Phase 7. The Phase 5 + Phase 6 drafts of the migration guide (435 lines) already cover every breaking change Yves wants surfaced; D7 explicitly forbids rewriting. The "move + polish" pattern preserves the existing prose while solving (a) the nav-slot location (`docs/migration/...` not `docs/guides/...`) and (b) strict-mode link resolution after the path change.

Output:
- `docs/migration/v1-to-v2.md` — moved + polished migration guide (replaces Plan 07-01 stub).
- `docs/guides/v1-to-v2.md` — DELETED (the `git mv` removes it).
- `examples/{basic, oidc-only, oci-chart, multi-env, migration-v1-to-v2}/` — 5 sub-directories.
- `pyproject.toml` — adds `check-jsonschema == 0.37.3` to `dev` group.
- `tests/structural/test_examples_lint.py` — structural gate.
- `tests/structural/test_migration_guide.py` — DOC-03 coverage gate.

This plan depends on Plan 07-01 (mkdocs.yml nav slot `Migration v1 → v2`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md
@.planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md
@.planning/phases/07-documentation-site-migration-guide/07-01-SUMMARY.md
@docs/guides/v1-to-v2.md
@docs/guides/oidc-setup.md
@docs/admin/repo-settings.md
@pyproject.toml
@src/aws_eks_helm_deploy/settings.py
@README.md
</context>

<artifacts_this_phase_produces>
- `docs/migration/v1-to-v2.md` (MOVED from `docs/guides/v1-to-v2.md` via `git mv` + polished)
- `docs/guides/v1-to-v2.md` (DELETED by the `git mv`)
- `examples/basic/bitbucket-pipelines.yml` (NEW)
- `examples/oidc-only/bitbucket-pipelines.yml` (NEW)
- `examples/oci-chart/bitbucket-pipelines.yml` (NEW)
- `examples/multi-env/bitbucket-pipelines.yml` (NEW)
- `examples/migration-v1-to-v2/before.yml` (NEW)
- `examples/migration-v1-to-v2/after.yml` (NEW)
- `examples/migration-v1-to-v2/README.md` (NEW)
- `pyproject.toml` (MODIFIED — adds check-jsonschema to dev group)
- `tests/structural/test_examples_lint.py` (NEW)
- `tests/structural/test_migration_guide.py` (NEW)
</artifacts_this_phase_produces>

<files_not_touched>
SUBPROCESS INVARIANT CARRIERS (D6 — Phase 7 introduces ZERO new src/ subprocess callers):
- `src/aws_eks_helm_deploy/helm/client.py` — NOT TOUCHED.
- `src/aws_eks_helm_deploy/chart/oci.py` — NOT TOUCHED.

Other invariant carriers:
- `src/aws_eks_helm_deploy/settings.py` — NOT TOUCHED (read-only by `examples/` author for env-var names).
- `.github/workflows/*.yml` — NOT TOUCHED (Plan 07-05 ships docs.yml; this plan does not modify ci.yml).
- `mkdocs.yml` — NOT TOUCHED (the nav slot for migration already points at the new path from Plan 07-01).
- `docs/adr/*.md` — NOT TOUCHED (Plan 07-02).
- `docs/admin/repo-settings.md` — NOT TOUCHED (Plan 07-07 extends it).
- `docs/guides/oidc-setup.md` — NOT TOUCHED (only re-linked from the migration guide; the file itself stays in `docs/guides/`).
- `README.md` — NOT TOUCHED (Plan 07-06).
- `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` — NOT TOUCHED (Plan 07-06).
</files_not_touched>

<tasks>

<task type="auto">
  <name>Task 1: `git mv` docs/guides/v1-to-v2.md → docs/migration/v1-to-v2.md + polish + cross-link retarget</name>
  <files>docs/migration/v1-to-v2.md, docs/guides/v1-to-v2.md</files>
  <read_first>
    - docs/guides/v1-to-v2.md (current 435-line draft — preserve every breaking-change section)
    - .planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md (D7 — move-not-rewrite; D10 — 6-month placeholder cross-reference)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (Q9 — strict-mode link resolution; pitfall: relative links must be retargeted after path change)
    - docs/guides/oidc-setup.md (target for the AUTH section's cross-link — note that this file STAYS in docs/guides/)
    - mkdocs.yml (Plan 07-01 — confirm the nav slot is `migration/v1-to-v2.md`, not `migration/v1-to-v2/index.md`)
  </read_first>
  <action>
1. Replace the Plan 07-01 Wave-1 stub at `docs/migration/v1-to-v2.md` with the moved draft. Use `git mv` to preserve history:

   ```bash
   # Delete the Wave-1 stub first (it's untracked-as-final; git mv won't overwrite a tracked file)
   rm docs/migration/v1-to-v2.md
   git mv docs/guides/v1-to-v2.md docs/migration/v1-to-v2.md
   git status  # confirms: renamed: docs/guides/v1-to-v2.md -> docs/migration/v1-to-v2.md
   ```

   IMPORTANT: `git mv` preserves git history. Verify with `git log --follow docs/migration/v1-to-v2.md` after the move — the log should show every prior commit on the file under its old path.

2. EDIT the moved file to:

   **a. Update the "Draft status" header.** The current file opens with:
   ```
   > **Draft status:** This guide covers Phase 5 breaking changes and new workflows.
   > Phase 7 will polish wording, add a table of contents, add mkdocs-material admonitions,
   > integrate screenshots, and publish the guide at `/v2/migration/v1-to-v2/` on the docs site.
   ```
   REPLACE with:
   ```markdown
   > **Status:** Published. This guide covers every v1.x → v2.0 breaking change. The 6-month v1.x security window ends `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.` (per D10).
   ```
   The literal placeholder `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.` is enforced by the SI-07-07 grep gate (Plan 07-06 also commits this to SECURITY.md).

   **b. Add a Table of Contents** near the top (after the breaking-changes glance table). Use mkdocs-material's auto-toc; the `toc:` markdown extension is already configured in mkdocs.yml (Plan 07-01). A manual ToC is optional — recommend a one-line `[TOC]` directive OR rely on mkdocs-material's auto-rendered sidebar.

   **c. Add admonitions** to high-signal sections. Examples:
   ```markdown
   !!! warning "Breaking change"
       In v1.x, `INJECT_BITBUCKET_METADATA` was unconditionally `true`. v2.0 defaults to unset (tri-state). Charts referencing `.Values.bitbucket.*` will silently render `null` in v2 unless you set `INJECT_BITBUCKET_METADATA=true`.

   !!! tip "v2 best practice"
       Pin to a specific tag (`:2.0.0`) for production. The `:2` rolling tag follows the v2 major; `:latest` is reserved for the freshly-published latest release and may change at any time.
   ```
   Apply admonitions to: `INJECT_BITBUCKET_METADATA` flip section, `NAMESPACE` correction, image-tag policy, Distribution change.

   **d. Re-target every internal cross-link.** Current file has links like `[docs/guides/oidc-setup.md](oidc-setup.md)` that were relative to `docs/guides/`. After the move, relative paths change:
   - `[…](oidc-setup.md)` → `[…](../guides/oidc-setup.md)` (oidc-setup.md stays in `docs/guides/`)
   - `[…](v2-runtime-config.md)` (if any) → adjust similarly
   - Absolute repo links (`https://github.com/...`) stay unchanged.
   - References to `examples/migration-v1-to-v2/` are RELATIVE to repo root (not docs/), so use `https://github.com/yves-vogl/aws-eks-helm-deploy/tree/main/examples/migration-v1-to-v2/` for safety. OR use mkdocs-material's `pymdownx.snippets` to inline. **Recommended: use absolute GitHub URLs for `examples/` references** — keeps the docs site decoupled from the source-tree layout.

   **e. Add a new "Distribution change (Phase 6 / MIG-01)" section verification.** The current file already has this section at line 367. Leave it — but verify the GHCR URL and `:2` rolling tag policy match Phase 6 final shipped state.

   **f. Add cross-references to `examples/migration-v1-to-v2/`** in EVERY breaking-change section. Example:
   ```markdown
   See the line-level diff: [`examples/migration-v1-to-v2/before.yml` vs `after.yml`](https://github.com/yves-vogl/aws-eks-helm-deploy/tree/main/examples/migration-v1-to-v2) (specifically lines NN-MM in `after.yml`).
   ```

3. Run `uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site`. Strict mode is FATAL on broken internal links (RESEARCH Q9 + pitfall: relative paths break after the move). Iterate until exit-0:
   - If a `validation.links.not_found` warning surfaces, fix the link.
   - If a `validation.nav.not_found` warning surfaces, verify mkdocs.yml's nav slot matches the new file location exactly.

4. Verify no German prose accidentally slipped in (CLAUDE.md: docs are English-only).

5. Confirm git history is preserved: `git log --follow --oneline docs/migration/v1-to-v2.md | head -10` — should show Phase 5/6 commits.

6. The OLD location `docs/guides/v1-to-v2.md` no longer exists after the move. Verify: `test ! -e docs/guides/v1-to-v2.md`.
  </action>
  <verify>
    <automated>test -f docs/migration/v1-to-v2.md</automated>
    <automated>test ! -e docs/guides/v1-to-v2.md</automated>
    <automated>grep -F 'INJECT_BITBUCKET_METADATA' docs/migration/v1-to-v2.md</automated>
    <automated>grep -F 'NAMESPACE' docs/migration/v1-to-v2.md</automated>
    <automated>grep -F 'Distribution change' docs/migration/v1-to-v2.md</automated>
    <automated>grep -F '2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.' docs/migration/v1-to-v2.md</automated>
    <automated>grep -F '../guides/oidc-setup.md' docs/migration/v1-to-v2.md</automated>
    <automated>grep -F 'examples/migration-v1-to-v2' docs/migration/v1-to-v2.md</automated>
    <automated>! grep -E '!!! warning' docs/migration/v1-to-v2.md && echo 'WARN: no admonitions added' || true</automated>
    <automated>uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site</automated>
  </verify>
  <acceptance_criteria>
    - `docs/migration/v1-to-v2.md` exists and contains every breaking-change section title from the Phase 5/6 draft (INJECT_BITBUCKET_METADATA, SET/VALUES, NAMESPACE, image-tag policy, Distribution change).
    - `docs/guides/v1-to-v2.md` no longer exists.
    - `git log --follow docs/migration/v1-to-v2.md` shows the prior phase commits (history preserved via `git mv`).
    - `mkdocs build --strict` exits 0 — every internal link resolves.
    - The D10 6-month placeholder is committed verbatim (SI-07-07 gate).
    - Cross-references to `examples/migration-v1-to-v2/` appear in at least 3 breaking-change sections.
    - At least 3 mkdocs-material admonition blocks added (`!!! warning`, `!!! tip`, `!!! info`).
  </acceptance_criteria>
  <threat_model>
    - T-07-V14 (strict-mode link validation): MITIGATED — `mkdocs build --strict` is the build command; broken links fail the build.
    - SI-07-07 (D10 6-month placeholder present): MITIGATED — literal placeholder string committed verbatim.
  </threat_model>
  <done>
    Migration guide moved with history preserved, polished with admonitions, cross-links retargeted, `mkdocs build --strict` green, D10 placeholder present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship the `examples/` corpus (D8 + MIG-03)</name>
  <files>examples/basic/bitbucket-pipelines.yml, examples/oidc-only/bitbucket-pipelines.yml, examples/oci-chart/bitbucket-pipelines.yml, examples/multi-env/bitbucket-pipelines.yml, examples/migration-v1-to-v2/before.yml, examples/migration-v1-to-v2/after.yml, examples/migration-v1-to-v2/README.md</files>
  <read_first>
    - README.md (existing v1 Quick start snippet — source for `examples/basic/` shape and `before.yml`)
    - docs/guides/oidc-setup.md (Phase 4 — IAM trust policy + Bitbucket OIDC step shape)
    - src/aws_eks_helm_deploy/settings.py (canonical env-var names: OIDC_AUDIENCE, REGISTRY_USERNAME, REPO_URL, CHART_VERSION, POST_DIFF_AS_COMMENT, BITBUCKET_TOKEN, SAFE_UPGRADE, etc.)
    - docs/migration/v1-to-v2.md (Task 1 output — examples cross-reference back into the guide)
    - .planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md (D8 structure + header-block convention)
  </read_first>
  <action>
**Top-of-file header block convention** (applied to EVERY example file):
```yaml
# Example: <one-line purpose>
# Prerequisites:
#   - <prereq 1>
#   - <prereq 2>
# Expected outcome:
#   <one or two sentences>
# Reference: docs/migration/v1-to-v2.md or docs/guides/oidc-setup.md (cite specific section).
```

**Tag policy used in examples:** all v2 examples reference `docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2.0.0` (pinned) OR `docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2` (rolling major). MIG-01 distribution change context.

---

1. **`examples/basic/bitbucket-pipelines.yml`** — static keys + local chart (parity with v1 minus the `awscli` baggage).

   ```yaml
   # Example: basic v2 pipeline — static AWS keys + local Helm chart at ./charts/my-app.
   # Prerequisites:
   #   - AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY in repository variables.
   #   - A target EKS cluster reachable from the access keys (kubectl perms via aws-auth or access entries).
   #   - A Helm chart checked into the same repo at ./charts/my-app.
   # Expected outcome:
   #   helm upgrade --install of release "my-app" into namespace "production".
   # Reference: docs/migration/v1-to-v2.md (Quick migration checklist + Distribution change).

   image: atlassian/default-image:4

   pipelines:
     branches:
       main:
         - step:
             name: Deploy to EKS (basic)
             deployment: production
             script:
               - pipe: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2
                 variables:
                   AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
                   AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
                   AWS_REGION: eu-central-1
                   CLUSTER_NAME: my-eks-cluster
                   CHART: ./charts/my-app
                   RELEASE_NAME: my-app
                   NAMESPACE: production
                   CREATE_NAMESPACE: "true"
                   WAIT: "true"
                   TIMEOUT: "10m"
   ```

2. **`examples/oidc-only/bitbucket-pipelines.yml`** — OIDC + `repo://` chart from a public Helm repository.

   ```yaml
   # Example: OIDC-only v2 pipeline (no static keys) + repo:// chart from a Helm repository.
   # Prerequisites:
   #   - IAM role configured per docs/guides/oidc-setup.md (trust policy scoped to
   #     BITBUCKET_WORKSPACE_UUID + BITBUCKET_REPO_UUID).
   #   - The role's OIDC audience matches OIDC_AUDIENCE below.
   # Expected outcome:
   #   helm upgrade --install of release "nginx" from the bitnami repo.
   # Reference: docs/guides/oidc-setup.md + docs/migration/v1-to-v2.md AUTH section.

   image: atlassian/default-image:4

   pipelines:
     branches:
       main:
         - step:
             name: Deploy to EKS (OIDC)
             deployment: production
             oidc: true
             script:
               - pipe: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2
                 variables:
                   OIDC_AUDIENCE: $OIDC_AUDIENCE
                   ROLE_ARN: $DEPLOY_ROLE_ARN
                   AWS_REGION: eu-central-1
                   CLUSTER_NAME: my-eks-cluster
                   CHART: repo://bitnami/nginx
                   REPO_URL: https://charts.bitnami.com/bitnami
                   CHART_VERSION: 18.2.0
                   RELEASE_NAME: nginx
                   NAMESPACE: production
                   WAIT: "true"
                   TIMEOUT: "10m"
   ```

3. **`examples/oci-chart/bitbucket-pipelines.yml`** — OIDC + `oci://` chart from GHCR with optional Cosign verify.

   ```yaml
   # Example: OCI Helm chart from GHCR, OIDC AWS auth, Cosign keyless verify.
   # Prerequisites:
   #   - IAM role per docs/guides/oidc-setup.md.
   #   - Chart published at oci://ghcr.io/<org>/<chart> and signed with Cosign keyless.
   #   - REGISTRY_USERNAME / REGISTRY_PASSWORD if the OCI registry is private.
   # Expected outcome:
   #   Cosign verifies the chart signature, then helm upgrade --install.
   # Reference: docs/migration/v1-to-v2.md + REQUIREMENTS CHART-03 + CHART-04.

   image: atlassian/default-image:4

   pipelines:
     branches:
       main:
         - step:
             name: Deploy signed OCI chart
             deployment: production
             oidc: true
             script:
               - pipe: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2
                 variables:
                   OIDC_AUDIENCE: $OIDC_AUDIENCE
                   ROLE_ARN: $DEPLOY_ROLE_ARN
                   AWS_REGION: eu-central-1
                   CLUSTER_NAME: my-eks-cluster
                   CHART: oci://ghcr.io/my-org/my-chart
                   CHART_VERSION: 1.4.2
                   REGISTRY_USERNAME: $GHCR_USERNAME
                   REGISTRY_PASSWORD: $GHCR_TOKEN
                   CHART_VERIFY: "true"
                   CHART_VERIFY_CERTIFICATE_IDENTITY: https://github.com/my-org/my-chart/.github/workflows/release.yml@refs/heads/main
                   CHART_VERIFY_CERTIFICATE_OIDC_ISSUER: https://token.actions.githubusercontent.com
                   RELEASE_NAME: my-app
                   NAMESPACE: production
                   WAIT: "true"
                   TIMEOUT: "10m"
   ```

4. **`examples/multi-env/bitbucket-pipelines.yml`** — multi-env deploy with `helm-diff` PR comment on PRs to `main`.

   ```yaml
   # Example: Multi-env (dev / staging / production) deploy with helm-diff PR comment
   # posted to Bitbucket PRs.
   # Prerequisites:
   #   - OIDC role per docs/guides/oidc-setup.md.
   #   - BITBUCKET_TOKEN (repository or workspace variable with PR-write scope) for
   #     posting the diff comment.
   # Expected outcome:
   #   PR → posts `helm diff upgrade` output as a PR comment, no cluster mutation.
   #   main → deploys to production with --wait --atomic.
   # Reference: docs/migration/v1-to-v2.md ACTION=diff section + REQUIREMENTS PIPE-03 + PIPE-05.

   image: atlassian/default-image:4

   pipelines:
     pull-requests:
       '**':
         - step:
             name: helm diff against production (preview)
             deployment: production-preview
             oidc: true
             script:
               - pipe: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2
                 variables:
                   ACTION: diff
                   OIDC_AUDIENCE: $OIDC_AUDIENCE
                   ROLE_ARN: $DEPLOY_ROLE_ARN
                   AWS_REGION: eu-central-1
                   CLUSTER_NAME: prod-eks
                   CHART: ./charts/my-app
                   RELEASE_NAME: my-app
                   NAMESPACE: production
                   POST_DIFF_AS_COMMENT: "true"
                   BITBUCKET_TOKEN: $BITBUCKET_TOKEN

     branches:
       main:
         - step:
             name: Deploy to production (safe upgrade)
             deployment: production
             oidc: true
             script:
               - pipe: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2
                 variables:
                   OIDC_AUDIENCE: $OIDC_AUDIENCE
                   ROLE_ARN: $DEPLOY_ROLE_ARN
                   AWS_REGION: eu-central-1
                   CLUSTER_NAME: prod-eks
                   CHART: ./charts/my-app
                   RELEASE_NAME: my-app
                   NAMESPACE: production
                   SAFE_UPGRADE: "true"
                   WAIT: "true"
                   TIMEOUT: "10m"
   ```

5. **`examples/migration-v1-to-v2/before.yml`** — verbatim v1 pipeline (single deployment step, v1 env-var names, Docker Hub image tag).

   ```yaml
   # Example (MIG-03): v1.x reference pipeline — for diff comparison only.
   # Prerequisites:
   #   - AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY repository variables.
   # Expected outcome:
   #   helm upgrade --install via the legacy v1 pipe (yvogl/aws-eks-helm-deploy:1.3.0).
   # Reference: docs/migration/v1-to-v2.md — every breaking-change section cites a line
   # range in this file or in after.yml.

   image: atlassian/default-image:4

   pipelines:
     branches:
       main:
         - step:
             name: Deploy to EKS (v1)
             deployment: production
             script:
               - pipe: docker://yvogl/aws-eks-helm-deploy:1.3.0
                 variables:
                   AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
                   AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
                   AWS_REGION: eu-central-1
                   CLUSTER_NAME: my-eks-cluster
                   CHART: ./charts/my-app
                   RELEASE_NAME: my-app
                   # NAMESPACE not set → v1 BUG defaults it to "kube-public"
                   CREATE_NAMESPACE: "true"
                   WAIT: "true"
                   TIMEOUT: "10m"
                   # v1 ALWAYS injects bitbucket.* values (META-02 → v2 default flip)
   ```

6. **`examples/migration-v1-to-v2/after.yml`** — v2 equivalent with every breaking change applied.

   ```yaml
   # Example (MIG-03): v2.0 equivalent pipeline.
   # Diff lines vs before.yml:
   #   - Image tag: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2.0.0 (registry + version pin)
   #   - NAMESPACE: production explicit (was implicit kube-public bug in v1)
   #   - INJECT_BITBUCKET_METADATA: "true" if your chart references .Values.bitbucket.* (was unconditional in v1)
   #   - Optional: switch to OIDC (commented below)
   # Prerequisites:
   #   - AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY (or migrate to OIDC).
   # Expected outcome:
   #   helm upgrade --install of release "my-app" into namespace "production".
   # Reference: docs/migration/v1-to-v2.md — Quick migration checklist.

   image: atlassian/default-image:4

   pipelines:
     branches:
       main:
         - step:
             name: Deploy to EKS (v2)
             deployment: production
             script:
               - pipe: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2.0.0
                 variables:
                   AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
                   AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
                   AWS_REGION: eu-central-1
                   CLUSTER_NAME: my-eks-cluster
                   CHART: ./charts/my-app
                   RELEASE_NAME: my-app
                   NAMESPACE: production
                   CREATE_NAMESPACE: "true"
                   WAIT: "true"
                   TIMEOUT: "10m"
                   # v2: set this ONLY if your chart references .Values.bitbucket.*
                   INJECT_BITBUCKET_METADATA: "true"
   ```

7. **`examples/migration-v1-to-v2/README.md`** — line-level explanations.

   ```markdown
   # Migration v1 → v2 — Before/After Diff

   This directory pairs a v1 reference pipeline (`before.yml`) with the v2 equivalent (`after.yml`). Use the `diff -u before.yml after.yml` view alongside [`docs/migration/v1-to-v2.md`](../../docs/migration/v1-to-v2.md) for the prose explanation of each change.

   ## Line-level changes

   | before.yml line | after.yml line | Change | Reference |
   |-----------------|----------------|--------|-----------|
   | `pipe: docker://yvogl/aws-eks-helm-deploy:1.3.0` | `pipe: docker://ghcr.io/yves-vogl/aws-eks-helm-deploy:2.0.0` | Image registry: Docker Hub → GHCR. Pin to `:2.0.0` (or `:2` rolling). | MIG-01 / docs/migration/v1-to-v2.md "Distribution change" |
   | implicit NAMESPACE | `NAMESPACE: production` (explicit) | v1 had a BUG defaulting NAMESPACE to `kube-public`. v2 defaults to `default`. Always set it explicitly. | docs/migration/v1-to-v2.md "NAMESPACE correction" |
   | implicit `INJECT_BITBUCKET_METADATA` (= injected) | `INJECT_BITBUCKET_METADATA: "true"` (explicit opt-in) | v1 unconditionally injected `bitbucket.*` values; v2 defaults to unset. Set `true` ONLY if your chart references `.Values.bitbucket.*`. | META-02 / docs/migration/v1-to-v2.md "INJECT_BITBUCKET_METADATA" |
   | n/a | comment block | Header block standard for every example. | DOC-08 |

   ## Trying it locally

   ```bash
   diff -u before.yml after.yml
   ```

   ## OIDC migration (optional)

   Drop the static-key variables and add `OIDC_AUDIENCE` + `ROLE_ARN` per [`docs/guides/oidc-setup.md`](../../docs/guides/oidc-setup.md). The trust policy template scopes the role to `BITBUCKET_WORKSPACE_UUID` + `BITBUCKET_REPO_UUID` to prevent cross-repo misuse.

   See [`examples/oidc-only/bitbucket-pipelines.yml`](../oidc-only/bitbucket-pipelines.yml) for the full OIDC pipeline.
   ```

8. Verify YAML validity locally:
   - `python -c "import yaml; [yaml.safe_load(open(f)) for f in ['examples/basic/bitbucket-pipelines.yml', 'examples/oidc-only/bitbucket-pipelines.yml', 'examples/oci-chart/bitbucket-pipelines.yml', 'examples/multi-env/bitbucket-pipelines.yml', 'examples/migration-v1-to-v2/before.yml', 'examples/migration-v1-to-v2/after.yml']]"` exits 0.

9. The check-jsonschema lint runs in Task 3 (after pyproject.toml gains the dep).
  </action>
  <verify>
    <automated>test -f examples/basic/bitbucket-pipelines.yml</automated>
    <automated>test -f examples/oidc-only/bitbucket-pipelines.yml</automated>
    <automated>test -f examples/oci-chart/bitbucket-pipelines.yml</automated>
    <automated>test -f examples/multi-env/bitbucket-pipelines.yml</automated>
    <automated>test -f examples/migration-v1-to-v2/before.yml</automated>
    <automated>test -f examples/migration-v1-to-v2/after.yml</automated>
    <automated>test -f examples/migration-v1-to-v2/README.md</automated>
    <automated>grep -F 'OIDC_AUDIENCE' examples/oidc-only/bitbucket-pipelines.yml</automated>
    <automated>grep -F 'oci://' examples/oci-chart/bitbucket-pipelines.yml</automated>
    <automated>grep -F 'POST_DIFF_AS_COMMENT' examples/multi-env/bitbucket-pipelines.yml</automated>
    <automated>grep -F 'yvogl/aws-eks-helm-deploy:1.3.0' examples/migration-v1-to-v2/before.yml</automated>
    <automated>grep -F 'ghcr.io/yves-vogl/aws-eks-helm-deploy:2.0.0' examples/migration-v1-to-v2/after.yml</automated>
    <automated>grep -F 'INJECT_BITBUCKET_METADATA' examples/migration-v1-to-v2/README.md</automated>
  </verify>
  <acceptance_criteria>
    - 7 new files exist under `examples/` (4 standalone YML + before.yml + after.yml + README.md).
    - Every YML file opens with the header block (purpose / prerequisites / expected outcome / reference).
    - `before.yml` uses `yvogl/aws-eks-helm-deploy:1.3.0` (v1 Docker Hub image).
    - `after.yml` uses `ghcr.io/yves-vogl/aws-eks-helm-deploy:2.0.0` (v2 GHCR image).
    - `examples/migration-v1-to-v2/README.md` cites every breaking change with a cross-reference into `docs/migration/v1-to-v2.md`.
    - YAML parses cleanly via `yaml.safe_load` (pre-lint sanity).
  </acceptance_criteria>
  <threat_model>
    - T-07-DOC-XSS: examples are repo-authored Markdown/YAML; no user input.
    - T-07-V5 (lint gate): MITIGATED by Task 3's `check-jsonschema` gate.
  </threat_model>
  <done>
    `examples/` corpus complete; all 7 files present with consistent header blocks; YAML parses; cross-references back into migration guide.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add `check-jsonschema 0.37.3` to dev group + ship the lint gate + the migration-guide structural test</name>
  <files>pyproject.toml, tests/structural/test_examples_lint.py, tests/structural/test_migration_guide.py</files>
  <read_first>
    - pyproject.toml (`[dependency-groups] dev` block — add check-jsonschema sibling to existing dev tools)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (Q8 — check-jsonschema invocation; vendor.bitbucket-pipelines built-in schema)
    - tests/structural/test_governance_files.py (existing structural test style — REPO_ROOT, frozenset of required strings, pytestmark)
    - tests/structural/test_workflow_digest_pins.py (existing helper-function pattern)
    - examples/**/*.yml + docs/migration/v1-to-v2.md (Tasks 1-2 outputs — assertions reference these)
  </read_first>
  <action>
1. Modify `pyproject.toml` `[dependency-groups] dev` array. ADD `check-jsonschema == 0.37.3` (RESEARCH Q8 verified pin) as a new entry alongside the existing dev deps (ruff, mypy, pytest, etc.). DO NOT add to `docs` extra — this is a CI lint tool, NOT a docs-build tool.

   Example block after edit (preserve existing entries; just append):

   ```toml
   [dependency-groups]
   dev = [
       "ruff ~= 0.15",
       "mypy ~= 2.1",
       # ... existing entries unchanged ...
       "check-jsonschema == 0.37.3",
   ]
   ```

2. Create `tests/structural/test_examples_lint.py`:
   - Module docstring: "Structural tests for Phase 7 / Plan 07-04 / DOC-08 + MIG-03. Lints every `examples/**/*.yml` and `examples/**/*.yaml` against the SchemaStore `vendor.bitbucket-pipelines` schema via `check-jsonschema 0.37.3` (RESEARCH Q8). Asserts the MIG-03 trio (before.yml, after.yml, README.md) is complete."
   - `from __future__ import annotations`.
   - Imports: `import pathlib`, `import shutil`, `import subprocess`, `import pytest`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `REPO_ROOT = pathlib.Path(__file__).resolve().parents[2]`
     - `EXAMPLES_DIR = REPO_ROOT / "examples"`
     - `MIGRATION_DIR = EXAMPLES_DIR / "migration-v1-to-v2"`
     - `REQUIRED_EXAMPLE_DIRS: frozenset[str] = frozenset({"basic", "oidc-only", "oci-chart", "multi-env", "migration-v1-to-v2"})`
   - Helper: `def _collect_example_yamls() -> list[pathlib.Path]: return sorted(list(EXAMPLES_DIR.rglob("*.yml")) + list(EXAMPLES_DIR.rglob("*.yaml")))`.
   - Test functions:
     - `test_examples_dir_exists` — `assert EXAMPLES_DIR.is_dir()`.
     - `test_all_required_example_subdirs_exist` — `actual = {p.name for p in EXAMPLES_DIR.iterdir() if p.is_dir()}`; assert `REQUIRED_EXAMPLE_DIRS.issubset(actual)` with failure listing missing entries.
     - `test_each_required_example_has_bitbucket_pipelines_yml` — for each required dir EXCEPT `migration-v1-to-v2`, assert `(EXAMPLES_DIR / dir / "bitbucket-pipelines.yml").is_file()`.
     - `test_migration_dir_has_before_after_readme` — assert `(MIGRATION_DIR / "before.yml").is_file()`, `(MIGRATION_DIR / "after.yml").is_file()`, `(MIGRATION_DIR / "README.md").is_file()` (MIG-03).
     - `test_examples_yamls_lint_clean_via_check_jsonschema` — skip if `shutil.which("uv") is None` OR `importlib.util.find_spec("check_jsonschema") is None`. Collect yamls via helper. Run:
       ```python
       cmd = ["uv", "run", "check-jsonschema", "--builtin-schema", "vendor.bitbucket-pipelines"]
       cmd.extend(str(p) for p in _collect_example_yamls())
       result = subprocess.run(cmd, cwd=REPO_ROOT, capture_output=True, text=True, timeout=60)
       assert result.returncode == 0, f"check-jsonschema failed:\n{result.stdout}\n{result.stderr}"
       ```
     - `test_each_example_yml_has_header_block` — for each yml, assert its first 4 lines contain `# Example:` AND `# Prerequisites:` AND `# Expected outcome:` (the D8 header convention).
   - Type annotations: full mypy --strict.

3. Create `tests/structural/test_migration_guide.py`:
   - Module docstring: "Structural tests for Phase 7 / Plan 07-04 / DOC-03. Asserts the migration guide covers every breaking change and the D10 6-month placeholder is committed verbatim."
   - `from __future__ import annotations`.
   - Imports: `import pathlib`, `import pytest`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `REPO_ROOT = pathlib.Path(__file__).resolve().parents[2]`
     - `MIGRATION_MD = REPO_ROOT / "docs" / "migration" / "v1-to-v2.md"`
     - `OLD_MIGRATION_MD = REPO_ROOT / "docs" / "guides" / "v1-to-v2.md"`
     - `REQUIRED_BREAKING_CHANGE_TOKENS: frozenset[str] = frozenset({"INJECT_BITBUCKET_METADATA", "NAMESPACE", "SET", "VALUES", "Distribution change", "ghcr.io/yves-vogl/aws-eks-helm-deploy"})`
     - `D10_PLACEHOLDER = "2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut."`
   - Test functions:
     - `test_migration_guide_at_new_path` — `assert MIGRATION_MD.is_file()`.
     - `test_old_migration_guide_removed` — `assert not OLD_MIGRATION_MD.exists()`. (Verifies the `git mv` completed.)
     - `test_migration_guide_covers_every_breaking_change` — read text; for each token in `REQUIRED_BREAKING_CHANGE_TOKENS`, assert it appears in the file; failure lists missing tokens.
     - `test_migration_guide_has_d10_six_month_placeholder` — `assert D10_PLACEHOLDER in MIGRATION_MD.read_text()` (SI-07-07 gate; Plan 07-06 also commits this to SECURITY.md).
     - `test_migration_guide_cross_references_examples_diff` — `assert "examples/migration-v1-to-v2" in MIGRATION_MD.read_text()` (MIG-03 cross-link from prose into diff dir).
     - `test_migration_guide_uses_mkdocs_admonitions` — `text = MIGRATION_MD.read_text()`; assert `text.count("!!! ") >= 3` (at least 3 admonition blocks — warning/tip/info/note).
   - Type annotations: full mypy --strict.

4. Run `uv sync --group dev` (installs check-jsonschema 0.37.3).

5. Verify:
   - `bash -lc 'uv run check-jsonschema --builtin-schema vendor.bitbucket-pipelines examples/basic/bitbucket-pipelines.yml examples/oidc-only/bitbucket-pipelines.yml examples/oci-chart/bitbucket-pipelines.yml examples/multi-env/bitbucket-pipelines.yml examples/migration-v1-to-v2/before.yml examples/migration-v1-to-v2/after.yml'` exits 0.
   - `uv run pytest tests/structural/test_examples_lint.py tests/structural/test_migration_guide.py -x --no-cov` — both files green.
   - `uv run pytest tests/structural -q --no-cov` — full structural suite green.
   - `uv run ruff check tests/structural/test_examples_lint.py tests/structural/test_migration_guide.py` clean.
   - `uv run mypy --strict tests/structural/test_examples_lint.py tests/structural/test_migration_guide.py` clean.
  </action>
  <verify>
    <automated>grep -F 'check-jsonschema == 0.37.3' pyproject.toml</automated>
    <automated>test -f tests/structural/test_examples_lint.py</automated>
    <automated>test -f tests/structural/test_migration_guide.py</automated>
    <automated>grep -F 'vendor.bitbucket-pipelines' tests/structural/test_examples_lint.py</automated>
    <automated>grep -F 'def test_migration_guide_covers_every_breaking_change' tests/structural/test_migration_guide.py</automated>
    <automated>grep -F 'D10_PLACEHOLDER' tests/structural/test_migration_guide.py</automated>
    <automated>uv run pytest tests/structural/test_examples_lint.py -x --no-cov</automated>
    <automated>uv run pytest tests/structural/test_migration_guide.py -x --no-cov</automated>
    <automated>uv run check-jsonschema --builtin-schema vendor.bitbucket-pipelines examples/basic/bitbucket-pipelines.yml examples/oidc-only/bitbucket-pipelines.yml examples/oci-chart/bitbucket-pipelines.yml examples/multi-env/bitbucket-pipelines.yml examples/migration-v1-to-v2/before.yml examples/migration-v1-to-v2/after.yml</automated>
    <automated>uv run ruff check tests/structural/test_examples_lint.py tests/structural/test_migration_guide.py</automated>
    <automated>uv run mypy --strict tests/structural/test_examples_lint.py tests/structural/test_migration_guide.py</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[dependency-groups] dev` declares `check-jsonschema == 0.37.3` (RESEARCH Q8 verified pin).
    - `tests/structural/test_examples_lint.py` ships with 6 tests; all green.
    - `tests/structural/test_migration_guide.py` ships with 6 tests; all green.
    - `check-jsonschema --builtin-schema vendor.bitbucket-pipelines` against all 6 example YMLs exits 0.
    - The D10 placeholder appears in `docs/migration/v1-to-v2.md` (SI-07-07).
    - `mkdocs build --strict` continues to exit 0.
  </acceptance_criteria>
  <threat_model>
    - T-07-V10 (Tampering — check-jsonschema): MITIGATED by `==` pin (Apache-2.0; verified 2026-06-21 on PyPI).
    - T-07-V5 (Input validation — bitbucket-pipelines.yml schema): MITIGATED structurally — every shipped example is `vendor.bitbucket-pipelines`-clean.
    - SI-07-05 (D6 invariant): the lint test uses `subprocess` but lives under `tests/`, NOT `src/`.
  </threat_model>
  <done>
    Dev-group dep added, 2 new structural test files ship and are green, lint gate exits 0 against all examples, migration guide DOC-03 coverage verified, D10 placeholder gate green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → CI lint env | `check-jsonschema 0.37.3` pinned; SchemaStore CDN fetched on first run (HTTPS); cached after. |
| consumer copy-pastes examples | `examples/**/*.yml` are static; consumer-side risk is on their AWS/Bitbucket config, not the example file itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-V5 | Tampering | examples/**/*.yml drift from Bitbucket schema | mitigate | check-jsonschema gate in structural test; runs on every PR via the `unit-coverage` ci.yml job (Phase 6) |
| T-07-V10 | Tampering | check-jsonschema package | mitigate | `==` pin; Dependabot |
| T-07-V9 | Communications | SchemaStore HTTPS fetch | accept | One-time per CI cache; HTTPS; cached locally after first run |
| T-07-V14 | Configuration | mkdocs strict-mode broken-link after path move | mitigate | Task 1 retargets all internal links; `mkdocs build --strict` enforced |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | check-jsonschema is Apache-2.0; verified 2026-06-21 |
</threat_model>

<verification>
After all tasks complete:

1. `uv run pytest tests/structural -q --no-cov` — full structural suite green.
2. `uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site` exits 0.
3. `uv run check-jsonschema --builtin-schema vendor.bitbucket-pipelines examples/**/*.yml` exits 0.
4. `test -f docs/migration/v1-to-v2.md && test ! -e docs/guides/v1-to-v2.md` (move successful).
5. `git log --follow --oneline docs/migration/v1-to-v2.md | head -3` shows pre-move history.
6. `grep -F '2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.' docs/migration/v1-to-v2.md` returns 1 hit (SI-07-07).
7. `ls examples/ | wc -l` returns 5.
8. `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2 (D6 invariant).
</verification>

<success_criteria>
- DOC-03: migration guide covers every breaking change in `docs/migration/v1-to-v2.md`; structural test enforces.
- DOC-08: 5 example sub-directories ship; check-jsonschema lint-clean.
- MIG-03: `examples/migration-v1-to-v2/{before, after, README}` ships; line-level explanations.
- `git mv` preserves history.
- mkdocs strict-mode green.
- D10 placeholder committed verbatim (SI-07-07).
- D6 subprocess invariant preserved.
- No new top-level runtime deps (check-jsonschema is dev-group only).
</success_criteria>

<output>
Create `.planning/phases/07-documentation-site-migration-guide/07-04-SUMMARY.md` documenting:
- Confirmation: `git mv` preserved history (output of `git log --follow --oneline docs/migration/v1-to-v2.md | head -5`).
- The list of internal cross-links that were retargeted (path + before/after).
- The number of admonition blocks added (>=3).
- Any check-jsonschema lint warnings that surfaced + how they were resolved.
- Confirmation: `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2.
- Confirmation: the SI-07-07 placeholder string is present in `docs/migration/v1-to-v2.md`.
</output>
</content>
</invoke>