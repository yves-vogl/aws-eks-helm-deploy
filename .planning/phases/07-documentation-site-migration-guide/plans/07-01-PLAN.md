---
phase: 07-documentation-site-migration-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - mkdocs.yml
  - docs/index.md
  - tests/structural/test_mkdocs_build.py
  - tests/structural/test_readme_polish.py
autonomous: true
requirements: [DOC-01, DOC-02]
requirements_addressed: [DOC-01, DOC-02]

must_haves:
  truths:
    - "`mkdocs.yml` declares `site_name: aws-eks-helm-deploy`, the mkdocs-material theme, the mermaid superfences custom_fences block, and the canonical Phase 7 nav skeleton (Home, Quickstart, Migration v1 → v2, Guides, Reference, ADRs, Admin) — every nav slot points at a file that 07-02 / 07-03 / 07-04 / 07-06 will populate (per D1 + D7)."
    - "`pyproject.toml` adds a `[project.optional-dependencies] docs` extra pinning `mkdocs-material == 9.7.6` and `mike == 2.2.0` (RESEARCH Q1 + Q2 verified PyPI pins, 2026-06-21); the existing runtime `[project] dependencies` block is untouched (no new top-level runtime deps per CONTEXT cap-rules)."
    - "`docs/index.md` is the docs-site landing page with a hero card linking to the Quickstart (README), to the Migration v1 → v2 guide, and to the ADR archive — matches D1/D7 'migration guide is the headline page' framing."
    - "`mkdocs build --strict` exits 0 against the Wave 1 nav skeleton (broken-link warnings are fatal per RESEARCH Q9); structural test `tests/structural/test_mkdocs_build.py` runs the build and asserts exit-0."
    - "`tests/structural/test_readme_polish.py` is the DOC-01 gate placeholder — it ships in Wave 1 so Plan 07-06 has a fixed target to extend; in Wave 1 it asserts the README has the existing 7-badge row and the docs-site link is reachable (DOC-01 SC anchor)."
    - "No new subprocess callers introduced under `src/` — D6 subprocess invariant carried forward; structural sanity is enforced by Plan 07-04's grep gate but verified manually in this plan's done criteria as well."
  artifacts:
    - path: "mkdocs.yml"
      provides: "mkdocs-material site config: theme, markdown_extensions, plugins (none beyond mkdocs-material defaults per RESEARCH Q3), nav skeleton"
      contains: "site_name: aws-eks-helm-deploy"
    - path: "docs/index.md"
      provides: "docs-site landing page with hero card pointing at Migration + Quickstart + ADRs"
      contains: "Migration v1"
    - path: "pyproject.toml"
      provides: "`[project.optional-dependencies] docs` extra pinning mkdocs-material 9.7.6 + mike 2.2.0"
      contains: "mkdocs-material == 9.7.6"
    - path: "tests/structural/test_mkdocs_build.py"
      provides: "Structural test invoking `uv run mkdocs build --strict` against tmp_path"
      contains: "def test_mkdocs_build_strict_exits_zero"
    - path: "tests/structural/test_readme_polish.py"
      provides: "DOC-01 gate: README has 7-badge row + 60-second quickstart + docs-site link"
      contains: "def test_readme_has_badge_row"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/migration/v1-to-v2.md"
      via: "nav entry 'Migration v1 → v2' — file created by Plan 07-04"
      pattern: "migration/v1-to-v2.md"
    - from: "mkdocs.yml"
      to: "docs/adr/"
      via: "nav entry 'ADRs' — populated by Plan 07-02"
      pattern: "adr/"
    - from: "mkdocs.yml"
      to: "docs/reference/variables.md"
      via: "nav entry 'Reference / Variables' — file generated by Plan 07-03"
      pattern: "reference/variables.md"
---

<objective>
Scaffold the mkdocs-material 9.7.6 + mike 2.2.0 docs site: ship `mkdocs.yml`, `docs/index.md`, the `[project.optional-dependencies] docs` extra in `pyproject.toml`, and two Wave-1 structural tests that downstream Wave-2 plans (07-03, 07-04, 07-05) and Wave-3 plan (07-06) depend on. The nav skeleton encodes the D1 + D7 framing — Migration v1 → v2 is the second top-level entry, ADRs are an auto-listed directory, and the variable reference under `docs/reference/variables.md` is reserved for Plan 07-03 to fill.

Purpose: DOC-01 (README as entry doc) + DOC-02 (mkdocs-material + mike site published to GitHub Pages) are foundation REQs for the v2.0 docs surface. Plans 07-02 (ADRs), 07-03 (variable generator), 07-04 (migration guide move + examples), 07-05 (docs.yml workflow), 07-06 (README/SECURITY polish), and 07-07 (release ceremony prep) all assume the mkdocs.yml nav exists and `mkdocs build --strict` is green when content is added. This plan is the single Wave-1 prerequisite alongside 07-02 (which is parallel-safe because ADRs slot into an auto-listed nav directory).

Output:
- `mkdocs.yml` with the canonical Phase 7 nav skeleton.
- `docs/index.md` hero card pointing at Migration + Quickstart + ADRs + Reference.
- `pyproject.toml` `[project.optional-dependencies] docs` extra pinning the two RESEARCH-verified PyPI versions.
- `tests/structural/test_mkdocs_build.py` runs `mkdocs build --strict` in a tmp dir and asserts exit-0 with no broken-link warnings.
- `tests/structural/test_readme_polish.py` — Wave-1 DOC-01 gate placeholder that Plan 07-06 will extend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md
@.planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md
@pyproject.toml
@README.md
@docs/guides/v1-to-v2.md
@docs/guides/oidc-setup.md
@docs/admin/repo-settings.md
</context>

<artifacts_this_phase_produces>
This plan creates NEW artifacts consumed by every downstream Phase 7 plan:

- `mkdocs.yml` (NEW; sole source-of-truth for nav, theme, extensions)
- `docs/index.md` (NEW; landing page)
- `pyproject.toml` modifications (NEW `[project.optional-dependencies] docs` extra)
- `tests/structural/test_mkdocs_build.py` — exports test functions:
  - `test_mkdocs_build_strict_exits_zero`
  - `test_mkdocs_yml_declares_mkdocs_material_theme`
  - `test_mkdocs_yml_has_nav_skeleton`
- `tests/structural/test_readme_polish.py` — exports test functions:
  - `test_readme_has_badge_row`
  - `test_readme_has_docs_site_link`
  - `test_readme_has_quickstart_section`

This plan EXTENDS no existing module. Subsequent plans 07-02..07-06 add NEW files which are exercised by `test_mkdocs_build.py` automatically (every new docs page must be reachable from `mkdocs.yml` nav OR auto-listed via the `adr/` glob).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add `[project.optional-dependencies] docs` extra to pyproject.toml + ship Wave-1 structural tests</name>
  <files>pyproject.toml, tests/structural/test_mkdocs_build.py, tests/structural/test_readme_polish.py</files>
  <read_first>
    - pyproject.toml (current `[project]` block + `[dependency-groups]` + `[tool.pytest.ini_options]` — confirm where to slot the new optional-dependencies extra)
    - tests/structural/test_governance_files.py (existing structural test style — docstring, pytestmark, REPO_ROOT pattern)
    - tests/structural/test_workflow_digest_pins.py (existing pathlib + iteration style)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (Q1 + Q2 pinned versions; Q3 plugin set)
  </read_first>
  <action>
1. Modify `pyproject.toml`. Locate the `[project]` table and the `[dependency-groups]` table. After the `[project]` table (before `[project.scripts]`, OR before `[build-system]` — whichever the file currently uses; preserve TOML ordering), add a new `[project.optional-dependencies]` table:

   ```
   [project.optional-dependencies]
   docs = [
       "mkdocs-material == 9.7.6",
       "mike == 2.2.0",
   ]
   ```

   The runtime `dependencies = [...]` list MUST stay untouched. Do NOT touch any existing dependency-groups entries (Plan 07-03 adds `settings-doc == 4.3.2` to the same `docs` extra; Plan 07-04 adds `check-jsonschema == 0.37.3` to the `dev` group). Use targeted edits, not file rewrites.

2. Create `tests/structural/test_mkdocs_build.py` matching the project's structural-test style:
   - Module docstring (2 lines): "Structural tests for Phase 7 Plan 07-01 mkdocs scaffolding. Asserts `mkdocs.yml` declares the mkdocs-material theme + canonical nav, and that `uv run mkdocs build --strict` exits 0 against the current docs tree (per D1 + RESEARCH Q9)."
   - `from __future__ import annotations` first.
   - Imports: `import pathlib`, `import subprocess` (NOTE: this is a TEST file; D6 invariant counts `src/` callers only — `grep -E '^import subprocess' src/aws_eks_helm_deploy/**/*.py` is what is gated, NOT `tests/`), `import pytest`, `import yaml`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `REPO_ROOT = pathlib.Path(__file__).resolve().parents[2]`
     - `MKDOCS_YML = REPO_ROOT / "mkdocs.yml"`
     - `REQUIRED_NAV_TITLES: frozenset[str] = frozenset({"Home", "Quickstart", "Migration v1 → v2", "Guides", "Reference", "ADRs", "Admin"})`
   - Test functions (named verbatim):
     - `test_mkdocs_yml_exists` — `assert MKDOCS_YML.is_file()`.
     - `test_mkdocs_yml_declares_mkdocs_material_theme` — load the YAML, assert `cfg.get("theme", {}).get("name") == "material"`.
     - `test_mkdocs_yml_has_nav_skeleton` — load the YAML, walk `cfg["nav"]` and collect the top-level title strings; assert `REQUIRED_NAV_TITLES.issubset(set(titles))` with failure listing the missing entries.
     - `test_mkdocs_yml_strict_mode_invariant_documented` — assert the file contains the literal substring `strict: true` (mkdocs `strict:` top-level key OR a comment documenting the workflow uses `--strict`; the workflow check is hard-enforced by Plan 07-05, but the structural test prefers an inline `strict: true` for belt-and-braces). Recommendation: declare `strict: true` in mkdocs.yml.
     - `test_mkdocs_build_strict_exits_zero(tmp_path)` — run `uv run mkdocs build --strict --site-dir <tmp_path>/site` from `REPO_ROOT`; assert `returncode == 0`. Capture stderr and include the FIRST 40 lines of stderr in the failure message so verifier output is greppable. Skip with `pytest.skip("mkdocs-material not installed; install with `uv sync --extra docs`")` if `mkdocs` is not on PATH (importlib.util.find_spec("mkdocs") is None). The Wave-1 docs surface is intentionally thin — index.md + ADR auto-listing — so the build is fast (< 5 s).
   - Type annotations: full mypy --strict.
   - No new dependencies. PyYAML is already in deps; `mkdocs` is installed via the new `docs` extra.

3. Create `tests/structural/test_readme_polish.py` as the Wave-1 DOC-01 gate placeholder (Plan 07-06 extends it for sponsor/stars/issues badges):
   - Module docstring: "Structural tests for Phase 7 DOC-01 — README badge row, 60-second quickstart, docs-site link. Plan 07-06 extends with sponsor/stars/issues badge assertions."
   - Imports: `import pathlib`, `import re`, `import pytest`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `REPO_ROOT = pathlib.Path(__file__).resolve().parents[2]`
     - `README_MD = REPO_ROOT / "README.md"`
     - `REQUIRED_BADGE_SOURCES: frozenset[str] = frozenset({"img.shields.io/github/license", "img.shields.io/github/v/release", "GHCR Image", "actions/workflows/ci.yml/badge.svg", "coverage-100%25-brightgreen", "cosign-verified", "securityscorecards.dev"})`
   - Test functions (named verbatim):
     - `test_readme_exists` — `assert README_MD.is_file()`.
     - `test_readme_has_badge_row` — read the file; for each entry in `REQUIRED_BADGE_SOURCES`, assert the string is present in the first 30 lines of the README (badges live at the top). Failure message lists missing badges.
     - `test_readme_has_quickstart_section` — assert the README contains `## Quick start` (case-insensitive match acceptable; use `re.search(r"^##\s+Quick start", text, re.IGNORECASE | re.MULTILINE)`).
     - `test_readme_has_docs_site_link` — assert the README contains a link to either `https://yves-vogl.github.io/aws-eks-helm-deploy/` OR the relative anchor `https://yves-vogl.github.io/aws-eks-helm-deploy` (Plan 07-06 wires this in; Wave-1 placeholder: the test currently expects ONE OR THE OTHER form, OR the literal string `docs site` linked to GitHub Pages — accept any of the three to keep this Wave-1 test green until 07-06 ships). For the Wave-1 commit, leave a TODO comment that 07-06 hardens this to the exact final URL.
   - Type annotations: full mypy --strict.

4. Verify all three changes:
   - `uv run ruff check pyproject.toml tests/structural/test_mkdocs_build.py tests/structural/test_readme_polish.py` (`pyproject.toml` is not a Python file; skip it from ruff — only the `.py` files).
   - `uv run mypy --strict tests/structural/test_mkdocs_build.py tests/structural/test_readme_polish.py`.

NOTE: This task does NOT install the new `docs` extra in CI — Plan 07-05's `docs.yml` workflow runs `uv sync --extra docs --frozen`. Local verification should run `uv sync --extra docs` once after editing `pyproject.toml`.
  </action>
  <verify>
    <automated>grep -F 'mkdocs-material == 9.7.6' pyproject.toml</automated>
    <automated>grep -F 'mike == 2.2.0' pyproject.toml</automated>
    <automated>test -f tests/structural/test_mkdocs_build.py</automated>
    <automated>test -f tests/structural/test_readme_polish.py</automated>
    <automated>grep -F 'def test_mkdocs_yml_declares_mkdocs_material_theme' tests/structural/test_mkdocs_build.py</automated>
    <automated>grep -F 'def test_mkdocs_build_strict_exits_zero' tests/structural/test_mkdocs_build.py</automated>
    <automated>grep -F 'def test_readme_has_badge_row' tests/structural/test_readme_polish.py</automated>
    <automated>uv run ruff check tests/structural/test_mkdocs_build.py tests/structural/test_readme_polish.py</automated>
    <automated>uv run mypy --strict tests/structural/test_mkdocs_build.py tests/structural/test_readme_polish.py</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` declares `[project.optional-dependencies] docs` with EXACTLY `mkdocs-material == 9.7.6` and `mike == 2.2.0` (RESEARCH Q1 + Q2 pins, verbatim).
    - `tests/structural/test_mkdocs_build.py` exists with 5 test functions (4 file/config assertions + 1 build assertion).
    - `tests/structural/test_readme_polish.py` exists with 4 test functions and is currently green (the existing README already has the 7-badge row from Phase 6).
    - `uv run ruff check tests/structural/test_mkdocs_build.py tests/structural/test_readme_polish.py` exits 0.
    - `uv run mypy --strict tests/structural/test_mkdocs_build.py tests/structural/test_readme_polish.py` exits 0.
    - Runtime `[project] dependencies = [...]` is byte-identical to its pre-edit form.
  </acceptance_criteria>
  <threat_model>
    - T-07-V10 (Supply chain — unpinned mkdocs-material/mike): MITIGATED by exact `==` pins from RESEARCH Q1 + Q2; Dependabot will keep them fresh via the `pip` ecosystem block from Phase 6.
    - V14 Configuration (least-privilege): no runtime deps changed; the `docs` extra only loads in the doc-build workflow context.
    - D6 subprocess invariant: this plan's test file imports `subprocess`, but that file lives under `tests/structural/`, NOT `src/`. The Phase 6 / Phase 5 invariant counts `^import subprocess` lines under `src/aws_eks_helm_deploy/` only.
  </threat_model>
  <done>
    `pyproject.toml` has the new `docs` extra. Two Wave-1 structural test files exist and lint-clean. `test_readme_polish.py` is green against the current README. `test_mkdocs_build.py` will go green after Task 2 ships `mkdocs.yml` and `docs/index.md`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author mkdocs.yml + docs/index.md and prove `mkdocs build --strict` is green</name>
  <files>mkdocs.yml, docs/index.md</files>
  <read_first>
    - .planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md (D1 mkdocs-material; D2 mike layout; D7 migration headline; D8 examples corpus locations referenced from nav)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (Q3 zero-plugin set; Q9 strict-mode invariant; mermaid superfences block)
    - README.md (top 30 lines — confirms badge row + quickstart shape that docs/index.md mirrors with hero-card links)
    - docs/guides/v1-to-v2.md (top 30 lines — confirms the existing migration draft is the source-of-truth for Plan 07-04's move target)
  </read_first>
  <action>
1. Create `mkdocs.yml` at REPO ROOT (sibling of `pyproject.toml`, NOT inside `docs/`). Use exactly this skeleton (verbatim — verifier greps these strings):

   ```yaml
   site_name: aws-eks-helm-deploy
   site_description: >-
     Bitbucket Pipelines Pipe deploying Helm charts to AWS EKS — versioned docs for v2.x.
   site_url: https://yves-vogl.github.io/aws-eks-helm-deploy/
   repo_url: https://github.com/yves-vogl/aws-eks-helm-deploy
   repo_name: yves-vogl/aws-eks-helm-deploy
   edit_uri: edit/main/docs/

   # Phase 7 / DOC-02 / D1: mkdocs-material 9.7.6 + mike 2.2.0 (RESEARCH Q1 + Q2).
   # Strict mode is BELT-AND-BRACES: top-level `strict: true` AND `mkdocs build --strict`
   # in `.github/workflows/docs.yml` (Plan 07-05). Strict mode fails on broken internal
   # links AND missing nav references (RESEARCH Q9).
   strict: true

   theme:
     name: material
     features:
       - navigation.tabs
       - navigation.tabs.sticky
       - navigation.sections
       - navigation.expand
       - navigation.top
       - search.highlight
       - search.suggest
       - content.code.copy
       - content.code.annotate
       - content.tabs.link
     palette:
       - media: "(prefers-color-scheme: light)"
         scheme: default
         primary: indigo
         accent: indigo
         toggle:
           icon: material/brightness-7
           name: Switch to dark mode
       - media: "(prefers-color-scheme: dark)"
         scheme: slate
         primary: indigo
         accent: indigo
         toggle:
           icon: material/brightness-4
           name: Switch to light mode
     icon:
       repo: fontawesome/brands/github

   # Phase 7 / D1 + RESEARCH Q3: ZERO additional plugins for v2.0. mkdocs-material's
   # bundled `pymdownx.*` extensions cover all Phase 7 content (admonitions, code tabs,
   # mermaid via superfences, snippets). Add plugins later only when concretely needed.
   plugins:
     - search

   markdown_extensions:
     - admonition
     - attr_list
     - md_in_html
     - tables
     - toc:
         permalink: true
     - pymdownx.details
     - pymdownx.snippets
     - pymdownx.tabbed:
         alternate_style: true
     - pymdownx.superfences:
         custom_fences:
           - name: mermaid
             class: mermaid
             format: !!python/name:pymdownx.superfences.fence_code_format

   # Phase 7 / D7: Migration v1 → v2 is the second top-level nav entry (after Quickstart).
   # ADRs live under docs/adr/ and are auto-discovered (no per-file nav entries needed).
   # Reference / Variables is populated by Plan 07-03 (settings-doc generator).
   nav:
     - Home: index.md
     - Quickstart:
         - Overview: quickstart/overview.md
     - "Migration v1 → v2": migration/v1-to-v2.md
     - Guides:
         - OIDC Setup: guides/oidc-setup.md
     - Reference:
         - Variables: reference/variables.md
     - ADRs:
         - Overview: adr/index.md
     - Admin:
         - Repo Settings Runbook: admin/repo-settings.md
   ```

   Important nav notes:
   - `docs/quickstart/overview.md` is a thin stub shipped in Task 3 below. The detailed quickstart lives in README.md (DOC-01); the docs-site mirror is intentionally minimal.
   - `docs/migration/v1-to-v2.md` is created by Plan 07-04 (git mv of `docs/guides/v1-to-v2.md`). Until 07-04 lands, the `mkdocs build --strict` check on the Phase 7 PR would fail. **Workaround: in Wave 1 ship a temporary stub at `docs/migration/v1-to-v2.md` ALONG with the real target file at `docs/guides/v1-to-v2.md`. Plan 07-04 replaces the stub with the polished move.** Document this in Task 3.
   - `docs/guides/oidc-setup.md` already exists (73 lines, Phase 4).
   - `docs/reference/variables.md` is populated by Plan 07-03. Ship a Wave-1 stub here too (1-line: `<!-- Stub — populated by Plan 07-03 / settings-doc generator -->` + a heading).
   - `docs/adr/index.md` is populated by Plan 07-02. Ship a Wave-1 stub too.
   - `docs/admin/repo-settings.md` already exists (299 lines, Phase 6) — no stub needed; Plan 07-07 extends it.

2. Create `docs/index.md` (hero card + landing copy):

   ```markdown
   # aws-eks-helm-deploy

   A Bitbucket Pipelines Pipe that deploys Helm charts to AWS EKS — small, opinionated,
   and supply-chain hardened. v2.x is the active line; v1.x is frozen.

   ## Where to next

   - **[Migration v1 → v2](migration/v1-to-v2.md)** — read this FIRST if you're upgrading from `yvogl/aws-eks-helm-deploy:1.x`. Covers `INJECT_BITBUCKET_METADATA` flip, `NAMESPACE` correction, image-tag pinning policy, and the distribution change from Docker Hub to GHCR.
   - **[Quickstart](quickstart/overview.md)** — drop a single pipe step into your `bitbucket-pipelines.yml`. The 60-second snippet also lives at the top of the [GitHub README](https://github.com/yves-vogl/aws-eks-helm-deploy).
   - **[Variable reference](reference/variables.md)** — autogenerated from the v2 Pydantic Settings model on every commit.
   - **[OIDC setup guide](guides/oidc-setup.md)** — IAM trust policy + Bitbucket OIDC step-by-step.
   - **[ADRs](adr/index.md)** — every architectural decision shipped in v2.0, MADR 4.0 template.

   ## Versions

   This site uses [mike](https://github.com/jimporter/mike) for versioning:

   - `/v2/` — current (this site).
   - `/v1/` — frozen v1.3.0 reference; security-only patches until 6 months post-v2.0.0 release.

   ## Supply chain

   Every release image is signed with Cosign keyless (Sigstore / Fulcio / Rekor) and ships
   SPDX + CycloneDX SBOMs and SLSA build provenance. See [SECURITY.md](https://github.com/yves-vogl/aws-eks-helm-deploy/blob/main/SECURITY.md) for the disclosure flow.
   ```

3. Ship Wave-1 stub files referenced by the nav so `mkdocs build --strict` passes immediately:

   - `docs/quickstart/overview.md`:
     ```markdown
     # Quickstart

     The canonical 60-second quickstart lives in the [GitHub README](https://github.com/yves-vogl/aws-eks-helm-deploy#quick-start). This page mirrors the snippet for the docs site.

     <!-- TODO Plan 07-04: expand with copy-pasted snippet from README + cross-links to examples/basic/bitbucket-pipelines.yml. -->
     ```
   - `docs/migration/v1-to-v2.md`:
     ```markdown
     # Migration v1 → v2

     <!-- STUB — Plan 07-04 replaces this with the polished move of docs/guides/v1-to-v2.md via `git mv`. Keeping the file present at this path satisfies mkdocs `--strict` for Wave 1. -->

     See [docs/guides/v1-to-v2.md](../guides/v1-to-v2.md) for the current draft (Phase 5/6 content). Plan 07-04 moves the draft here and polishes it.
     ```
   - `docs/reference/variables.md`:
     ```markdown
     <!-- STUB — Plan 07-03 replaces this via `scripts/generate-variables-doc.py` running `settings-doc generate --class aws_eks_helm_deploy.settings.Settings --output-format markdown`. -->

     # Variables

     This page is generated from `src/aws_eks_helm_deploy/settings.py`. Do NOT edit by hand.
     ```
   - `docs/adr/index.md`:
     ```markdown
     # Architecture Decision Records

     <!-- STUB — Plan 07-02 ships 9 MADR 4.0 ADRs and replaces this index with a full listing. -->
     ```

4. Run `uv sync --extra docs` locally (the maintainer's environment) so `uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site` exits 0. Capture any strict-mode warnings (RESEARCH Q9: `validation.links.not_found` is fatal) and fix them by adjusting either the nav or the stub content. The Wave-1 docs surface is small — index.md + 4 stubs + the existing `docs/guides/oidc-setup.md` + `docs/admin/repo-settings.md` — so the build is fast (< 5 s).

5. Verify `tests/structural/test_mkdocs_build.py` now passes (`uv run pytest tests/structural/test_mkdocs_build.py -x --no-cov`).

NOTE on mkdocs `strict:` top-level key: as of mkdocs 1.6+, `strict: true` at the mkdocs.yml top level is honored by `mkdocs build` (no `--strict` flag needed). The Phase 7 workflow (Plan 07-05) keeps `--strict` on the CLI as belt-and-braces.
  </action>
  <verify>
    <automated>test -f mkdocs.yml</automated>
    <automated>test -f docs/index.md</automated>
    <automated>test -f docs/quickstart/overview.md</automated>
    <automated>test -f docs/migration/v1-to-v2.md</automated>
    <automated>test -f docs/reference/variables.md</automated>
    <automated>test -f docs/adr/index.md</automated>
    <automated>grep -F 'site_name: aws-eks-helm-deploy' mkdocs.yml</automated>
    <automated>grep -F 'name: material' mkdocs.yml</automated>
    <automated>grep -F 'strict: true' mkdocs.yml</automated>
    <automated>grep -F 'Migration v1 → v2' mkdocs.yml</automated>
    <automated>grep -F 'pymdownx.superfences' mkdocs.yml</automated>
    <automated>uv run pytest tests/structural/test_mkdocs_build.py -x --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `mkdocs.yml` exists at repo root with EXACTLY the nav skeleton declared above (`REQUIRED_NAV_TITLES` from Task 1 are all present).
    - `theme.name == "material"`, `strict: true`, `pymdownx.superfences` with `mermaid` custom_fence are all present.
    - `docs/index.md` exists with hero card linking to Migration + Quickstart + Variables + OIDC + ADRs.
    - 4 stub files exist (`quickstart/overview.md`, `migration/v1-to-v2.md`, `reference/variables.md`, `adr/index.md`) — sufficient to make `mkdocs build --strict` green in Wave 1.
    - `uv run pytest tests/structural/test_mkdocs_build.py -x --no-cov` exits 0 with all 5 tests passing.
    - The build artifact at `/tmp/mkdocs-test-site/index.html` opens and shows the rendered nav.
  </acceptance_criteria>
  <threat_model>
    - T-07-DOC-XSS (RESEARCH §Security Domain): docs are repo-authored; no user input → XSS surface = N/A.
    - T-07-V14 (mkdocs strict mode prevents broken-link drift): `strict: true` at top level + `--strict` on CLI both enforced.
    - SI-07-03 (mkdocs build --strict is the build command): asserted both by `mkdocs.yml strict: true` AND the workflow flag in Plan 07-05.
  </threat_model>
  <done>
    `mkdocs.yml` + `docs/index.md` + 4 nav stubs ship; `mkdocs build --strict` exits 0; all 5 tests in `test_mkdocs_build.py` pass; the nav skeleton matches D1 + D7 exactly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire tests/structural into the unit-coverage tier and verify full suite green</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (`[tool.pytest.ini_options]` testpaths block and `[tool.coverage]` — confirm `tests/structural` is already covered via the parent `tests/` path)
    - tests/structural/__init__.py (already exists from Phase 6)
  </read_first>
  <action>
1. Re-read `pyproject.toml` `[tool.pytest.ini_options]`. The current `testpaths = ["tests"]` already includes `tests/structural` implicitly. **No edit needed in most cases.** If `testpaths` lists specific subdirs explicitly (e.g., `["tests/unit", "tests/structural"]`), confirm `tests/structural` is already in the list and skip this step.

2. Verify the full local test suite still passes with the new Wave-1 tests:
   - `uv run pytest tests/structural -q --no-cov` — all Phase 6 + Phase 7 structural tests pass.
   - `uv run pytest tests/unit tests/structural --cov=src/aws_eks_helm_deploy --cov-branch --cov-fail-under=100` — coverage gate holds.

3. Verify mypy + ruff stay green across the new files:
   - `uv run mypy --strict src/aws_eks_helm_deploy` — coverage gate is `src/` only.
   - `uv run ruff check src/ tests/ scripts/` — repo-wide lint.

4. Do NOT add `tests/structural` to coverage `omit` — structural tests use stdlib + yaml only; they do not introduce uncovered production code.

5. If `pyproject.toml` already covers `tests/structural` (it does via `testpaths = ["tests"]`), this Task ships ZERO file changes. Document that explicitly in the SUMMARY: "No pyproject.toml edit required in Plan 07-01 Task 3 — `testpaths = ['tests']` already picks up `tests/structural`."

NOTE: pyproject.toml is a heavily-shared file. Use targeted edits ONLY if strictly necessary; otherwise leave it untouched.
  </action>
  <verify>
    <automated>uv run pytest tests/structural -q --no-cov</automated>
    <automated>uv run pytest tests/unit tests/structural --cov=src/aws_eks_helm_deploy --cov-branch --cov-fail-under=100</automated>
    <automated>uv run mypy --strict src/aws_eks_helm_deploy</automated>
    <automated>uv run ruff check src/ tests/ scripts/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/structural -q --no-cov` exits 0 — Phase 6 structural suite + new Phase 7 Wave-1 tests all green.
    - `uv run pytest tests/unit tests/structural --cov-fail-under=100` exits 0 — 100% coverage on `src/aws_eks_helm_deploy` holds.
    - `uv run mypy --strict src/aws_eks_helm_deploy` exits 0 — no new strict regressions.
    - `uv run ruff check src/ tests/ scripts/` exits 0.
    - pyproject.toml diff is minimal (likely zero lines) if testpaths already covered structural.
  </acceptance_criteria>
  <threat_model>
    No new threats introduced. Coverage discipline preserves Phase 6 SI-06-09 (Python source coverage gate).
  </threat_model>
  <done>
    Wave-1 scaffolding ships, all structural tests green, coverage gate green, lint + mypy green. Plans 07-02 / 07-03 / 07-04 / 07-05 / 07-06 / 07-07 can build on a working mkdocs.yml + nav skeleton.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External PyPI → docs build environment | `mkdocs-material 9.7.6` and `mike 2.2.0` downloaded from PyPI; mitigated by exact `==` version pin + `uv.lock` integrity + future Dependabot bumps. |
| docs/* → rendered HTML on GitHub Pages | Docs are repo-authored Markdown; no user input → no XSS surface (RESEARCH §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | PyPI packages mkdocs-material / mike | mitigate | Exact `==` version pins (RESEARCH Q1 + Q2 verified 2026-06-21); `uv.lock` integrity; Dependabot pip ecosystem keeps current. |
| T-07-02 | Tampering | mkdocs.yml broken-link drift | mitigate | `strict: true` top-level + `mkdocs build --strict` in workflow + `test_mkdocs_build_strict_exits_zero` structural test. |
| T-07-03 | Information Disclosure | docs rendering arbitrary user input | accept | N/A — docs are repo-authored; no XSS surface (RESEARCH §Security Domain V5). |
| T-07-SC | Tampering | new PyPI packages introduced this plan | mitigate | mkdocs-material + mike are both well-established (≥ 100k weekly downloads; MIT + BSD-3 licenses; verified on PyPI 2026-06-21). NO `[ASSUMED]` / `[SUS]` packages in this plan — Plan 07-03 (settings-doc) and Plan 07-04 (check-jsonschema) carry the same disposition. |
</threat_model>

<verification>
After all tasks complete:

1. `uv run pytest tests/structural -x --no-cov` — Wave-1 structural tests pass (3 new test files; mkdocs build assertions green).
2. `uv run pytest tests/unit tests/structural --cov=src/aws_eks_helm_deploy --cov-branch --cov-fail-under=100` — 100% coverage holds.
3. `uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site` exits 0 against the Wave-1 nav skeleton.
4. `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2 (D6 invariant — Plan 07-01 introduces ZERO new src/ subprocess callers).
5. `grep -F 'mkdocs-material == 9.7.6' pyproject.toml` returns 1 hit.
6. `grep -F 'mike == 2.2.0' pyproject.toml` returns 1 hit.
</verification>

<success_criteria>
- mkdocs-material 9.7.6 + mike 2.2.0 scaffolding ships in `pyproject.toml` `[project.optional-dependencies] docs`.
- `mkdocs.yml` declares the canonical D1 + D7 nav skeleton with `Migration v1 → v2` as the second top-level entry.
- `strict: true` is enforced at both mkdocs.yml level AND CLI level (Plan 07-05 honors `--strict` in the workflow).
- `docs/index.md` is a hero-card landing page with links to Migration, Quickstart, Variables, OIDC, ADRs.
- Wave-1 stubs exist for migration, quickstart, reference, adr — sufficient for `mkdocs build --strict` to be green pre-content.
- Two new structural test files (`test_mkdocs_build.py`, `test_readme_polish.py`) ship and are green.
- D6 subprocess invariant preserved — zero new `src/` callers (test files don't count).
- 100% line+branch coverage holds on `src/aws_eks_helm_deploy`.
- No top-level runtime deps added (the `docs` extra is optional).
</success_criteria>

<output>
Create `.planning/phases/07-documentation-site-migration-guide/07-01-SUMMARY.md` when done, documenting:
- The final mkdocs.yml nav structure (so Plan 07-04 has an exact target for the migration file move and Plan 07-03 has an exact target for the variable reference page).
- The list of Wave-1 stub files shipped (so Plan 07-04 knows the migration stub to REPLACE via git mv).
- Any deviation from the planned plugin set (e.g., if a content need surfaced requiring `mkdocs-include-markdown-plugin`; default expectation per RESEARCH Q3 is zero additional plugins).
- pyproject.toml diff (likely ~6 lines: the new `[project.optional-dependencies]` table).
- Confirmation that `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2 (D6 invariant preserved).
</output>
</content>
</invoke>