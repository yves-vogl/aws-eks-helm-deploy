---
phase: 07-documentation-site-migration-guide
plan: 07
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06"]
files_modified:
  - .github/PULL_REQUEST_TEMPLATE.md
  - docs/admin/repo-settings.md
  - tests/structural/test_governance_files.py
  - tests/structural/test_repo_settings_runbook.py
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06, DOC-07, DOC-08, MIG-03]
requirements_addressed: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06, DOC-07, DOC-08, MIG-03]

must_haves:
  truths:
    - "`docs/admin/repo-settings.md` gains 5 new sections (5-9) AFTER the existing 4 — Pages enablement (Section 5), `mike set-default v2` one-shot (Section 6), `mike deploy v1` one-shot (Section 7), Bitbucket Pipe Marketplace listing update (Section 8), Docker Hub README banner update (Section 9); the existing sections 1-4 (PVR, branch protection, GPG-sign, project board) stay byte-identical."
    - "`.github/PULL_REQUEST_TEMPLATE.md` is EXTENDED to include a 'Maintainer manual steps after merge' checklist for v2.0.0 tag-cut ceremony per D11 (existing template content is preserved; new section is additive)."
    - "Section 5 (Pages enablement) ships the EXACT `gh api` POST-then-PUT idempotent pattern from RESEARCH Q6 (the public-repo caveat is N/A); plus the `gh api ... --jq` verification command."
    - "Section 7 (`mike deploy v1` one-shot) explicitly notes RESEARCH Q10 pitfall #6: this command MUST run from the maintainer's local machine, NEVER from CI."
    - "Section 8 (Bitbucket Pipe Marketplace) documents the manual web-UI update step + a link to the marketplace listing edit URL; Section 9 (Docker Hub) documents the manual paste of the deprecation banner from `docs/migration/v1-to-v2.md` 'Distribution change' section."
    - "Two structural tests: (a) `test_repo_settings_runbook.py` asserts sections 5-9 are present with the required `gh api` / `mike` / link strings; (b) `test_governance_files.py` is EXTENDED with one new test asserting the PR template contains the new maintainer-checklist section."
    - "A blocking maintainer checkpoint at the end of this plan (`type: checkpoint:human-verify`) — the plan is `autonomous: false` because Yves must confirm the v2.0.0 release ceremony scope (D11) before this plan is considered complete."
  artifacts:
    - path: "docs/admin/repo-settings.md"
      provides: "Maintainer runbook with sections 5-9 added (Pages, mike one-shots, Marketplace, Docker Hub)"
      contains: "## 5. Enable GitHub Pages"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "Pre-existing PR template extended with v2.0.0 tag-cut maintainer checklist"
      contains: "Maintainer manual steps after merge"
    - path: "tests/structural/test_repo_settings_runbook.py"
      provides: "Structural assertions on sections 5-9 of docs/admin/repo-settings.md"
      contains: "def test_repo_settings_has_pages_enablement_section"
    - path: "tests/structural/test_governance_files.py"
      provides: "Existing test extended with PR-template maintainer-checklist assertion"
      contains: "def test_pr_template_has_maintainer_checklist"
  key_links:
    - from: "docs/admin/repo-settings.md"
      to: ".github/workflows/docs.yml"
      via: "Section 5 enables Pages; once enabled, docs.yml's mike deploy can push to gh-pages"
      pattern: "gh-pages"
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "docs/admin/repo-settings.md"
      via: "PR template checklist references the runbook sections for each manual step"
      pattern: "docs/admin/repo-settings.md"
---

<objective>
Ship the v2.0.0 release ceremony preparation per D11: extend `docs/admin/repo-settings.md` with 5 new sections covering Pages enablement (RESEARCH Q6), `mike set-default v2` one-shot (RESEARCH Q10 pitfall #5), `mike deploy v1` one-shot (RESEARCH Q10 pitfall #6), Bitbucket Pipe Marketplace listing update (D11), and Docker Hub README deprecation banner (MIG-01 carry-over via D11). Extend `.github/PULL_REQUEST_TEMPLATE.md` with a maintainer-manual-steps checklist for the v2.0.0 tag-cut. Finalize the plan with a blocking maintainer checkpoint (D11 confirmation).

Purpose: D11 specifies the v2.0.0 release ceremony scope. The Phase 7 PR ships every artifact except the actual tag-cut + Bitbucket Pipe Marketplace update + Docker Hub banner update — those are post-merge manual maintainer actions. This plan SURFACES them via the runbook + PR-template checklist so Yves doesn't lose track post-merge.

Output:
- `docs/admin/repo-settings.md` — extended with sections 5-9 (additive after the existing 4 sections).
- `.github/PULL_REQUEST_TEMPLATE.md` — extended with the v2.0.0 maintainer-checklist section.
- `tests/structural/test_repo_settings_runbook.py` — new file with section-presence assertions.
- `tests/structural/test_governance_files.py` — extended with 1 new test for the PR-template checklist.
- Blocking maintainer checkpoint at end-of-plan.

This plan depends on all 6 prior plans because the runbook sections REFERENCE the artifacts they ship (mkdocs.yml from 07-01, ADRs from 07-02, docs-drift from 07-03, migration guide + examples from 07-04, docs.yml from 07-05, SECURITY.md + badges from 07-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md
@.planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md
@docs/admin/repo-settings.md
@docs/migration/v1-to-v2.md
@.github/PULL_REQUEST_TEMPLATE.md
@.github/workflows/docs.yml
@tests/structural/test_governance_files.py
@SECURITY.md
@README.md
</context>

<artifacts_this_phase_produces>
- `docs/admin/repo-settings.md` (MODIFIED — adds sections 5-9 + a new Notes bullet referencing them)
- `.github/PULL_REQUEST_TEMPLATE.md` (MODIFIED — adds maintainer-checklist section)
- `tests/structural/test_repo_settings_runbook.py` (NEW)
- `tests/structural/test_governance_files.py` (MODIFIED — adds 1 test fn)
</artifacts_this_phase_produces>

<files_not_touched>
SUBPROCESS INVARIANT CARRIERS (D6 — Phase 7 introduces ZERO new src/ subprocess callers):
- `src/aws_eks_helm_deploy/helm/client.py` — NOT TOUCHED.
- `src/aws_eks_helm_deploy/chart/oci.py` — NOT TOUCHED.

Other invariant carriers:
- `pyproject.toml` — NOT TOUCHED (no new deps in Wave 4).
- `.github/workflows/*.yml` — NOT TOUCHED (Plans 07-03 + 07-05 ship the workflow changes).
- `mkdocs.yml` — NOT TOUCHED (Plan 07-01).
- `docs/**/*.md` EXCEPT `docs/admin/repo-settings.md` — NOT TOUCHED (Plans 07-02 + 07-04 + 07-06).
- `examples/**/*` — NOT TOUCHED (Plan 07-04).
- `scripts/**/*` — NOT TOUCHED (Plan 07-03).
- `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `README.md` — NOT TOUCHED (Plan 07-06).
- All ADRs `0001-..0009-*.md` and `0000-template.md` — NOT TOUCHED (Plan 07-02).
</files_not_touched>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs/admin/repo-settings.md with sections 5-9</name>
  <files>docs/admin/repo-settings.md</files>
  <read_first>
    - docs/admin/repo-settings.md (Phase 6 — 299-line file with sections 1-4 and a Notes section at the end; preserve sections 1-4 byte-identical)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (Q6 — exact Pages enablement payload; Q10 pitfalls #5 + #6 — mike one-shots)
    - .planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md (D11 — v2.0.0 release ceremony scope)
    - docs/migration/v1-to-v2.md (Plan 07-04 — "Distribution change" section is the source of the Docker Hub deprecation banner text)
  </read_first>
  <action>
1. APPEND sections 5, 6, 7, 8, 9 to the END of the existing sections 1-4 (i.e., insert BEFORE the existing `## Notes` block at line 286). Do NOT modify sections 1-4 byte-by-byte — verifier will diff them.

2. **Section 5 — Enable GitHub Pages (DOC-02 / Plan 07-05 prerequisite)**

   ```markdown
   ## 5. Enable GitHub Pages (DOC-02)

   The `.github/workflows/docs.yml` workflow (Plan 07-05) requires GitHub Pages to
   be enabled with `gh-pages` as the source branch BEFORE the first `mike deploy`
   run can succeed. This is a one-shot step.

   **Command (idempotent — try POST first, fall back to PUT if Pages already enabled):**

   ```bash
   # Try first-time enable:
   gh api repos/yves-vogl/aws-eks-helm-deploy/pages \
     -X POST \
     -f source[branch]=gh-pages \
     -f source[path]=/

   # If POST returned 409 (already enabled), use PUT instead:
   gh api repos/yves-vogl/aws-eks-helm-deploy/pages \
     -X PUT \
     -f source[branch]=gh-pages \
     -f source[path]=/
   ```

   **Verify:**

   ```bash
   gh api repos/yves-vogl/aws-eks-helm-deploy/pages \
     --jq '{url:.html_url,source:.source}'
   # Expected: { "url": "https://yves-vogl.github.io/aws-eks-helm-deploy/",
   #             "source": { "branch": "gh-pages", "path": "/" } }
   ```

   **Permissions:** repo admin OR "manage GitHub Pages settings".

   **Source:** RESEARCH Q6.
   ```

3. **Section 6 — `mike set-default v2` one-shot (RESEARCH Q10 pitfall #5)**

   ```markdown
   ## 6. Set default mike alias to v2 (DOC-02 SC-3)

   `.github/workflows/docs.yml` deploys `v2 + latest` aliases on every `main`
   commit. Setting the default alias writes `index.html` at the gh-pages root
   redirecting to `/v2/`. This is a one-shot maintainer command — NOT in CI
   (RESEARCH Q10 pitfall #5: combining with concurrent CI deploys can produce a
   stale default).

   **Run ONCE after the first `mike deploy v2 latest` workflow run succeeds:**

   ```bash
   git fetch origin gh-pages
   git worktree add /tmp/gh-pages gh-pages
   cd /tmp/gh-pages

   uv sync --extra docs   # installs mike 2.2.0
   uv run mike set-default v2 --push

   cd - && git worktree remove /tmp/gh-pages
   ```

   **Verify:**

   ```bash
   curl -sI https://yves-vogl.github.io/aws-eks-helm-deploy/ | grep -F 'location'
   # Expected: location header redirecting to /v2/
   ```

   **Re-run only if** the gh-pages branch is reset OR a future `mike deploy` is
   misconfigured.
   ```

4. **Section 7 — `mike deploy v1` one-shot snapshot (RESEARCH Q10 pitfall #6)**

   ```markdown
   ## 7. Deploy frozen v1 docs snapshot (D2 — Plan 07-01)

   The mike layout in CONTEXT D2 reserves `/v1/` for a single-page frozen v1.3.0
   reference. This deploy is a one-shot maintainer command (NEVER from CI —
   RESEARCH Q10 pitfall #6: running from CI would re-render `/v1/` on every push,
   potentially with newer mkdocs-material HTML).

   **Run ONCE from a local workspace pinned at the v1 docs snapshot:**

   ```bash
   # Check out the v1.3.0 docs snapshot (or a `v1.3.0-docs-snapshot` tag if one exists):
   git checkout v1.3.0  # or v1.3.0-docs-snapshot
   uv sync --extra docs
   uv run mike deploy --push v1

   git checkout main
   ```

   **Banner on /v1/:** CONTEXT D2 specifies the banner "v1 is frozen — security-only
   patches until 2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.
   Use v2: <link>." Update the v1 docs site index manually with this banner before
   the `mike deploy --push v1` command.

   **Verify:**

   ```bash
   uv run mike list
   # Expected output contains both `v1` and `v2`.
   curl -sf https://yves-vogl.github.io/aws-eks-helm-deploy/v1/ | head -5
   # Expected: v1 docs HTML renders.
   ```
   ```

5. **Section 8 — Update Bitbucket Pipe Marketplace listing (D11)**

   ```markdown
   ## 8. Update Bitbucket Pipe Marketplace listing (D11)

   After the v2.0.0 tag-cut, update the Bitbucket Pipe Marketplace listing to
   reflect the new image registry, version, and capabilities (OIDC, OCI charts,
   diff/rollback actions, signed images). The marketplace listing is a web-UI
   only step (no Bitbucket REST API for marketplace edits as of 2026-06).

   **Listing edit URL:**
   https://bitbucket.org/yvesvogl/aws-eks-helm-deploy/admin/pipelines/pipe-info

   **Fields to update:**
   - Image tag (point at `ghcr.io/yves-vogl/aws-eks-helm-deploy:2.0.0`).
   - "What's new in 2.0" description: paste from `docs/migration/v1-to-v2.md` Quick
     migration checklist.
   - Tags: add `oidc`, `signed`, `multi-arch`.
   - README pointer: link to the GitHub docs site (https://yves-vogl.github.io/aws-eks-helm-deploy/).

   This is **not idempotent via web UI** — verify visually after submitting.
   ```

6. **Section 9 — Post Docker Hub README deprecation banner (MIG-01 / D10 carryover)**

   ```markdown
   ## 9. Post Docker Hub README deprecation banner (MIG-01)

   The Docker Hub `yvogl/aws-eks-helm-deploy` repository is frozen at v1.3.0
   (MIG-01). Post a deprecation banner to its README so consumers searching
   Docker Hub land on the GHCR migration path.

   **Banner source:** `docs/migration/v1-to-v2.md`, section "Distribution change
   (Phase 6 / MIG-01)" — paste the prose verbatim.

   **Docker Hub README edit URL:**
   https://hub.docker.com/repository/docker/yvogl/aws-eks-helm-deploy/general

   **Recommended banner template** (copy into the top of the Docker Hub README,
   above the existing content):

   ```markdown
   > **⚠️ Deprecated.** v1.3.0 is the final v1.x image and is frozen on Docker
   > Hub. New consumers should pull v2.x from GitHub Container Registry at
   > `ghcr.io/yves-vogl/aws-eks-helm-deploy` (rolling `:2` tag) or a pinned
   > version (`:2.0.0`).
   >
   > Security fixes for v1.x are released for 6 months from the v2.0.0 release
   > date — ending `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.`
   >
   > Migration guide: https://yves-vogl.github.io/aws-eks-helm-deploy/migration/v1-to-v2/
   ```

   Web-UI only step; no API. **Not idempotent via API.** Confirm visually after
   submitting.
   ```

7. UPDATE the existing `## Notes` section at the end (line 286-300) to reference the new sections. ADD these bullets to the existing list:

   ```markdown
   - Sections 5–7 (Pages, mike set-default, mike deploy v1) are `gh api` + `mike`
     commands and are idempotent for sections 5 and 6 (Section 7 is a one-shot
     intent — re-running is harmless but unnecessary).
   - Sections 8–9 (Bitbucket Pipe Marketplace + Docker Hub banner) are web-UI
     only with no programmatic idempotency guarantee; confirm visually.
   - The literal placeholder `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.` is the SI-07-07 invariant; it appears in three places in v2.0:
     `SECURITY.md` (Plan 07-06), `docs/migration/v1-to-v2.md` (Plan 07-04), AND
     `docs/admin/repo-settings.md` Section 7 + 9 (this plan). After the v2.0.0
     tag-cut, the maintainer replaces all four occurrences in a single follow-up
     PR.
   ```

8. Verify the file still renders cleanly under `mkdocs build --strict`:
   - `uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site` exits 0.
   - All internal links resolve.

9. Verify sections 1-4 byte-identical to pre-edit form:
   - `git diff docs/admin/repo-settings.md | head -200` shows ONLY additions in the section 5-9 region + the Notes additions. Sections 1-4 must show zero diff.
  </action>
  <verify>
    <automated>grep -F '## 5. Enable GitHub Pages' docs/admin/repo-settings.md</automated>
    <automated>grep -F '## 6. Set default mike alias to v2' docs/admin/repo-settings.md</automated>
    <automated>grep -F '## 7. Deploy frozen v1 docs snapshot' docs/admin/repo-settings.md</automated>
    <automated>grep -F '## 8. Update Bitbucket Pipe Marketplace listing' docs/admin/repo-settings.md</automated>
    <automated>grep -F '## 9. Post Docker Hub README deprecation banner' docs/admin/repo-settings.md</automated>
    <automated>grep -F 'gh api repos/yves-vogl/aws-eks-helm-deploy/pages' docs/admin/repo-settings.md</automated>
    <automated>grep -F 'mike set-default v2 --push' docs/admin/repo-settings.md</automated>
    <automated>grep -F 'mike deploy --push v1' docs/admin/repo-settings.md</automated>
    <automated>grep -F 'bitbucket.org/yvesvogl/aws-eks-helm-deploy' docs/admin/repo-settings.md</automated>
    <automated>grep -F 'hub.docker.com/repository/docker/yvogl/aws-eks-helm-deploy' docs/admin/repo-settings.md</automated>
    <automated>grep -F '2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.' docs/admin/repo-settings.md</automated>
    <automated>uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site</automated>
  </verify>
  <acceptance_criteria>
    - Sections 5-9 ship at the END of the runbook (before the Notes block).
    - Sections 1-4 are byte-identical to pre-edit form (verified by diff).
    - Sections 5-9 contain the exact `gh api`, `mike`, and web-UI URLs documented above.
    - The D10 6-month placeholder appears in sections 7 + 9 (SI-07-07).
    - mkdocs build --strict is green.
  </acceptance_criteria>
  <threat_model>
    - SI-07-07 (D10 placeholder propagated): MITIGATED — the placeholder now appears in 3 source files (SECURITY.md, docs/migration/v1-to-v2.md, docs/admin/repo-settings.md).
    - T-07-DOC: documentation surface; no code paths.
  </threat_model>
  <done>
    Maintainer runbook complete; mkdocs strict green; all 5 new sections present with verbatim commands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend .github/PULL_REQUEST_TEMPLATE.md with maintainer-checklist + ship structural tests</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md, tests/structural/test_repo_settings_runbook.py, tests/structural/test_governance_files.py</files>
  <read_first>
    - .github/PULL_REQUEST_TEMPLATE.md (Phase 6 / Plan 06-10 — current 30ish-line file; preserve existing content)
    - tests/structural/test_governance_files.py (Phase 6 — `PR_TEMPLATE` constant + existing assertions)
    - docs/admin/repo-settings.md (Task 1 output — runbook URLs to reference)
  </read_first>
  <action>
1. Extend `.github/PULL_REQUEST_TEMPLATE.md`. APPEND a new section at the END (after existing sections):

   ```markdown
   ## Maintainer manual steps after merge (v2.0.0 tag-cut ceremony — D11)

   *This section applies ONLY to the Phase 7 PR that lands the final v2.0 docs surface.*
   *Subsequent PRs should remove this section from their body before opening.*

   - [ ] `git tag v2.0.0 && git push --tags` — triggers `.github/workflows/release.yml`.
   - [ ] Monitor release.yml run; verify Cosign signature + SBOM attestations attached.
   - [ ] After the v2 docs site is live: enable GitHub Pages per [`docs/admin/repo-settings.md` §5](../blob/main/docs/admin/repo-settings.md#5-enable-github-pages-doc-02).
   - [ ] After the first `mike deploy v2 latest` workflow run succeeds: run [`mike set-default v2 --push`](../blob/main/docs/admin/repo-settings.md#6-set-default-mike-alias-to-v2-doc-02-sc-3).
   - [ ] Deploy the frozen v1 docs snapshot per [`docs/admin/repo-settings.md` §7](../blob/main/docs/admin/repo-settings.md#7-deploy-frozen-v1-docs-snapshot-d2--plan-07-01).
   - [ ] Update the Bitbucket Pipe Marketplace listing per [`docs/admin/repo-settings.md` §8](../blob/main/docs/admin/repo-settings.md#8-update-bitbucket-pipe-marketplace-listing-d11).
   - [ ] Post the Docker Hub README deprecation banner per [`docs/admin/repo-settings.md` §9](../blob/main/docs/admin/repo-settings.md#9-post-docker-hub-readme-deprecation-banner-mig-01).
   - [ ] Compute the absolute end-of-support date for v1.x (= v2.0.0 release date + 6 months) and open a follow-up PR replacing the literal `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.` placeholder in `SECURITY.md`, `docs/migration/v1-to-v2.md`, AND `docs/admin/repo-settings.md` sections 7 + 9 (SI-07-07 invariant gate enforces).
   ```

   DO NOT remove or modify the existing PR template sections. Append-only.

2. Create `tests/structural/test_repo_settings_runbook.py`:
   - Module docstring: "Structural tests for Phase 7 / Plan 07-07. Asserts docs/admin/repo-settings.md ships the 5 new sections (5-9) for the v2.0.0 release ceremony per D11."
   - `from __future__ import annotations`.
   - Imports: `import pathlib`, `import pytest`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `RUNBOOK = pathlib.Path(__file__).resolve().parents[2] / "docs" / "admin" / "repo-settings.md"`
     - `D10_PLACEHOLDER = "2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut."`
   - Test functions:
     - `test_runbook_exists` — `assert RUNBOOK.is_file()`.
     - `test_runbook_has_pages_enablement_section` — `assert "## 5. Enable GitHub Pages" in RUNBOOK.read_text()`.
     - `test_runbook_section_5_uses_gh_api_pages_endpoint` — `assert "gh api repos/yves-vogl/aws-eks-helm-deploy/pages" in RUNBOOK.read_text()` (RESEARCH Q6 verbatim payload).
     - `test_runbook_has_mike_set_default_section` — `assert "## 6. Set default mike alias to v2" in RUNBOOK.read_text()`.
     - `test_runbook_section_6_uses_mike_set_default_push` — `assert "mike set-default v2 --push" in RUNBOOK.read_text()`.
     - `test_runbook_has_mike_v1_deploy_section` — `assert "## 7. Deploy frozen v1 docs snapshot" in RUNBOOK.read_text()`.
     - `test_runbook_section_7_uses_mike_deploy_push_v1` — `assert "mike deploy --push v1" in RUNBOOK.read_text()`.
     - `test_runbook_has_marketplace_section` — `assert "## 8. Update Bitbucket Pipe Marketplace listing" in RUNBOOK.read_text()`.
     - `test_runbook_has_docker_hub_banner_section` — `assert "## 9. Post Docker Hub README deprecation banner" in RUNBOOK.read_text()`.
     - `test_runbook_propagates_d10_placeholder` — `text = RUNBOOK.read_text()`; `assert text.count(D10_PLACEHOLDER) >= 2` (SI-07-07 — placeholder appears in Section 7 + Section 9 + possibly the Notes block).
   - Type annotations: full mypy --strict.

3. EXTEND `tests/structural/test_governance_files.py`. ADD ONE new test function (do NOT mutate existing tests):

   ```python
   def test_pr_template_has_maintainer_checklist_for_v2_release_ceremony() -> None:
       """Phase 7 / Plan 07-07 / D11: PR template surfaces the v2.0.0 maintainer checklist."""
       text = PR_TEMPLATE.read_text()
       assert "Maintainer manual steps after merge" in text, (
           "Expected PR template to surface the v2.0.0 maintainer-checklist (D11)"
       )
       assert "git tag v2.0.0" in text, (
           "PR template MUST include the `git tag v2.0.0 && git push --tags` command"
       )
       assert "mike set-default v2" in text, (
           "PR template MUST reference the mike set-default v2 one-shot (Section 6)"
       )
       assert "Docker Hub README" in text, (
           "PR template MUST reference the Docker Hub banner step (Section 9)"
       )
   ```

   `PR_TEMPLATE` is already a module constant in this file (Phase 6 — `.github/PULL_REQUEST_TEMPLATE.md`).

4. Verify:
   - `uv run pytest tests/structural/test_repo_settings_runbook.py tests/structural/test_governance_files.py -x --no-cov` — all green.
   - `uv run pytest tests/structural -q --no-cov` — full structural suite green.
   - `uv run ruff check tests/structural/test_repo_settings_runbook.py tests/structural/test_governance_files.py` — clean.
   - `uv run mypy --strict tests/structural/test_repo_settings_runbook.py tests/structural/test_governance_files.py` — clean.
  </action>
  <verify>
    <automated>grep -F 'Maintainer manual steps after merge' .github/PULL_REQUEST_TEMPLATE.md</automated>
    <automated>grep -F 'git tag v2.0.0' .github/PULL_REQUEST_TEMPLATE.md</automated>
    <automated>grep -F 'mike set-default v2' .github/PULL_REQUEST_TEMPLATE.md</automated>
    <automated>grep -F 'Docker Hub README' .github/PULL_REQUEST_TEMPLATE.md</automated>
    <automated>test -f tests/structural/test_repo_settings_runbook.py</automated>
    <automated>grep -F 'def test_runbook_has_pages_enablement_section' tests/structural/test_repo_settings_runbook.py</automated>
    <automated>grep -F 'def test_pr_template_has_maintainer_checklist_for_v2_release_ceremony' tests/structural/test_governance_files.py</automated>
    <automated>uv run pytest tests/structural/test_repo_settings_runbook.py tests/structural/test_governance_files.py -x --no-cov</automated>
    <automated>uv run pytest tests/structural -q --no-cov</automated>
    <automated>uv run pytest tests/unit tests/structural --cov=src/aws_eks_helm_deploy --cov-branch --cov-fail-under=100</automated>
  </verify>
  <acceptance_criteria>
    - PR template extended with the maintainer-checklist section (existing content preserved).
    - `tests/structural/test_repo_settings_runbook.py` ships with 10 tests; all green.
    - `tests/structural/test_governance_files.py` has 1 new test fn; all existing tests stay green.
    - Full structural suite green.
    - 100% coverage holds on `src/aws_eks_helm_deploy`.
  </acceptance_criteria>
  <threat_model>
    - SI-07-07 (D10 placeholder propagation): MITIGATED — structural test enforces ≥ 2 placeholder occurrences in runbook.
    - T-07-DOC: documentation + checklist surface; no code paths.
  </threat_model>
  <done>
    PR template + runbook structural test + extended governance test all ship and are green; v2.0.0 ceremony scope is documented for Yves at PR-review time.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Maintainer confirmation of v2.0.0 release ceremony scope (D11)</name>
  <what-built>
    All Phase 7 plans 07-01..07-06 have shipped. This plan added:
    - `docs/admin/repo-settings.md` sections 5-9 (Pages, mike one-shots, Marketplace, Docker Hub).
    - `.github/PULL_REQUEST_TEMPLATE.md` maintainer-checklist section.
    - 1 new structural test file + 1 extended test.

    Per D11, after the Phase 7 PR merges to main, Yves runs:
    1. `git tag v2.0.0 && git push --tags` → triggers release.yml.
    2. `gh api repos/.../pages -X POST ...` → enables GitHub Pages.
    3. `mike set-default v2 --push` → from a local gh-pages worktree.
    4. `mike deploy --push v1` → from a v1.3.0 checkout.
    5. Update Bitbucket Pipe Marketplace listing (web UI).
    6. Post Docker Hub README banner (web UI).
    7. Compute the absolute end-of-support date and open a follow-up PR replacing
       the `2026-MM-DD` placeholder in SECURITY.md + docs/migration/v1-to-v2.md +
       docs/admin/repo-settings.md sections 7 + 9.
  </what-built>
  <how-to-verify>
    Before approving, Yves should:

    1. Skim the Phase 7 PR body and confirm the maintainer-checklist is visible.
    2. Skim `docs/admin/repo-settings.md` sections 5-9 — verify the `gh api` and
       `mike` commands match Yves's expected mental model. RESEARCH Q6 + Q10
       pitfalls #5 + #6 inform the wording.
    3. Visually confirm `mkdocs build --strict` is green via `uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site`.
    4. Visually confirm the 9 ADRs render in the local mkdocs preview:
       `uv run mkdocs serve` and visit http://localhost:8000/adr/.
    5. Confirm `examples/` corpus parses + lints cleanly:
       `uv run check-jsonschema --builtin-schema vendor.bitbucket-pipelines examples/**/*.yml`.
    6. Confirm `docs/migration/v1-to-v2.md` reads cleanly end-to-end after the move + polish.
    7. Confirm the D10 6-month placeholder appears in all three files (SECURITY.md,
       docs/migration/v1-to-v2.md, docs/admin/repo-settings.md) — `grep -F '2026-MM-DD' SECURITY.md docs/migration/v1-to-v2.md docs/admin/repo-settings.md`.

    The maintainer-manual steps themselves run AFTER merge — they are documented,
    not executed by this PR.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — Phase 7 PR is ready to open for review (Plan 07-07 is complete).
    - `revise: <comment>` — list what to change before approving.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/admin/repo-settings.md → maintainer | Runbook is read by Yves; no programmatic execution. |
| PR template → reviewer / Yves | Checklist surfaces the manual steps but doesn't execute them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| SI-07-07 | Tampering | D10 placeholder stale across multiple files | mitigate | Structural tests in Plans 07-04 + 07-06 + 07-07 each assert the placeholder; Plan 07-07 explicitly documents the multi-file replacement step. |
| T-07-DOC | Information Disclosure | Public runbook URLs | accept | Public-by-design; URLs are read-only and require auth to act on. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | NO new deps in this plan. |
</threat_model>

<verification>
After all tasks complete:

1. `uv run pytest tests/structural -q --no-cov` — full structural suite green.
2. `uv run pytest tests/unit tests/structural --cov-fail-under=100` — coverage gate green.
3. `uv run mkdocs build --strict --site-dir /tmp/mkdocs-test-site` exits 0.
4. `grep -F '2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.' SECURITY.md docs/migration/v1-to-v2.md docs/admin/repo-settings.md` returns ≥ 4 hits (1 SECURITY.md + 1 docs/migration + 2-3 docs/admin/repo-settings.md sections 7+9+Notes).
5. `grep -F 'Maintainer manual steps after merge' .github/PULL_REQUEST_TEMPLATE.md` returns 1 hit.
6. `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2 (D6 invariant).
7. Yves approved the maintainer checkpoint.
</verification>

<success_criteria>
- D11 v2.0.0 release ceremony scope is documented in two places (runbook + PR template).
- 5 new runbook sections ship with verbatim `gh api` / `mike` commands.
- Sections 1-4 of the runbook are byte-identical to pre-edit form.
- SI-07-07 placeholder propagated to 3 source files.
- Maintainer checkpoint passed.
- D6 subprocess invariant preserved.
- All structural tests green; 100% coverage holds.
</success_criteria>

<output>
Create `.planning/phases/07-documentation-site-migration-guide/07-07-SUMMARY.md` documenting:
- The 5 new runbook section headings (verbatim, for verifier cross-reference).
- Confirmation: sections 1-4 of `docs/admin/repo-settings.md` are byte-identical to pre-edit form.
- The PR-template checklist content (or a pointer to the diff).
- Confirmation: `grep -F '2026-MM-DD...' SECURITY.md docs/migration/v1-to-v2.md docs/admin/repo-settings.md` returns ≥ 4 hits.
- Confirmation: `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2.
- Maintainer checkpoint resolution (approved / revisions applied).
</output>
</content>
</invoke>