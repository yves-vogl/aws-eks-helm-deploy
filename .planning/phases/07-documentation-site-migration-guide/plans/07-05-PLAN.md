---
phase: 07-documentation-site-migration-guide
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .github/workflows/docs.yml
  - tests/structural/test_docs_yml_structure.py
autonomous: true
requirements: [DOC-02]
requirements_addressed: [DOC-02]

must_haves:
  truths:
    - "`.github/workflows/docs.yml` deploys the mkdocs-material site to GitHub Pages via `mike deploy --push --update-aliases v2 latest` (D9 + D2)."
    - "Workflow triggers: `push` to `main` (deploy v2 + latest aliases), `push` of `v*` tag (deploy versioned alias), `workflow_dispatch` (manual republish)."
    - "Permissions block: `contents: write` ONLY (to push to gh-pages branch). NO `id-token: write` anywhere in this workflow — SI-07-01 grep gate enforces (Phase 6 Pitfall #1 carry-forward; PR-triggered + push-triggered workflows MUST NOT request id-token elevation)."
    - "`concurrency: { group: docs-deploy-${{ github.ref }}, cancel-in-progress: false }` (RESEARCH Q10 pitfall #1 — `cancel-in-progress: false` queues rather than cancels; prevents `mike push` non-fast-forward races on gh-pages branch)."
    - "Strict-mode build: `uv run mkdocs build --strict` runs BEFORE `mike deploy` (Plan 07-04 retargeted the cross-links so the build is green; this workflow re-confirms on every CI run)."
    - "Generator-before-build ORDER (RESEARCH Q10 pitfall #4): `uv run scripts/generate-variables-doc.py` runs BEFORE `mkdocs build --strict` so the variable reference exists on disk before strict-mode link validation."
    - "Every `uses:` action reference is pinned to a 40-char SHA digest from RESEARCH Q5 (actions/checkout v4.2.2 → `11bd71901bbe5b1630ceea73d27597364c9af683`, astral-sh/setup-uv v3.2.4 → `caf0cab7a618c569241d31dcd442f54681755d39`). The existing Phase 6 `test_workflow_digest_pins.py` enforces this structurally."
    - "`mike deploy v1` is OUT of CI scope — Plan 07-07 documents it as a one-shot maintainer command in `docs/admin/repo-settings.md` (RESEARCH Q10 pitfall #6: never run from CI)."
    - "Maintenance steps (Pages enablement via `gh api`, `mike set-default v2 --push`, `mike deploy v1`) live in `docs/admin/repo-settings.md` per Plan 07-07 — this plan only ships the CI workflow."
    - "`tests/structural/test_docs_yml_structure.py` asserts: workflow exists, no `id-token`, concurrency settings, strict-build step ORDER, mike deploy step, generator-before-build ordering, all `uses:` 40-char SHA pinned."
  artifacts:
    - path: ".github/workflows/docs.yml"
      provides: "GitHub Pages deploy via mkdocs + mike (D9); mirrors Phase 6 supply-chain hardening"
      contains: "mike deploy --push --update-aliases"
    - path: "tests/structural/test_docs_yml_structure.py"
      provides: "Structural assertions on docs.yml shape (D9 invariants)"
      contains: "def test_docs_yml_does_not_request_id_token"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "scripts/generate-variables-doc.py"
      via: "step `uv run scripts/generate-variables-doc.py` runs BEFORE the strict build"
      pattern: "scripts/generate-variables-doc.py"
    - from: ".github/workflows/docs.yml"
      to: "gh-pages branch"
      via: "mike deploy --push updates the gh-pages tree; concurrency group prevents races"
      pattern: "gh-pages"
---

<objective>
Ship `.github/workflows/docs.yml` per CONTEXT D9: triggers on `push: branches: [main]`, `push: tags: [v*]`, and `workflow_dispatch`; builds the mkdocs-material site strict-mode; deploys to GitHub Pages via `mike deploy --push --update-aliases v2 latest`. Permissions are least-privilege: `contents: write` ONLY (NO `id-token: write` — Phase 6 SI carry-forward / Pitfall #1). Concurrency group prevents gh-pages races (RESEARCH Q10 pitfall #1).

Purpose: DOC-02 SC-1 + SC-3 mandate "mkdocs-material + mike site is deployed to GitHub Pages via `.github/workflows/docs.yml`; `mike list` shows both `v1` (frozen) and `v2` (default)". This plan ships SC-1 + SC-3 — `v2 + latest` lands automatically on every `main` commit. The `v1` snapshot is a one-shot maintainer command documented in Plan 07-07's `docs/admin/repo-settings.md` extension (pitfall #6).

Output:
- `.github/workflows/docs.yml` — single deploy job, 7-8 steps, mirrors Phase 6 release.yml supply-chain shape (digest pins, no id-token elevation, concurrency).
- `tests/structural/test_docs_yml_structure.py` — structural gate for D9 invariants.

This plan depends on Plan 07-01 (mkdocs.yml exists) and SHOULD run AFTER Plan 07-03 commits the autogenerated `docs/reference/variables.md` (otherwise the workflow's `--strict` build fails on the missing file). Wave 2 ordering: 07-03, 07-04, 07-05 are parallel-safe at the file level, but the workflow's first GREEN run requires 07-03's generator output to be on disk (which it is, because Plan 07-03 commits `docs/reference/variables.md`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md
@.planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md
@.planning/phases/07-documentation-site-migration-guide/07-01-SUMMARY.md
@.planning/phases/06-release-pipeline-supply-chain/06-CONTEXT.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@.github/workflows/cosign-verify.yml
@tests/structural/test_ci_yml_structure.py
@tests/structural/test_workflow_digest_pins.py
@mkdocs.yml
</context>

<artifacts_this_phase_produces>
- `.github/workflows/docs.yml` (NEW; single-job workflow)
- `tests/structural/test_docs_yml_structure.py` (NEW)
</artifacts_this_phase_produces>

<files_not_touched>
SUBPROCESS INVARIANT CARRIERS (D6 — Phase 7 introduces ZERO new src/ subprocess callers):
- `src/aws_eks_helm_deploy/helm/client.py` — NOT TOUCHED.
- `src/aws_eks_helm_deploy/chart/oci.py` — NOT TOUCHED.

Other invariant carriers:
- `.github/workflows/ci.yml` — NOT TOUCHED (Plan 07-03 extends with docs-drift job).
- `.github/workflows/release.yml`, `cosign-verify.yml`, `scorecard.yml`, `codeql.yml`, `dependabot-auto-merge.yml`, `release-please.yml`, `security-rescan.yml` — NOT TOUCHED.
- `mkdocs.yml` — NOT TOUCHED (Plan 07-01 ships it; this workflow consumes it).
- `pyproject.toml` — NOT TOUCHED (Plan 07-01 + 07-03 + 07-04 ship the dep changes).
- `docs/**/*.md` — NOT TOUCHED (content authored by Plans 07-02 + 07-04 + 07-06 + 07-07).
- `examples/**/*` — NOT TOUCHED (Plan 07-04).
- `scripts/**/*` — NOT TOUCHED (Plan 07-03).
</files_not_touched>

<tasks>

<task type="auto">
  <name>Task 1: Author `.github/workflows/docs.yml`</name>
  <files>.github/workflows/docs.yml</files>
  <read_first>
    - .github/workflows/ci.yml (Phase 6 — match permissions block style, concurrency block style, digest-pin comment style)
    - .github/workflows/release.yml (Phase 6 — confirms `astral-sh/setup-uv` version pin (`0.11.21`) used project-wide)
    - .github/workflows/cosign-verify.yml (Phase 6 — confirms NO `id-token: write` in non-release workflows is the project pattern)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (Q5 digest table; Q10 pitfall #1 concurrency; Q10 pitfall #4 generator-before-build order; Q10 pitfall #7 no id-token; §Code Examples docs.yml outline)
  </read_first>
  <action>
Create `.github/workflows/docs.yml` per RESEARCH §Code Examples + Q10 pitfalls. Verbatim shape:

```yaml
# Phase 7 / DOC-02 — mkdocs-material + mike deploy to GitHub Pages.
# Triggers:
#   - push to main → deploy v2 + latest aliases.
#   - push of v* tag → deploy the versioned alias (informational; v2.0.0 is the first such tag).
#   - workflow_dispatch → manual republish (useful if gh-pages drifts).
# Pitfall #1 (Phase 6): NO `id-token: write` anywhere — docs.yml does no OIDC signing.
# Pitfall #4 (RESEARCH Q10): generator MUST run before `mkdocs build --strict` so
# docs/reference/variables.md exists on disk for strict-mode link validation.
# Pitfall #1 (RESEARCH Q10): concurrency group with cancel-in-progress: false
# prevents mike push non-fast-forward races on the gh-pages branch.

name: docs

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write  # mike pushes to the gh-pages branch.
  # id-token: NOT requested — docs.yml does no OIDC signing (SI-07-01).

concurrency:
  group: docs-deploy-${{ github.ref }}
  cancel-in-progress: false  # queue, don't cancel — RESEARCH Q10 pitfall #1.

jobs:
  deploy:
    name: "docs (mkdocs --strict + mike deploy v2 + latest)"
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout (full history for gh-pages)
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0  # mike needs full history for gh-pages branch ops.

      - name: Install uv
        uses: astral-sh/setup-uv@caf0cab7a618c569241d31dcd442f54681755d39 # v3.2.4
        with:
          version: "0.11.21"

      - name: Install Python 3.13
        run: uv python install 3.13

      - name: uv sync (docs extra)
        run: uv sync --extra docs --frozen

      # ORDER MATTERS — generator MUST precede strict build (RESEARCH Q10 pitfall #4).
      - name: Regenerate docs/reference/variables.md
        run: uv run scripts/generate-variables-doc.py

      - name: Build site (strict)
        run: uv run mkdocs build --strict

      - name: Configure git for mike
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: mike deploy v2 + latest alias
        run: uv run mike deploy --push --update-aliases v2 latest
```

**Hard-rule verification before writing:**
- All `uses:` references end in 40-char SHA + `# vX.Y.Z` comment per Phase 6 pattern.
- `name: docs` (lowercase, matches Phase 6 `name: ci` / `name: release` style).
- `runs-on: ubuntu-24.04` (NOT `ubuntu-latest` — D4 from Phase 6).
- `permissions:` block has ONLY `contents: write` (no `id-token`, no `packages`, no `security-events`).
- `concurrency.cancel-in-progress: false` (NOT `true` — RESEARCH Q10 pitfall #1).
- Order: checkout → install uv → install python → uv sync --extra docs → generate variables.md → mkdocs build --strict → configure git → mike deploy. NO other steps.
- `mike deploy --push --update-aliases v2 latest` is the exact command (RESEARCH §Code Examples verbatim).
- NO `mike set-default v2` in CI (one-shot maintainer command per Plan 07-07).
- NO `mike deploy v1` in CI (one-shot maintainer command per RESEARCH Q10 pitfall #6).

Save the file with a trailing newline. Verify YAML parses:
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/docs.yml'))"` exits 0.
  </action>
  <verify>
    <automated>test -f .github/workflows/docs.yml</automated>
    <automated>grep -F 'name: docs' .github/workflows/docs.yml</automated>
    <automated>grep -F 'runs-on: ubuntu-24.04' .github/workflows/docs.yml</automated>
    <automated>grep -F 'contents: write' .github/workflows/docs.yml</automated>
    <automated>! grep -E 'id-token:\s*write' .github/workflows/docs.yml</automated>
    <automated>grep -F 'docs-deploy-${{ github.ref }}' .github/workflows/docs.yml</automated>
    <automated>grep -F 'cancel-in-progress: false' .github/workflows/docs.yml</automated>
    <automated>grep -F 'mkdocs build --strict' .github/workflows/docs.yml</automated>
    <automated>grep -F 'mike deploy --push --update-aliases v2 latest' .github/workflows/docs.yml</automated>
    <automated>grep -F 'scripts/generate-variables-doc.py' .github/workflows/docs.yml</automated>
    <automated>grep -F '11bd71901bbe5b1630ceea73d27597364c9af683' .github/workflows/docs.yml</automated>
    <automated>grep -F 'caf0cab7a618c569241d31dcd442f54681755d39' .github/workflows/docs.yml</automated>
    <automated>! grep -E 'pull_request' .github/workflows/docs.yml</automated>
    <automated>! grep -F 'mike set-default' .github/workflows/docs.yml</automated>
    <automated>! grep -F 'mike deploy --push v1' .github/workflows/docs.yml</automated>
    <automated>uv run pytest tests/structural/test_workflow_digest_pins.py -x --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/docs.yml` exists with the verbatim shape above.
    - Permissions block has ONLY `contents: write` (no id-token, no packages, no security-events).
    - Concurrency group is `docs-deploy-${{ github.ref }}` with `cancel-in-progress: false`.
    - `mkdocs build --strict` precedes `mike deploy` in step order.
    - `uv run scripts/generate-variables-doc.py` precedes `mkdocs build --strict` in step order.
    - All `uses:` references match RESEARCH Q5 verified SHAs verbatim.
    - The Phase 6 `test_workflow_digest_pins.py` test stays green (every `uses:` in EVERY workflow file in `.github/workflows/` is 40-char SHA pinned).
    - NO `mike set-default v2` (one-shot maintainer command).
    - NO `mike deploy --push v1` (one-shot maintainer command per Q10 pitfall #6).
    - NO `pull_request` trigger (Q10 pitfall #7: docs.yml is push + tags + workflow_dispatch only).
  </acceptance_criteria>
  <threat_model>
    - SI-07-01 (no `id-token: write` in docs.yml): MITIGATED structurally by the grep gate.
    - SI-07-02 (all actions 40-char SHA pinned): MITIGATED via RESEARCH Q5 verified pins.
    - SI-07-03 (mkdocs build --strict): MITIGATED via workflow command + Plan 07-01 mkdocs.yml `strict: true`.
    - SI-07-06 (concurrency group / cancel-in-progress: false): MITIGATED structurally; the Phase 6 grep gate verifies (T-07-V14 / RESEARCH Q10 pitfall #1).
    - T-07-V4 (PR-triggered OIDC exfil): N/A — docs.yml does NOT trigger on `pull_request`; SI carry-forward from Phase 6.
  </threat_model>
  <done>
    `.github/workflows/docs.yml` ships with the supply-chain-hardened shape; existing Phase 6 digest-pin test stays green; ordering pitfalls (#4) avoided; concurrency pitfall (#1) avoided; one-shot pitfalls (#5, #6) deferred to Plan 07-07.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship `tests/structural/test_docs_yml_structure.py`</name>
  <files>tests/structural/test_docs_yml_structure.py</files>
  <read_first>
    - .github/workflows/docs.yml (Task 1 output — confirms YAML key names this test asserts on)
    - tests/structural/test_ci_yml_structure.py (Phase 6 structural test — mirror module docstring style, fixture pattern, pytestmark)
    - tests/structural/test_cosign_verify_yml.py (Phase 6 — closest sibling: also asserts NO `id-token: write` on a single-job workflow)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (Q10 pitfalls #1, #4, #7 — these become the test assertions)
  </read_first>
  <action>
Create `tests/structural/test_docs_yml_structure.py`:

- Module docstring (2 lines): "Structural tests for Phase 7 / Plan 07-05 / DOC-02 SC-1 + SC-3. Asserts `.github/workflows/docs.yml` matches CONTEXT D9 + RESEARCH Q10 pitfalls #1/#4/#7: concurrency group, strict-build-after-generator ordering, NO `id-token: write`, 40-char SHA action pins."

- `from __future__ import annotations`.

- Imports:
  - `import pathlib`
  - `from typing import Any`
  - `import pytest`
  - `import yaml`

- `pytestmark = pytest.mark.unit`.

- Module constants:
  - `DOCS_YML_PATH = pathlib.Path(__file__).resolve().parents[2] / ".github" / "workflows" / "docs.yml"`

- Module-level fixture:
  ```python
  @pytest.fixture(scope="module")
  def docs_workflow() -> dict[str, Any]:
      return yaml.safe_load(DOCS_YML_PATH.read_text())
  ```

- Test functions (named verbatim):
  - `test_docs_yml_exists` — `assert DOCS_YML_PATH.is_file()`.
  - `test_docs_yml_workflow_name_is_docs(docs_workflow)` — `assert docs_workflow.get("name") == "docs"`.
  - `test_docs_yml_triggers_on_push_main_and_tags(docs_workflow)` — examine `docs_workflow["on"]`. Triggers can be parsed as either a dict (`{"push": {...}, "workflow_dispatch": None}`) or a list (`["push", "workflow_dispatch"]`); handle both shapes. Assert `push` AND `workflow_dispatch` are present. If `push` is a dict, assert `branches == ["main"]` AND `tags == ["v*"]` (or that the list contains both).
  - `test_docs_yml_does_NOT_trigger_on_pull_request(docs_workflow)` — assert `pull_request` is NOT in the trigger keys (RESEARCH Q10 pitfall #7).
  - `test_docs_yml_does_not_trigger_on_pull_request_target(docs_workflow)` — assert `pull_request_target` is NOT in the trigger keys (Phase 6 SI carry-forward).
  - `test_docs_yml_permissions_block_minimal(docs_workflow)` — assert `docs_workflow.get("permissions") == {"contents": "write"}` (exact equality — least privilege per D9). Failure message: `"docs.yml permissions block MUST be exactly {'contents': 'write'} per CONTEXT D9 + SI-07-01"`.
  - `test_docs_yml_does_not_request_id_token(docs_workflow)` — iterate the workflow-level permissions AND every job's permissions; assert `id-token` is NOT a key in any of them. Failure message cites Phase 6 Pitfall #1.
  - `test_docs_yml_has_concurrency_group(docs_workflow)` — `concurrency = docs_workflow.get("concurrency", {})`; assert `concurrency.get("group") == "docs-deploy-${{ github.ref }}"`; assert `concurrency.get("cancel-in-progress") is False` (NOT True). Failure message cites RESEARCH Q10 pitfall #1.
  - `test_docs_yml_deploy_job_uses_ubuntu_24_04(docs_workflow)` — `deploy = docs_workflow["jobs"]["deploy"]`; assert `deploy["runs-on"] == "ubuntu-24.04"` (NOT `ubuntu-latest`).
  - `test_docs_yml_step_order_generator_before_build(docs_workflow)` — collect the step names of the `deploy` job. Find the index of the step that runs `scripts/generate-variables-doc.py` (search the `run:` strings for that substring). Find the index of the step that runs `mkdocs build --strict`. Assert `generator_idx < build_idx`. Failure cites RESEARCH Q10 pitfall #4.
  - `test_docs_yml_uses_mike_deploy_with_aliases(docs_workflow)` — find the step whose `run:` contains `mike deploy --push --update-aliases v2 latest`; assert it exists.
  - `test_docs_yml_does_not_run_mike_set_default(docs_workflow)` — assert no step's `run:` contains `mike set-default` (one-shot maintainer command).
  - `test_docs_yml_does_not_run_mike_v1_deploy(docs_workflow)` — assert no step's `run:` contains `mike deploy --push v1` (RESEARCH Q10 pitfall #6 — one-shot maintainer command).

- Type annotations: full mypy --strict.

Run:
- `uv run pytest tests/structural/test_docs_yml_structure.py -x --no-cov` — all green.
- `uv run ruff check tests/structural/test_docs_yml_structure.py` — clean.
- `uv run mypy --strict tests/structural/test_docs_yml_structure.py` — clean.
  </action>
  <verify>
    <automated>test -f tests/structural/test_docs_yml_structure.py</automated>
    <automated>grep -F 'def test_docs_yml_does_not_request_id_token' tests/structural/test_docs_yml_structure.py</automated>
    <automated>grep -F 'def test_docs_yml_has_concurrency_group' tests/structural/test_docs_yml_structure.py</automated>
    <automated>grep -F 'def test_docs_yml_step_order_generator_before_build' tests/structural/test_docs_yml_structure.py</automated>
    <automated>grep -F 'def test_docs_yml_does_not_run_mike_set_default' tests/structural/test_docs_yml_structure.py</automated>
    <automated>grep -F 'def test_docs_yml_does_not_run_mike_v1_deploy' tests/structural/test_docs_yml_structure.py</automated>
    <automated>uv run pytest tests/structural/test_docs_yml_structure.py -x --no-cov</automated>
    <automated>uv run pytest tests/structural -q --no-cov</automated>
    <automated>uv run ruff check tests/structural/test_docs_yml_structure.py</automated>
    <automated>uv run mypy --strict tests/structural/test_docs_yml_structure.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/structural/test_docs_yml_structure.py` ships with 12 test functions covering every D9 + Q10 pitfall invariant.
    - All 12 tests green against Task 1's docs.yml.
    - Phase 6 structural suite (test_ci_yml_structure.py, test_workflow_digest_pins.py, etc.) stays green.
    - ruff + mypy --strict both clean.
  </acceptance_criteria>
  <threat_model>
    Same threat dispositions as Task 1 — this is the structural enforcement layer.
  </threat_model>
  <done>
    Structural test file ships with full pitfall coverage; docs.yml is gated against regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → gh-pages branch | `mike deploy --push` writes to gh-pages; concurrency group prevents races (Q10 pitfall #1). |
| Workflow → external actions (`actions/checkout`, `astral-sh/setup-uv`) | All actions pinned to 40-char SHA (Q5). |
| docs.yml → GitHub OIDC | NOT used — workflow does no OIDC signing (SI-07-01 carry-forward from Phase 6 Pitfall #1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-V4-OIDC | EoP | docs.yml requesting `id-token: write` accidentally | mitigate | structural test `test_docs_yml_does_not_request_id_token`; SI-07-01 grep gate. |
| T-07-V10 | Tampering | Compromised mutable action tag | mitigate | every `uses:` pinned to 40-char SHA (RESEARCH Q5); Phase 6 `test_workflow_digest_pins.py` enforces structurally. |
| T-07-V14-RACE | Repudiation/DoS | mike push non-fast-forward on gh-pages | mitigate | concurrency group `docs-deploy-${{ github.ref }}` with `cancel-in-progress: false` (Q10 pitfall #1). |
| T-07-V14-ORDER | Tampering | mkdocs strict fails because variables.md missing | mitigate | generator step ordered BEFORE strict build (Q10 pitfall #4); structural test asserts ordering. |
| T-07-V4-PT | EoP | malicious `pull_request_target` elevation | mitigate | NO pull_request_target trigger; structural test asserts. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dep introduced by this plan; uses Plan 07-01's `docs` extra. |
</threat_model>

<verification>
After all tasks complete:

1. `uv run pytest tests/structural -q --no-cov` — full suite green including new docs.yml gate.
2. `grep -F 'id-token: write' .github/workflows/docs.yml` returns 0 hits (SI-07-01).
3. `grep -F 'docs-deploy-${{ github.ref }}' .github/workflows/docs.yml` returns 1 hit (SI-07-06).
4. `grep -F 'cancel-in-progress: false' .github/workflows/docs.yml` returns 1 hit (SI-07-06).
5. `grep -F 'mkdocs build --strict' .github/workflows/docs.yml` returns 1 hit (SI-07-03).
6. `grep -F 'mike deploy --push --update-aliases v2 latest' .github/workflows/docs.yml` returns 1 hit.
7. `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2 (D6 invariant).
8. `uv run pytest tests/structural/test_workflow_digest_pins.py -x --no-cov` — Phase 6 digest gate green against the new docs.yml.
9. The Phase 7 PR's first CI run shows the new `docs` workflow as queued (it triggers on push to main, not on the PR itself); the PR's `ci` workflow remains the gating check.
</verification>

<success_criteria>
- DOC-02 SC-1 + SC-3 contract met: docs.yml deploys v2 + latest aliases on every main push.
- Concurrency group prevents gh-pages races (Q10 pitfall #1).
- Generator runs BEFORE strict build (Q10 pitfall #4).
- No `id-token: write` (SI-07-01).
- All actions 40-char SHA pinned (SI-07-02).
- `mike deploy --push v1` and `mike set-default v2` deferred to Plan 07-07's maintainer runbook (Q10 pitfalls #5 + #6).
- D6 subprocess invariant preserved.
- Structural test file ships with 12 pitfall-coverage assertions.
</success_criteria>

<output>
Create `.planning/phases/07-documentation-site-migration-guide/07-05-SUMMARY.md` documenting:
- The exact action SHAs used in docs.yml (verifier cross-checks via `gh api repos/<action>/git/refs/tags/<tag>`).
- Confirmation: `grep -F 'id-token' .github/workflows/docs.yml` returns 0 (SI-07-01).
- The new workflow's `name:` field (`docs`) for branch-protection awareness (Plan 07-07 may want to surface it; docs.yml is push-triggered, NOT PR-triggered, so it does NOT need to be a required status check).
- Confirmation: `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2.
- Note: the FIRST run of docs.yml will happen on the Phase 7 PR merge to main; the maintainer should monitor the gh-pages branch creation in real time.
</output>
</content>
</invoke>