---
phase: 07-documentation-site-migration-guide
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - README.md
  - SECURITY.md
  - tests/structural/test_readme_polish.py
  - tests/structural/test_security_md.py
  - tests/structural/test_contributing.py
  - tests/structural/test_code_of_conduct.py
autonomous: true
requirements: [DOC-01, DOC-05, DOC-06, DOC-07]
requirements_addressed: [DOC-01, DOC-05, DOC-06, DOC-07]

must_haves:
  truths:
    - "`README.md` badge row is EXTENDED additively with 3 new badges (sponsors, stars, open issues) per D6 — existing 7 badges are NOT reordered (D6: 'additions, no reorder')."
    - "`SECURITY.md` 6-month wording is sharpened per D10 + the literal placeholder `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.` is committed verbatim (SI-07-07 gate); the existing 90-day coordinated disclosure window stays unchanged (D10 explicit constraint)."
    - "`CONTRIBUTING.md` VERIFICATION (DOC-05): the existing file already documents `uv sync` / `pre-commit` / `pytest` / `kind` loop + Conventional-Commits rule; this plan SHIPS A STRUCTURAL TEST that asserts the required tokens are present — and edits the file ONLY if any required token is missing."
    - "`CODE_OF_CONDUCT.md` VERIFICATION (DOC-07): the existing file references Contributor Covenant 2.1; structural test asserts it; no edit if already correct."
    - "Plan 07-01 shipped a WAVE-1 placeholder `tests/structural/test_readme_polish.py` — this plan HARDENS it (adds assertions for sponsors / stars / open issues badges + the new docs-site link)."
    - "Three new structural test files ship: `test_security_md.py`, `test_contributing.py`, `test_code_of_conduct.py` (DOC-05/06/07 gates)."
    - "All changes are independent (no file overlap with Wave 2 plans 07-03 / 07-04 / 07-05); Wave 3 means this plan can run in parallel with Wave 2 but ALSO can land late, since DOC-06 placeholder is consumed by Plan 07-04 (already shipped in `docs/migration/v1-to-v2.md`) and Plan 07-07 (which references this plan's SECURITY.md update)."
  artifacts:
    - path: "README.md"
      provides: "Polished landing page with 10-badge row (7 existing + 3 new additive)"
      contains: "img.shields.io/github/sponsors"
    - path: "SECURITY.md"
      provides: "D10 6-month placeholder committed; 90-day disclosure flow unchanged"
      contains: "2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut."
    - path: "tests/structural/test_readme_polish.py"
      provides: "EXTENDS Plan 07-01 placeholder with sponsors/stars/issues/docs-site assertions"
      contains: "def test_readme_has_sponsors_badge"
    - path: "tests/structural/test_security_md.py"
      provides: "DOC-06 gate: 6-month placeholder + 90-day window + PVR link"
      contains: "def test_security_md_has_d10_six_month_placeholder"
    - path: "tests/structural/test_contributing.py"
      provides: "DOC-05 gate: uv sync / pre-commit / pytest / kind tokens present"
      contains: "def test_contributing_documents_uv_sync"
    - path: "tests/structural/test_code_of_conduct.py"
      provides: "DOC-07 gate: Contributor Covenant 2.1 reference present"
      contains: "Contributor Covenant"
  key_links:
    - from: "README.md"
      to: "https://github.com/sponsors/yves-vogl"
      via: "sponsors badge links to GitHub Sponsors page"
      pattern: "github.com/sponsors/yves-vogl"
    - from: "SECURITY.md"
      to: "docs/migration/v1-to-v2.md"
      via: "Both files reference the same D10 6-month placeholder (SI-07-07 gate verifies both)"
      pattern: "2026-MM-DD"
---

<objective>
Polish the three governance files (README + SECURITY + CONTRIBUTING + CODE_OF_CONDUCT) per D6 + D10 + DOC-05/07 — additively, with structural test gates. CONTRIBUTING.md + CODE_OF_CONDUCT.md are verified-only (no edits if already correct); README.md gets 3 new badges (sponsors / stars / open issues) additively; SECURITY.md gets the literal D10 placeholder + sharpened 6-month wording.

Purpose: DOC-01 SC requires the README badge row to include 10 entries (license, release, GHCR image, build, coverage, Cosign verified, OpenSSF Scorecard, sponsors, stars, open issues — the last three are net-new additive entries per D6). DOC-05 asserts CONTRIBUTING.md documents the `uv sync` / `pre-commit` / `pytest` / `kind` loop (verification only — already present). DOC-06 asserts SECURITY.md has the v1.x 6-month security window wording (D10 placeholder enforced). DOC-07 asserts CODE_OF_CONDUCT.md references Contributor Covenant 2.1.

Output:
- `README.md` — extended badge row (3 additive badges).
- `SECURITY.md` — D10 6-month placeholder added + wording sharpened.
- `tests/structural/test_readme_polish.py` — extends Plan 07-01 placeholder.
- `tests/structural/test_security_md.py` — DOC-06 gate.
- `tests/structural/test_contributing.py` — DOC-05 gate.
- `tests/structural/test_code_of_conduct.py` — DOC-07 gate.

This plan is independent (Wave 3); no file overlap with Wave 2 plans 07-03/07-04/07-05. It can run in parallel with Waves 1-2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md
@.planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md
@README.md
@SECURITY.md
@CONTRIBUTING.md
@CODE_OF_CONDUCT.md
</context>

<artifacts_this_phase_produces>
- `README.md` (MODIFIED — additive badge row only)
- `SECURITY.md` (MODIFIED — D10 placeholder added + wording sharpened)
- `tests/structural/test_readme_polish.py` (MODIFIED — extended from Plan 07-01)
- `tests/structural/test_security_md.py` (NEW)
- `tests/structural/test_contributing.py` (NEW)
- `tests/structural/test_code_of_conduct.py` (NEW)
</artifacts_this_phase_produces>

<files_not_touched>
SUBPROCESS INVARIANT CARRIERS (D6 — Phase 7 introduces ZERO new src/ subprocess callers):
- `src/aws_eks_helm_deploy/helm/client.py` — NOT TOUCHED.
- `src/aws_eks_helm_deploy/chart/oci.py` — NOT TOUCHED.

Other invariant carriers:
- `pyproject.toml` — NOT TOUCHED (no new deps in Wave 3).
- `.github/workflows/*.yml` — NOT TOUCHED (Plans 07-03 + 07-05).
- `mkdocs.yml` — NOT TOUCHED.
- `docs/**/*.md` — NOT TOUCHED (Plans 07-02 + 07-04).
- `examples/**/*` — NOT TOUCHED (Plan 07-04).
- `scripts/**/*` — NOT TOUCHED (Plan 07-03).
- `CONTRIBUTING.md` — VERIFICATION ONLY (no edits if already correct per D6 / DOC-05 — see Task 3).
- `CODE_OF_CONDUCT.md` — VERIFICATION ONLY (no edits if already correct per D6 / DOC-07 — see Task 3).
</files_not_touched>

<tasks>

<task type="auto">
  <name>Task 1: Extend README badge row with 3 additive badges + harden test_readme_polish.py</name>
  <files>README.md, tests/structural/test_readme_polish.py</files>
  <read_first>
    - README.md (current 201-line file — badge row at lines 3-9; quickstart section + docs-site link to be wired up)
    - tests/structural/test_readme_polish.py (Plan 07-01 Wave-1 placeholder)
    - .planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md (D6 — badge order + additive policy + style)
  </read_first>
  <action>
1. Edit `README.md`. Three additive badges per D6 — order: append AFTER the existing 7-badge row, BEFORE the cold-start line:

   ```markdown
   [![License](https://img.shields.io/github/license/yves-vogl/aws-eks-helm-deploy)](LICENSE)
   [![Release](https://img.shields.io/github/v/release/yves-vogl/aws-eks-helm-deploy)](https://github.com/yves-vogl/aws-eks-helm-deploy/releases)
   [![GHCR Image](https://img.shields.io/badge/GHCR-ghcr.io%2Fyves--vogl%2Faws--eks--helm--deploy-blue)](https://github.com/yves-vogl/aws-eks-helm-deploy/pkgs/container/aws-eks-helm-deploy)
   [![CI](https://github.com/yves-vogl/aws-eks-helm-deploy/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/yves-vogl/aws-eks-helm-deploy/actions/workflows/ci.yml)
   [![Coverage](https://img.shields.io/badge/coverage-100%25-brightgreen)](https://github.com/yves-vogl/aws-eks-helm-deploy/actions/workflows/ci.yml)
   [![Cosign verified](https://img.shields.io/badge/cosign-verified-success?logo=sigstore)](https://github.com/yves-vogl/aws-eks-helm-deploy/actions/workflows/cosign-verify.yml)
   [![OpenSSF Scorecard](https://api.securityscorecards.dev/projects/github.com/yves-vogl/aws-eks-helm-deploy/badge)](https://scorecard.dev/viewer/?uri=github.com/yves-vogl/aws-eks-helm-deploy)
   [![Sponsors](https://img.shields.io/github/sponsors/yves-vogl?style=flat-square)](https://github.com/sponsors/yves-vogl)
   [![Stars](https://img.shields.io/github/stars/yves-vogl/aws-eks-helm-deploy?style=flat-square)](https://github.com/yves-vogl/aws-eks-helm-deploy/stargazers)
   [![Open issues](https://img.shields.io/github/issues/yves-vogl/aws-eks-helm-deploy?style=flat-square)](https://github.com/yves-vogl/aws-eks-helm-deploy/issues)
   ```

   D6 STYLE rules:
   - New badges use `?style=flat-square` (D6: "consistent `?style=flat-square` via shields.io for net-new badges").
   - Existing 7 badges KEEP their current style (no churn — verifier diffs them line-by-line).
   - ORDER: 7 existing → 3 new at the END.

2. Also wire the docs-site link Plan 07-01's placeholder test was waiting for. Find the line `### Basic` (around line 117 in the current README) — INSTEAD, add a prominent docs-site link earlier in the file. Recommended location: in the "Status" callout (around line 28) or as a new line right after the badge row. Use this insertion (just after the badge row, before the cold-start line):

   ```markdown
   📖 **Documentation site:** [yves-vogl.github.io/aws-eks-helm-deploy](https://yves-vogl.github.io/aws-eks-helm-deploy/) — versioned via `mike` (v1 frozen, v2 current).
   ```

3. Polish the existing "Status" callout (line 28) for v2.0 — keep it informational but tighten wording. The current text references "v2.0 is in active development". UPDATE to:

   ```markdown
   > **Status:** v2.0 is the active line published exclusively to GitHub Container Registry (`ghcr.io/yves-vogl/aws-eks-helm-deploy`). v1.3.0 is frozen on Docker Hub as the v1.x archive. See [docs/migration/v1-to-v2.md](https://github.com/yves-vogl/aws-eks-helm-deploy/blob/main/docs/migration/v1-to-v2.md) for upgrade steps.
   ```

   Conservative edit: leave the milestones link too if it's still useful — Plan 07-07 may re-edit this to point at the v2.0.0 release after tag-cut.

4. EXTEND `tests/structural/test_readme_polish.py`:
   - Plan 07-01 shipped 4 test functions; ADD 4 more:
     - `test_readme_has_sponsors_badge` — `assert "img.shields.io/github/sponsors/yves-vogl" in README_MD.read_text()`.
     - `test_readme_has_stars_badge` — `assert "img.shields.io/github/stars/yves-vogl/aws-eks-helm-deploy" in README_MD.read_text()`.
     - `test_readme_has_open_issues_badge` — `assert "img.shields.io/github/issues/yves-vogl/aws-eks-helm-deploy" in README_MD.read_text()`.
     - `test_readme_has_docs_site_link_hardened` — `assert "yves-vogl.github.io/aws-eks-helm-deploy" in README_MD.read_text()`. Replace the lenient Plan-07-01 `test_readme_has_docs_site_link` (which accepted ANY of three forms) with this hardened version — change the assertion text inline.
     - `test_readme_new_badges_use_flat_square_style` — for sponsors / stars / open issues, assert `?style=flat-square` appears in the same line; D6 style rule.
   - Update the `REQUIRED_BADGE_SOURCES` frozenset to ADD the 3 new entries (matching what Plan 07-06 ships).
   - `pytestmark = pytest.mark.unit` already present.

5. Verify:
   - `uv run pytest tests/structural/test_readme_polish.py -x --no-cov` — all 8+ tests green.
   - `uv run ruff check tests/structural/test_readme_polish.py` — clean.
   - `uv run mypy --strict tests/structural/test_readme_polish.py` — clean.
   - `uv run mkdocs build --strict` — still green (docs/index.md and docs/migration/v1-to-v2.md don't depend on README, but the docs site references the README via the `repo_url:` link).
  </action>
  <verify>
    <automated>grep -F 'img.shields.io/github/sponsors/yves-vogl' README.md</automated>
    <automated>grep -F 'img.shields.io/github/stars/yves-vogl/aws-eks-helm-deploy' README.md</automated>
    <automated>grep -F 'img.shields.io/github/issues/yves-vogl/aws-eks-helm-deploy' README.md</automated>
    <automated>grep -F 'yves-vogl.github.io/aws-eks-helm-deploy' README.md</automated>
    <automated>grep -F 'docs/migration/v1-to-v2.md' README.md</automated>
    <automated>grep -E 'style=flat-square' README.md</automated>
    <automated>grep -F 'def test_readme_has_sponsors_badge' tests/structural/test_readme_polish.py</automated>
    <automated>uv run pytest tests/structural/test_readme_polish.py -x --no-cov</automated>
    <automated>uv run ruff check tests/structural/test_readme_polish.py</automated>
    <automated>uv run mypy --strict tests/structural/test_readme_polish.py</automated>
  </verify>
  <acceptance_criteria>
    - README.md now has 10 badges (7 existing + 3 new); existing badges are byte-identical to pre-edit form (no reorder, no style churn).
    - New badges use `?style=flat-square`.
    - The docs-site link `yves-vogl.github.io/aws-eks-helm-deploy` is present prominently (near the badge row).
    - A link to `docs/migration/v1-to-v2.md` is present in the Status callout.
    - `test_readme_polish.py` now has ≥ 8 test functions; all green.
    - ruff + mypy --strict clean.
  </acceptance_criteria>
  <threat_model>
    Content-only changes. No code paths affected. No new dependencies.
  </threat_model>
  <done>
    README badge row extended additively per D6; docs-site link wired; migration guide link surfaced; hardened structural test green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sharpen SECURITY.md 6-month wording + commit D10 placeholder + ship DOC-06 structural test</name>
  <files>SECURITY.md, tests/structural/test_security_md.py</files>
  <read_first>
    - SECURITY.md (current 67-line file — `## Supported versions` section is the D10 polish target)
    - .planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md (D10 — exact wording rules; 90-day disclosure stays unchanged)
    - .planning/phases/07-documentation-site-migration-guide/07-RESEARCH.md (§Implementation Hints — SI-07-07 verifier rule)
    - docs/migration/v1-to-v2.md (Plan 07-04 — same D10 placeholder lives here too)
  </read_first>
  <action>
1. Edit `SECURITY.md` `## Supported versions` section. The current table:

   ```markdown
   | Version | Supported              |
   |---------|------------------------|
   | 2.x     | Yes (active development on `main`) |
   | 1.x     | No (frozen on Docker Hub; security-only patches at maintainer's discretion) |
   ```

   REPLACE the `1.x` row with the D10-compliant wording (per CONTEXT D10):

   ```markdown
   | Version | Supported              |
   |---------|------------------------|
   | 2.x     | Yes (active development on `main`) |
   | 1.x     | Security fixes for 6 months from the v2.0.0 release date — ending `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.` Frozen at v1.3.0 on Docker Hub. |
   ```

   IMPORTANT:
   - The placeholder string `2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.` is the EXACT verbatim string that SI-07-07 grep gate enforces. Single trailing period inside the backticks. Match it byte-for-byte.
   - Do NOT change the `## Reporting a vulnerability`, `## Disclosure timeline` (the 90-day flow stays unchanged per D10), `## Scope`, `## What we do automatically`, or `## Acknowledgements` sections.

2. Optional polish to the `## Acknowledgements` paragraph (D10 is silent on this; D6 implies the security file is in scope for sharpening). If the current paragraph is already fine, leave it. Recommendation: leave it.

3. Create `tests/structural/test_security_md.py`:
   - Module docstring: "Structural tests for Phase 7 / Plan 07-06 / DOC-06 + D10. Asserts SECURITY.md contains the 6-month placeholder verbatim, the PVR disclosure link, and the unchanged 90-day window."
   - `from __future__ import annotations`.
   - Imports: `import pathlib`, `import pytest`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `REPO_ROOT = pathlib.Path(__file__).resolve().parents[2]`
     - `SECURITY_MD = REPO_ROOT / "SECURITY.md"`
     - `D10_PLACEHOLDER = "2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut."`
   - Test functions:
     - `test_security_md_exists` — `assert SECURITY_MD.is_file()`.
     - `test_security_md_has_d10_six_month_placeholder` — `assert D10_PLACEHOLDER in SECURITY_MD.read_text()` (SI-07-07 gate).
     - `test_security_md_mentions_v1_x_six_month_window` — assert the text "6 months" appears in the v1.x supported-versions row context.
     - `test_security_md_has_pvr_link` — `assert "Private Vulnerability Reporting" in SECURITY_MD.read_text()` OR `assert "security/advisories/new" in SECURITY_MD.read_text()` (Phase 6 carries this; ensure DOC-06 verification holds).
     - `test_security_md_disclosure_timeline_unchanged` — assert the text "Day 90 (max)" appears (Day-0/14/60/90 timeline; D10 says this stays unchanged).
     - `test_security_md_has_scope_section` — `assert "## Scope" in SECURITY_MD.read_text()` (DOC-06 + Phase 6 governance — present at repo root and linked from docs site).
   - Type annotations: full mypy --strict.

4. Verify:
   - `uv run pytest tests/structural/test_security_md.py -x --no-cov` — all green.
   - `uv run ruff check tests/structural/test_security_md.py` — clean.
   - `uv run mypy --strict tests/structural/test_security_md.py` — clean.
   - Cross-file consistency: `grep -F '2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.' SECURITY.md docs/migration/v1-to-v2.md` returns 2 hits (one per file). If `docs/migration/v1-to-v2.md` is missing the placeholder, Plan 07-04 is incomplete — STOP and flag.
  </action>
  <verify>
    <automated>grep -F '2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.' SECURITY.md</automated>
    <automated>grep -F '6 months' SECURITY.md</automated>
    <automated>grep -F 'Private Vulnerability Reporting' SECURITY.md</automated>
    <automated>grep -F 'Day 90 (max)' SECURITY.md</automated>
    <automated>test -f tests/structural/test_security_md.py</automated>
    <automated>grep -F 'def test_security_md_has_d10_six_month_placeholder' tests/structural/test_security_md.py</automated>
    <automated>uv run pytest tests/structural/test_security_md.py -x --no-cov</automated>
    <automated>uv run ruff check tests/structural/test_security_md.py</automated>
    <automated>uv run mypy --strict tests/structural/test_security_md.py</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md `## Supported versions` table has the D10-compliant v1.x row with the literal placeholder string.
    - SECURITY.md `## Disclosure timeline` is unchanged (Day-0/14/60/90 still present).
    - `tests/structural/test_security_md.py` ships with 6 tests; all green.
    - Cross-file: grep for the placeholder in BOTH SECURITY.md AND docs/migration/v1-to-v2.md returns 2 hits.
    - ruff + mypy --strict clean.
  </acceptance_criteria>
  <threat_model>
    - SI-07-07 (D10 placeholder verbatim): MITIGATED structurally by `test_security_md_has_d10_six_month_placeholder`.
    - T-07-DOC: documentation surface; no code paths.
    - Note: the actual replacement of `2026-MM-DD` with the real end-of-support date happens AFTER the v2.0.0 tag-cut in a follow-up patch by the maintainer (Plan 07-07 documents the step in `docs/admin/repo-settings.md`).
  </threat_model>
  <done>
    SECURITY.md polished per D10; literal placeholder committed; structural test ships and is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify CONTRIBUTING.md (DOC-05) + CODE_OF_CONDUCT.md (DOC-07) via new structural tests (no file edits if already correct)</name>
  <files>tests/structural/test_contributing.py, tests/structural/test_code_of_conduct.py</files>
  <read_first>
    - CONTRIBUTING.md (current 68-line file — confirms `uv sync`, `pre-commit`, `pytest`, `kind` tokens already present)
    - CODE_OF_CONDUCT.md (current 11-line file — references Contributor Covenant 2.1)
    - .planning/phases/07-documentation-site-migration-guide/07-CONTEXT.md (D6 — verification only if file already correct)
  </read_first>
  <action>
1. READ `CONTRIBUTING.md` and verify the following tokens are present (no edit needed in most cases):
   - `uv sync` (verified in current file: line 14 `make bootstrap` invokes uv; line 14 `uv sync --all-extras --frozen` literal).
   - `pre-commit` (verified: line 22).
   - `pytest` (verified: line 19 `make test` runs pytest with coverage gate).
   - `kind` (verified: line 20 `make integration-test` references kind).
   - `Conventional Commits` (verified: line 23).

   **IF any token is MISSING**, edit CONTRIBUTING.md to add it (minimal additive edit). **EXPECTED OUTCOME**: zero file edits needed (the current file already covers DOC-05).

2. READ `CODE_OF_CONDUCT.md` and verify:
   - References Contributor Covenant 2.1 (verified: line 3 — "[Contributor Covenant](https://www.contributor-covenant.org/), version 2.1").
   - Has a Reporting section (verified: line 5).
   - Has an Enforcement section (verified: line 9).

   **IF any of these are missing**, edit minimally. **EXPECTED OUTCOME**: zero file edits needed.

3. Create `tests/structural/test_contributing.py`:
   - Module docstring: "Structural tests for Phase 7 / Plan 07-06 / DOC-05. Asserts CONTRIBUTING.md documents the uv sync / pre-commit / pytest / kind development loop and the Conventional Commits rule."
   - `from __future__ import annotations`.
   - Imports: `import pathlib`, `import pytest`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `CONTRIBUTING_MD = pathlib.Path(__file__).resolve().parents[2] / "CONTRIBUTING.md"`
     - `REQUIRED_TOKENS: frozenset[str] = frozenset({"uv sync", "pre-commit", "pytest", "kind", "Conventional Commits"})`
   - Test functions:
     - `test_contributing_md_exists` — `assert CONTRIBUTING_MD.is_file()`.
     - `test_contributing_documents_uv_sync` — `assert "uv sync" in CONTRIBUTING_MD.read_text()`.
     - `test_contributing_documents_pre_commit` — `assert "pre-commit" in CONTRIBUTING_MD.read_text()`.
     - `test_contributing_documents_pytest` — `assert "pytest" in CONTRIBUTING_MD.read_text()`.
     - `test_contributing_documents_kind` — `assert "kind" in CONTRIBUTING_MD.read_text()`.
     - `test_contributing_documents_conventional_commits` — `assert "Conventional Commits" in CONTRIBUTING_MD.read_text()`.
     - `test_contributing_all_required_tokens_covered` — for each token in `REQUIRED_TOKENS`, assert it appears; failure lists missing tokens.
   - Type annotations: full mypy --strict.

4. Create `tests/structural/test_code_of_conduct.py`:
   - Module docstring: "Structural tests for Phase 7 / Plan 07-06 / DOC-07. Asserts CODE_OF_CONDUCT.md references Contributor Covenant 2.1 with Reporting + Enforcement sections."
   - `from __future__ import annotations`.
   - Imports: `import pathlib`, `import pytest`.
   - `pytestmark = pytest.mark.unit`.
   - Module constants:
     - `COC_MD = pathlib.Path(__file__).resolve().parents[2] / "CODE_OF_CONDUCT.md"`
   - Test functions:
     - `test_code_of_conduct_md_exists` — `assert COC_MD.is_file()`.
     - `test_code_of_conduct_references_contributor_covenant_2_1` — `text = COC_MD.read_text()`; assert `"Contributor Covenant" in text` AND `"2.1" in text`.
     - `test_code_of_conduct_has_reporting_section` — `assert "## Reporting" in COC_MD.read_text()` (or case-insensitive match).
     - `test_code_of_conduct_has_enforcement_section` — `assert "## Enforcement" in COC_MD.read_text()`.
   - Type annotations: full mypy --strict.

5. Verify:
   - `uv run pytest tests/structural/test_contributing.py tests/structural/test_code_of_conduct.py -x --no-cov` — both green.
   - `uv run pytest tests/structural -q --no-cov` — full structural suite green.
   - `uv run ruff check tests/structural/test_contributing.py tests/structural/test_code_of_conduct.py` — clean.
   - `uv run mypy --strict tests/structural/test_contributing.py tests/structural/test_code_of_conduct.py` — clean.
  </action>
  <verify>
    <automated>test -f tests/structural/test_contributing.py</automated>
    <automated>test -f tests/structural/test_code_of_conduct.py</automated>
    <automated>grep -F 'def test_contributing_documents_uv_sync' tests/structural/test_contributing.py</automated>
    <automated>grep -F 'def test_code_of_conduct_references_contributor_covenant_2_1' tests/structural/test_code_of_conduct.py</automated>
    <automated>uv run pytest tests/structural/test_contributing.py tests/structural/test_code_of_conduct.py -x --no-cov</automated>
    <automated>uv run pytest tests/structural -q --no-cov</automated>
    <automated>uv run ruff check tests/structural/test_contributing.py tests/structural/test_code_of_conduct.py</automated>
    <automated>uv run mypy --strict tests/structural/test_contributing.py tests/structural/test_code_of_conduct.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/structural/test_contributing.py` ships with 7 tests; all green against the current CONTRIBUTING.md.
    - `tests/structural/test_code_of_conduct.py` ships with 4 tests; all green against the current CODE_OF_CONDUCT.md.
    - If zero edits to CONTRIBUTING.md and CODE_OF_CONDUCT.md were needed, document that in the SUMMARY.
    - Full structural suite green.
  </acceptance_criteria>
  <threat_model>
    Verification-only. No code paths affected.
  </threat_model>
  <done>
    Two new structural test files ship; DOC-05 and DOC-07 are gated; minimal-to-zero edits to source files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README badges → shields.io | External SVG fetched on each page view; shields.io is treated as a free CDN. |
| SECURITY.md → reporter | Reporter follows the PVR link to file a confidential report; flow untouched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-V14 | Configuration | README badge churn breaks alignment | accept | Additive-only edits; existing badges byte-identical to pre-edit form. |
| SI-07-07 | Tampering | D10 placeholder stale | mitigate | structural test `test_security_md_has_d10_six_month_placeholder` (this plan) + `test_migration_guide_has_d10_six_month_placeholder` (Plan 07-04). Plan 07-07's runbook documents the post-tag-cut replacement step. |
| T-07-DOC | Information Disclosure | Public-by-design | accept | Project is open-source; governance files are intentionally public. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | NO new deps in this plan. |
</threat_model>

<verification>
After all tasks complete:

1. `uv run pytest tests/structural -q --no-cov` — full structural suite green.
2. `grep -c "shields.io" README.md` ≥ 10 (10 badges).
3. `grep -F '2026-MM-DD (= v2.0.0 release date + 6 months) — replace at tag-cut.' SECURITY.md docs/migration/v1-to-v2.md` returns 2 hits (SI-07-07 across both files).
4. `grep -F 'Contributor Covenant' CODE_OF_CONDUCT.md` returns ≥ 1 hit.
5. `grep -F 'uv sync' CONTRIBUTING.md` returns ≥ 1 hit.
6. `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2 (D6 invariant).
</verification>

<success_criteria>
- DOC-01: README has 10 badges (7 existing + 3 additive); docs-site link present; migration guide linked.
- DOC-05: CONTRIBUTING.md gated by structural test.
- DOC-06: SECURITY.md has D10 6-month placeholder verbatim; structural test enforces.
- DOC-07: CODE_OF_CONDUCT.md gated by structural test (Contributor Covenant 2.1).
- D6 subprocess invariant preserved.
- All edits are additive or verification-only.
- 4 new/extended structural test files ship.
</success_criteria>

<output>
Create `.planning/phases/07-documentation-site-migration-guide/07-06-SUMMARY.md` documenting:
- Confirmation: 3 new badges added additively; existing 7 badges byte-identical to pre-edit form.
- Whether CONTRIBUTING.md or CODE_OF_CONDUCT.md needed any edits (expected: no).
- The exact line of SECURITY.md that holds the D10 placeholder (for verifier cross-reference).
- Confirmation: `grep -F '2026-MM-DD...' SECURITY.md docs/migration/v1-to-v2.md` returns 2 hits.
- Confirmation: `grep -c "import subprocess" src/aws_eks_helm_deploy/**/*.py` returns 2.
</output>
</content>
</invoke>